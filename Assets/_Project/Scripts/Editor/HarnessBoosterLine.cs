namespace MBI.EditorTools
{
    /// <summary>
    /// 🗑️ **폐기 — 2026-09-17.** 여기 있던 추진제 줄은 **배포 시작 보드로 갔다**
    /// (`260917_W03` 3장 · 사용자 확정). 지금 자리는 `StartingBoard.Nodes/Belts` 와
    /// `StartingBoardB.Nodes/Belts` 다.
    ///
    /// ⚠️⚠️ **같은 배치를 두 곳에 두지 않는다.** 하네스에도 남겨 두면 배포 보드를
    /// 고쳤을 때 하네스가 **옛 줄을 재게 된다** — 그러면 측정과 게임이 다른 판이 되고,
    /// 그것은 재는 장치가 있으나 마나인 상태다(지침 §7).
    ///
    /// 📌 **지운 게 아니라 옮겼다는 기록으로 이 파일을 남긴다.** 09-17 회신문
    /// (`260917_V01` 2-1)이 이 클래스 이름으로 배치를 적어 두었으므로, 그 글을 읽는
    /// 사람이 「없어진 것」과 「옮겨 간 것」을 가를 수 있어야 한다.
    ///
    /// 여기 있던 것 — 가공(발전재료) → 기초 군수(추진제) → 분류기 → 부스터 둘.
    /// A 는 코어 북면(5,9)~(10,9)·(9,10), B 는 코어 서면(4,8)~(8,6)·(8,5).
    /// **자리는 그대로 옮겼다**(`260917_V01` 2-1 표와 같다).
    /// </summary>
    public static class HarnessBoosterLine
    {
        /// <summary>부스터 대수 — 사용자 확정 「부스터 둘」(`260917_W02` 3장). 시험이 읽는다.</summary>
        public const int BoosterCount = 2;
    }
}
