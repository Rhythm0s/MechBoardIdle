# CLAUDE.md — MECH BOARD IDLE

> 이 파일은 **Claude(및 Claude Code)가 사용하는 작업 기준 문서**다. 사용자는 직접 사용하지 않는다.
> 팀 작업 규칙 · 프로젝트 목표/방향 · 반복 실수 목록을 유지·갱신한다.
> 최초 작성 2026-07-10. **v2 갱신 2026-07-23 (구현 트랙 착수판 — 문서 12종 완결 · 검증 시나리오 ①~⑥ 종결 · 합체/버스트 분리 확정).**

## 0. 오리엔테이션 (새 세션 필독)

- **이 리포:** MECH BOARD IDLE — Unity 코드 리포 (2D URP · Unity 6000.4.3f1 · Android · Plastic SCM). 위치 `C:\Unity_Project\26_MechBoardIdle\MechBoardIdle`. 실제 폴더/asmdef 구조는 §6.
- **밸런스 1차 계약 = `balance_v4.json`(리포 루트).** `Docs/balance_v3.1_measured.json`은 구 실측본(참고).
- **워크플로:** 기획·밸런스는 **Claude(chat)+Notion에서 확립** → 사용자가 `balance_v4.json`·이 CLAUDE.md를 **수동 갱신**(상시 발생) → **Claude Code가 구현**. 데이터 흐름 = Notion 기획 → `balance_v4.json` → `BalanceAssetGenerator`가 SO 생성 → 게임/시뮬.
- **역할 경계:** Claude Code는 기획·밸런스 수치를 임의 변경하지 않는다. 구현 중 필요한 기획·밸런스 항목이 생기면 **사용자에게 보고** → 사용자가 Claude(chat) 검토 후 원천(CLAUDE.md/`balance_v4.json`)에 반영.
- **주의:** `NewProjectF`(유령의 집 디펜스)는 별개 게임 — 본 프로젝트와 무관.

---

## 1. 프로젝트 목표 · 방향

- **정체:** 포트폴리오 프로젝트 (직군 = 시스템 기획자). 취업용.
- **완료 기준 3종 (사용자 확정 2026-07-10):**
  1. 시스템 기획자로서 보여줄 **핵심 기획 문서** 체계 — ✅ 완결 (12종, 2026-07-23)
  2. 흥미 유도 + **AI 기술 활용 능력** + 문서의 **구현력**을 증빙하는 **빌드 영상** — ⏳ 구현 트랙
  3. **MVP 빌드** (플레이 가능) — ⏳ 구현 트랙 (현재 병목)
- **장르:** 도트 스타일 탑뷰 자동화 공장 방치형 RPG.
- **핵심 명제 (MVP 검증 목표):** *"물류 라인을 최적화하는 행위가 실제로 재미있는가?"*
- **엔진/타깃:** Unity (URP), **WebGL 배포** (모바일 타깃 UX 유지 — 터치 우선 UX를 웹으로 배포하는 것은 **의도된 선택**이며 실수가 아니다). 2026-08-18 변경(구: Android APK). 심사자의 직접 플레이 비중이 높다는 전제로 설계하며, **치트 패널을 빌드에 포함**한다.
- **개발 형태:** 1인 개발 + AI 보조(Claude Design / Claude Code).
- **일정 현실:** 3주 스프린트(6/29 시작) 기준일 초과 상태 — 문서 완벽주의 루프 금지, **빌드가 유일 병목**.

## 2. 진실 원천 위계 (문서 — 충돌 시 이 순서가 정답)

- **MVP 범위 · 영상 시나리오** = MVP 문서
- **시스템 상세** = 조립 / 전투 / 밸런스 / 07(플레이어블 로봇) / 09(스테이지) / 10(튜토리얼)
- **수치 계산 규칙** = **밸런스 문서의 공식만** 참조
- **확정 수치** = 밸런스 문서에 역기입 / **실측 기록** = 검증 대장
- Notion 허브: 🤖 MECH BOARD IDLE — 모든 문서는 WBS 이하에 위치(2026-07-08 규칙)
- **핵심 문서 링크:** 밸런스 = `391e6132cf188182bcdcebf55a6bc54d` / 스테이지 09 = `395e6132cf18816ba7e7fa4c691b4e1b` / 07 로봇 = `395e6132cf1881ccbd68e7218862c745` / 검증 대장 = `395e6132cf1881f9bba5e3930d64cc34` / 11 BM = `3a6e6132cf188162b8d3fe1f12d0070d` / 12 AI 파이프라인 = `3a6e6132cf188130ae31eb81cf8bcdee`
- **문서 트랙 상태 (2026-07-23):** 01~13 전 12종 완료(08 결번). 검증 시나리오 ①~⑥ 전체 종결(③만 보드 물리 상한 = Unity 구현 후 확인 조건부). **문서 재감사 금지 — 신규 발견 사항만 원천 판정 후 수정.**

## 3. 개발 규칙 (팀 = 1인)

- **플랫 모드 우선:** 코드/파일 변경 전, 현황 분석 + 계획부터 출력. 승인 후 실행.
- **한 문서/파일 = 한 책임.** 동일 내용 중복 금지(드리프트 원천). 필요 시 링크 + 한 줄 요약으로 축약.
- **수치 하드코딩 금지** → ScriptableObject / 설정 자산으로. 밸런스 문서 공식이 유일 원천, 미확정치는 TBD 밴드 상수로 placeholder 처리. **확정치는 `balance_v4.json` 이 1차 계약** (아래 §9).
- **초 단위 밸런싱:** 15/20/30초 그리드, 소수점 비노출(수치 복잡도 20% 원칙).
- **전력 긴장 영구화:** 전력 효율은 어떤 성장으로도 강화 불가. 전력 = 전 노드 가동 고정비 전용.
- **물류 무개입:** 물류 최적화에 별도 수치 보너스 없음 — 효율은 물리(실제 생산량)에서만.
- **Unity 관리 파일은 Editor 스크립트로만:** 씬(`.unity`)·SO(`.asset`)·`.meta`는 손 텍스트 편집 금지 → `EditorSceneManager`/`AssetDatabase` 기반 Editor 스크립트로만 생성·수정. (씬 YAML 수기 append는 SceneRoots·fileID 손상 위험 — §5-3 확립.)
- **레이아웃·비밸런스 수치는 밸런스 계약과 분리:** 그리드 치수 등 balance와 무관한 수치는 `balance_v4.json`/`BalanceConfig`(밸런스 미러)에 넣지 말고 별도 SO(예: `BoardConfig`)로. 밸런스 계약은 생성기 재실행 시 덮어써져 오염·드리프트 위험. (§3 "한 파일=한 책임" 적용.)
- **범위 판단 = "문서가 주장하는 시스템인가"** (영상 등장 여부가 아니다, 2026-08-18 확정). 문서가 주장하면 구현하거나 그 문서에 **"MVP 미구현"을 명시**한다 — **묵시적 생략 금지**(주장해 놓고 조용히 빼면 심사자에게는 "문서가 거짓말했다"로 읽힌다). 문서가 주장하지 않으면 구현하지 않는다(주석도 불필요). **구현이 시스템을 발명하지 않는다** — 문서에 없는 규칙을 코드가 정의하면 설계 순서 위반.
- **신규 전문 용어 설명 의무:** 대화에 처음 등장하는 전문 용어(엔지니어링·설계·업계 용어 불문)는
  사용자가 묻기 전에 그 자리에서 설명한다. 설명은 어린아이에게 말하듯 쉽게 풀어 쓴다 —
  짧은 문장, 일상 비유 1개, 어려운 말로 어려운 말을 설명하지 않기,
  "무엇인지"보다 "이게 없으면 어떤 곤란한 일이 생기는지"를 먼저.
  쉽게 쓰되 틀리지 않게 — 쉬운 설명 뒤에 정확한 한 줄을 덧붙인다.
  약어·외래어는 최초 1회 원어 병기. 이미 설명한 용어는 반복하지 않는다.
  근거: 문서 작성 원칙 "독자 가정 0"을 대화에도 동일 적용 —
  설명 없는 용어는 검토를 형식적으로 통과시켜 미검토 결정을 원천에 쌓는다.

## 4. MVP 스코프 가드레일 (초과 금지)

- 부품 Tier: **T1~T2만**.
- 마운트: **A/B 각 1개 붙박이**. 복수 장착은 확장 규칙(미등장).
- 합체: **2기 합체만**, 3기+ 제외. 연출 예산 초과 시 화면 전환 + 스탯 수치로 대체.
- 노드·모듈 강화: MVP는 **상수 1.0 확정** (성장 통로 3종은 S4 원샷 배율 ×1.45 하나에 흡수).
- 강화: S4 단독 벽. S5·S6는 물류 + 시스템 이해만으로 통과 가능해야 함.

## 5. Unity 빌드 순서 (임계경로 — 이 순서로 착수)

1. 프로젝트 세팅 (URP 파이프라인, 폴더 구조, 기본 씬)
2. **물류 노드 데이터 구조 (ScriptableObject)** ← 첫 Critical. 노드 타입(6종: 코어/가공/군수/발전/저장/쉴드) · 연결 규칙 · 변수(전력/탄약/발열) 저장 구조. `MbiBalanceExporter.cs` 스텁과 `balance_v4.json` 스키마를 SO 설계의 시드로 사용
3. 물류 보드 그리드 시스템 (스냅 · 배치 · 탭 입력) — **③ 보드 물리 상한**(천장 ×1.6 담보)이 여기서 결정됨: 그리드 크기·라인 수를 대표 상태 145 기준으로 역산
4. 벨트 시스템 (드래그 설치 · 면 자동 연결 · 병합기/분류기)
5. 물류 시뮬 로직 (전력/탄약/발열 · 병목 감지 · 출력값 계산)
6. 물류 출력값 ↔ 전투력 연동 (판정식 = 07 1장: `max(1, 발당피해 × 마운트계수 × 모듈배율 − 방어)`)
7. 자동 전투 (방치 진행 · 자원 타이머 · 방치 보상 스택)
8. 콘텐츠(스테이지 6종 SO 주입 — S1~S6 곡선 확정치 존재) · 온보딩 · 강화 UI · 레이어 전환 UI
9. (선택/영상 후 보류 후보) 고급 시스템: 합체 · 드론 · 태그

> **버티컬 슬라이스 우선:** 2~7이 코어 명제 증명 구간. 9(합체/드론/태그)는 코어가 "재밌다"고 증명된 뒤로 미룬다 — 영상엔 스크립트 연출로 대체 가능. 단, 수치는 전부 확정되어 있으므로(§9) 9번 구현 시 밸런스 재탐구 불필요.

## 6. 폴더 구조 (실제 — §5-3 시점)

```
Assets/_Project/
  Scripts/
    Core/           MBI.Core   — 순수 로직: NodeConnectionRules · BoardGrid · NodeInstance
    Data/           MBI.Data   — SO: NodeDefinition · BalanceConfig · BoardConfig · enums/ports
    Logistics/      MBI.Logistics — MonoBehaviour+InputSystem: BoardController
    Combat/  UI/    (예약)
    Editor/         MBI.Editor — 생성기: BalanceAssetGenerator · BoardConfigGenerator · LogisticsSceneCreator
    Tests/EditMode/ MBI.Tests.EditMode — BalanceAnchorTests · NodeSchemaTests · BoardGridTests
  ScriptableObjects/{Nodes, Modules, Belts, Stages, Robots}
  Scenes/Logistics.unity ·  Prefabs/  Art/  Audio/
(리포 루트) CLAUDE.md · balance_v4.json(밸런스 1차 계약, §3·§9) · Docs/balance_v3.1_measured.json(구 실측본·참고)
```

## 7. 반복 실수 목록 (자가 점검 시 반드시 확인)

- **[2026-07-10] WBS 날짜 허구.** 초기 WBS 시작/종료일은 실제와 무관한 플레이스홀더였음. → 일정은 실측 기준으로만 기재. 날짜를 사실처럼 인용 금지.
- **[문서 드리프트]** 동일 내용 중복으로 "하루 2회 동기화 수정" 불일치 발생 이력. → 원천 선언 + 링크 축약. 신규 중복 발견 시 즉시 원천 판정 후 나머지는 링크로.
- **[동음이의 오독]** 機體(몸체) → 氣體 오독 사례. → 용어는 용어 사전 링크 유지, 오독 소지 단어 개명.
- **[문서 완벽주의 루프 경계]** 문서가 "완료"인데 추가 감사를 반복하면 빌드가 지연됨. **문서 12종 완결(07-23) — 이제 빌드만이 병목.**
- **[2026-07-10] 시뮬 수식 이중계산.** 강화배율을 S1~S3에도 곱한 버그. 강화는 **S4부터**. → 수식 구현 후 반드시 node 등으로 경계값 손계산 대조.
- **[2026-07-11] 재화 혼동 부정피드백 오류.** 고철(킬 기반)과 강화재료(클리어 한정)는 분리 재화(E2). → 재화별 원천·용도를 먼저 확인한 뒤 정합성 판단.
- **[2026-07-11] 원천 문서 성급 개정.** 대화 결정을 확정 문서에 즉시 덮어썼다 롤백. → **"결정 진행" 스테이징 → 사용자 "확정" 이후에만 원천 개정** (커밋 게이트).
- **[2026-07-11] placeholder 통과 오인 경계.** 시뮬 "N/N 통과"는 입력 수치의 확정 여부와 무관한 계산 엔진 정합성 증명일 수 있음. → v3.1부터 SAMPLE = 확정치 동기화됨. 단 병목(전력/벨트/발열)·경제(스폰/고철/오프라인)는 여전히 TBD — 이 항목이 낀 결과는 "검증 완료" 표기 금지.
- **[2026-07-11] 요약 기반 산출 오류(중대).** 산출물을 원천 문서 대신 CLAUDE.md 요약만 보고 제작 → 3중 오류. → **산출물 제작 전 원천 문서(§2 위계) 직접 확인 필수.** §9 요약은 인덱스일 뿐 계약 아님. (2026-07-23 재검증: 메모리가 검증 대장 7/17 실측 1차를 모르고 있었음 — 원천 확인 규칙이 이중 오류를 막음.)
- **[2026-07-23] Notion 자동 링크화 무음 스킵.** `.md`/`.ai` 등 도메인 형태 텍스트를 존재하지 않는 URL로 자동 링크화하며, 일반 텍스트로 되돌리는 치환은 성공 응답 후 무음 스킵됨. → 파일명·제품명은 처음부터 코드 서식(백틱)으로 작성. 일반 텍스트 복원 치환은 재열람 필수.
- **[2026-07-23] 결합 용어의 형용모순 경계.** "합체(버스트)" 결합 표기가 "버스트 지속시간" 형용모순 + 예산식 화력 증발 모순을 낳음. 수치 모순 발견 시 **재해석 전에 용어 결합 여부부터 의심** — 한 이름 아래 지속 속성과 순간 속성이 공존하면 분리가 정답. (해소: 밸런스 5장 분리 개정 2026-07-23.)
- **[2026-07-23] 명명 원천 우선.** 실측 중 새 개체에 임의 명칭(표준/강습 드론)을 붙였다가 원천 문서(07 3장: 누적형/광역형)와 충돌 → 정정. → 새 수치를 붙일 개체의 이름은 원천 문서의 기존 명명을 먼저 확인.

## 8. 자가 점검 루프 (모든 작업 후)

작업 지시 → 작업 완료 → **자가 점검(이 문서 §7 확인)** → 피드백 → (필요 시 §7에 신규 실수 추가).

## 9. 밸런스 확정치 인덱스 (2026-07-23 — 원천은 항상 Notion 문서·검증 대장)

> ⚠️ 이 절은 **인덱스**다. 구현 시 수치의 계약은 `balance_v4.json`(전달본) → Unity SO 손 입력 → export 재생성. 충돌 시 Notion 원천 확인.

- **데이터 흐름(단방향):** Unity **SO = 단일 원천** → `balance.json` export → 시뮬 v3.1 **읽기 전용**. 되쓰기 없음.
- **판정식(07 1장):** `실피해(히트당) = max(1, 발당피해 × 마운트계수 × 모듈배율 − 방어)`. 난수 0. 방어는 히트당 뺄셈. 로봇 방어 스탯 없음(받는 피해 = 몬스터 공격력), 쉴드 = 별도 생존 게이지(예산 밖·후순위).
- **앵커:** 원점 100(관통 20×5 기본 라인) · 물류 천장 ×1.6(담보 = 보드 물리 상한, §5-3) · 요구치 S1 90/S2 105/S3 130(실측선 100/125/145) · S3돌파 145 = 관통1+분열1+폭발2 · S4 밴드 186~215(도달 210.3) · 강화 ×1.45 확정 · 도전 120초 · ±8% 스냅 · 태그 쿨 5초.
- **무기 스펙트럼(전부 확정, 등가선 100):** 탄종 관통 20×5 / 분열 25×4 / 폭발 50×2, 소비 상한 6발/초(고효율 우선) · 드론 누적형 100×1.0(기본 프리셋·S5 해답자) / 광역형 50×2.0. 슬롯 3 · 방출률 1.0 (상수, 강화 비대상).
- **태그(확정):** 저장 40발 → 주기 10초 · 교대 계수 0.96 · 등장 특공 = 비축 발수 × 강화 평균 발당피해 ≈ 2,103 (예산 밖).
- **합체/버스트 분리(2026-07-23 확정 — 구 결합 표기 폐기):**
  - 공통 게이지 1개: 전투 수행 충전 · **만충 90초** · 자동+수동 · 스테이지당 1회 → 합체·버스트 동시 발동
  - 합체(지속·예산 안): **20초** · 변신 화력 = (A+B 원 DPS) × **×1.8** = 531.5 (합체 중 태그 없음 → 교대 공백 미적용, 두 공장 가동 → 비축 적립 → 종료 후 만재 복귀)
  - 버스트(순간·예산 밖): 발동 순간 스냅샷 × **300%** ≈ 852 — 등장 특공과 동렬의 마진 항
  - S6 예산식: `보스 HP ≤ 일반DPS×(120−지속) + 합체체DPS×지속` → 예산 39,018 ≥ **보스 36,000**, 필연 밴드 34,067<HP≤39,018, 실효 109.4초
- **몬스터 곡선(체력·방어·수 확정, 공격력 = 쉴드 축 이월):** S1 보병40×270/1 · S2 +포격10×260/3(보병 250/1) · S3 보병 230/2+포격15×280/3+장갑5×440/6 · S4 보병 280/3+포격20×310/5+장갑12×410/8 · **S5 장갑15×1,100/45**(분열 36.25<45<폭발 72.5 경계) · **S6 보스1×36,000/12**.
- **경제:** E2 분리(고철 킬 기반/강화재료 도전 한정) · 오프라인 = 실측×시간×계수(36h 상한·고철만·계수 TBD) · ⑦ A안(잉여 0, 보상 30/33/37 = placeholder 역산치).
- **잔여 TBD(검증 대장):** 노드 카탈로그·모듈 F(물류 세부 — §5-2·5 구현과 결합 확정) / 쉴드·몬스터 공격력·로봇 HP(생존 축 후순위) / 스폰·마리당 고철·오프라인 계수(노드 가격과 결합) / 10장 갭 분해(병목 수치).
- **산출물:** `mbi_balance_simulator_v3_1.html`(분리 예산식·확정치 SAMPLE 동기화·node 손계산 ALL PASS) · `balance_v4.json`(confirmed 21/29) · `MbiBalanceExporter.cs`(스키마 4.0).

## 10. 구현 트랙 인계 사항 (2026-07-23)

1. ~~시뮬에 분리 모델 반영~~ → **완료 (v3.1 전달본)**. Unity 측은 SO → export 시 `mergeMult/gaugeFull/bc/bd` 키 유지.
2. 용어 사전 DB(`eae5bd85-ecc4-475b-984b-f945201ba66c`): "합체(버스트)" 행을 **합체(지속 상태) / 버스트(순간 필살)** 2행으로 분리 갱신 — Notion 접근 가능한 세션에서 수행.
3. 12 AI 파이프라인 문서의 미결 P-1(구현 정량 지표: 작업 단위 수·사람 수정 비율) — 구현 중 커밋/수정 로그를 남겨 종료 후 집계.
4. 영상 컷 우선순위(연출 → 비등장 구현 → 문서 불가침) 기준: B구간 = S3 실측선 145 서사(재설계 전 145.4초 실패 → 돌파), C구간 = 합체 연출 + S6(만충 90초 → 합체 20초 → 실효 109.4초), D구간 = 프리셋 전환 + BM 암시(11 문서).
