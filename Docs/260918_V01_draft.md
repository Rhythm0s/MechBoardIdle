# [문서] 260918_V01 구현 회신 (태그 스킬 피해 나눔 이행 · 답 넷 처리 · W09 접수)

> 구현 → 설계 · 2026-09-18 (금) · 직전 수신 `260917_W09` · 직전 발신 `260917_V06`
> **이행 커밋 하나** — `b2a6217`(태그 스킬 피해를 표적 수로 나눈다)
> EditMode **1173 중 1167 통과 · 0 실패 · 6 건너뜀**
> 빌드 **`?v=b2a6217`** (`http://localhost:8123/index.html?v=b2a6217` · index 200 · 빌드 파일 넷 200)

---

# 1. 반드시 읽을 것 둘

**① ⚠️⚠️ S1 한 판의 수는 그대로인데, 그것이 「나눔이 판을 안 바꿨다」는 뜻이 아니다.**
하네스는 **로봇 하나짜리 생성자**(`CombatSimulation(setup, spawns, …)`)를 쓴다 —
그 경로에서는 `Tag` 가 **null** 이라 태그 스킬이 **애초에 한 번도 안 나간다**.
그러니 이 판은 나눔을 **재는 판이 아니다.** 수가 같은 것은 결과가 아니라 **판의 성질**이다.
📌 **등가선 재산출의 입력으로 이 수를 쓰면 안 된다** — 나눔의 대가는 이 판에 안 잡힌다.
나눔이 실제로 걸리는 것은 **두 로봇 판(태그)** 이고, 그 자리는 시험이 잰다(4장).

**② 답 넷 중 코드가 움직인 것은 ④ 하나다.** ①은 값을 기다리는 동안 그대로,
②는 촬영 뒤(문안 뒤), ③은 이미 코드가 그 값이다. 아무것도 안 한 자리를
「이행했다」로 적지 않는다(3장).

---

# 2. `260917_W09` 접수

| 항목 | 구현 쪽 처리 |
|---|---|
| 1장 ① — 문서와 코드가 어긋난 셋은 **설계가 문서를 고친다** | 접수. **코드는 손대지 않았다.** 스폰 띠(반경 8~14) · A 마운트 칸(x1 · y0~3) · 태그 판정(화면 안 전부)은 지금 코드가 사실이다 |
| 2장 — `260917_V06` 접수(탭 일곱 · 개명 · 회피 이동 확정 · 고리 기본 끔 · 태그 주기) | 접수. 되돌릴 것 없음 |
| 4-4 ③ 「고쳤다가 판정까지 고친 것은 아니다」 | 접수. 이번 이행 보고도 **그림 · 판정 · 값**을 갈라 적었다(4장) |
| 5장 WBS — `W09` 는 구현의 첫 회신이 답하면 완료 | **이 장이 답한다** |

---

# 3. 판정 넷 — 처리

사용자 답(09-18 · 플랜 §83)을 그대로 받았다.

| # | 판정 | 사용자 답 | 코드에 한 일 |
|---|---|---|---|
| 1 | 광역형 드론의 광역 판정 반경 | **설계가 값을 낸다**(`0918_W01`) · 그때까지 그대로 | **손대지 않음** — 판정은 드론 사거리 **9.2** · 자리표시 고리 **꺼짐** |
| 2 | 태그 스킬 판정과 연출의 범위 | **판정은 화면 안 전부 그대로** · **연출을 화면 전부로 넓힌다**(09-16 의 부채꼴/빔을 뒤집음) | **손대지 않음** — 문안이 선 뒤에 한다. 지금 판정은 이미 답과 같다 |
| 3 | 태그 오토 스위치 기본값 | **꺼짐 유지** | **손대지 않음** — 코드가 이미 꺼짐이다. 문서의 「자동 기본」이 옛 표기다 |
| 4 | 태그 스킬 피해 나눔 | **나눈다** — 총 피해 고정 · 표적마다 총 ÷ 표적 수 | **이행**(4장 · `b2a6217`) |

⚠️ 1·2·3 은 **아무 줄도 안 바꿨다.** 「답을 받았다」와 「코드가 달라졌다」는 다른 칸이다.

---

# 4. ④ 이행 — 태그 스킬 피해를 표적 수로 나눈다

## 4-1. 무엇을 고쳤는가 — **판정**이다

| 갈래 | 이번에 손댔나 |
|---|---|
| **값** | 아니다 — 자산도 `balance_v4.json` 도 안 건드렸다. 나눔은 판정식의 모양이지 값이 아니다 |
| **판정** | **그렇다** — 표적마다 받는 피해가 **총 ÷ 표적 수**가 됐다 |
| **그림** | 아니다 — 연출은 2번 판정(문안)이 선 뒤다. 지금은 표적마다 폭발 연출 하나로 그대로다 |

## 4-2. 코드

`CombatSimulation.TagSkillStrike` 한 자리다.

```
- float damage = GrandEntrance.Damage(true, loadedRounds, avg);
+ float damage = GrandEntrance.Damage(true, loadedRounds, avg) / targetCount;
```

⚠️ **나눗셈은 표적별 판정식 뒤에 건다.** 방어력이 표적마다 다르므로
총을 먼저 나눠 넣으면 **방어 계산이 나눈 값을 밟는다** — 같은 수가 안 나온다.

📌 **보고값(`LastTagSkillDamage`)의 뜻은 안 바뀌었다** — 여전히 **실제로 들어간 피해의 합**이고,
나눔 뒤에는 그 합이 곧 **총 피해**다. 그래서 이 값을 읽는 기존 시험 둘이 그대로 섰다.
**바뀐 것은 한 체가 받는 몫**이다(종전에는 표적 수만큼 총이 불어났다).

## 4-3. 시험 — `TagSkill_SplitsDamageAmongTargets` (신설)

설계가 청한 셋을 그대로 잰다.

| 잰 것 | 결과 |
|---|---|
| 표적 **하나** = **전량** | 통과 |
| 표적 **둘** = **각 절반** | 통과 |
| 표적 **0** = **보류**(마운트도 안 비운다) | 규칙 불변 — 기존 `TagSkill_WithoutTargets_KeepsTheMountFull` 이 그대로 통과 |
| **총은 표적 수와 무관하게 고정** | 통과 — 하나짜리 판의 총 = 둘짜리 판의 총 |

**재는 법.** 평상시 사격이 섞이면 HP 감소분이 스킬 몫이 아니다 — 그래서 시험용 로봇은
**사거리 0**(평상시 사격이 안 닿는다)이고 적은 **방어 0 · 반격 0**이다.
그러면 적의 HP 감소분이 곧 스킬 몫이라, **보고값이 아니라 적의 몸으로** 잰다.

🗑️ 구 주석 「**가정 하나 — 전부에 같은 피해로 둔다**」(`260908_V05` 판정 요청) **폐기** —
09-18 사용자 확정으로 닫혔다. 그 자리에 「적이 많을수록 세지는 스킬은 광역의 대가가 없다」를 남겼다.

## 4-4. EditMode

**1173 중 1167 통과 · 0 실패 · 6 건너뜀** (시험 하나 늘어 1172 → 1173).

---

# 5. S1 한 판 재실행 — 값 그대로

`RunW07Batch` · 튜토리얼 종료 판 · 추진제 30 · HP 1000 · 자동 조종 **켬** · 배포 보드.

| 판 | 결과 | 09-17(`260917_V06`) |
|---|---|---|
| **배포 보드**(쉴드 줄 포함) · 자동 조종 켬 | **Win 100.2초 · HP 582/1000 (58%)** | 같다 |
| 기준선(쉴드 발생 노드를 뽑음) · 자동 조종 켬 | Win 100.2초 · HP 142/1000 (14%) | 같다 |
| 배포 보드 · 자동 조종 **끔** | LoseTimeout 120.0초 · 남은 적 7 · HP 692 | 같다 |

쉴드 축도 그대로다 — 최대치 200(노드 1대 × 200) · 충전률 5.00/초 · 빈 게이지 **40.0초** ·
**쉴드가 막은 피해 440** · 쉴드 0 인 틱 3.6%.

⚠️⚠️ **이 판에는 태그 스킬이 안 나간다**(1장 ①). 수가 같은 것은 **나눔이 이 판을 안 건드렸다**가 아니라
**이 판이 나눔을 안 태운다**는 뜻이다. **등가선은 이 수로 다시 못 뽑는다** —
나눔의 대가를 재려면 **두 로봇 판**이 필요하고, 그 판은 하네스에 아직 없다.
필요하면 만들 수 있다(요청 6-2).

---

# 6. 설계에 청하는 것 둘

**6-1. 광역형 드론의 광역 판정 반경 값** (이월 · `260917_V06` 6-1)
값이 오면 **판정과 고리를 같은 값에 잇고 고리를 켠다**. 오기 전에는 9.2 · 꺼짐 그대로다.

**6-2. 태그 두 로봇 하네스 판이 필요한가** (신규)
나눔의 대가(표적이 많을 때 스킬이 얼마나 약해지는가)는 **지금 어떤 판으로도 안 잡힌다** —
S1 하네스가 로봇 하나이기 때문이다. 등가선 재산출에 그 수가 필요하면 판을 새로 만든다.
**필요 없다면 안 만든다** — 안 쓸 판을 만들면 그 판이 또 하나의 「다른 판의 수」가 된다.

---

# 7. WBS

| 문서 | 처리 |
|---|---|
| `260917_W09` | **완료** — 이 장이 답한다 |
| `260918_V01` (이 장) | 진행중 |

⚠️ WBS 상태 변경은 **설계 권한**이다 — 여기 적은 것은 요청이 아니라 **대조용 기록**이다.

---

문서 마침 — `260918_V01` (2026-09-18 · 태그 스킬 피해 나눔 · 답 넷 처리 · W09 접수)
