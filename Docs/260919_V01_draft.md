# [문서] 260919_V01 구현 회신 (W02 이행 · 표 열둘 · 리허설 1 둘 · 정정 셋)

> 구현 → 설계 · 2026-09-19 (토) · 직전 수신 `260918_W02` · 직전 발신 `260918_V03`
> **이행 커밋 — `260918_V03` 게시 뒤 전부**
> `7a53194`(육안 반영 셋) · `0b1b4f5`(하네스 S0~S5) · `455d21b`(키운 판 스위프) ·
> `670e8ea`(드론 활동 범위 · 탄종 섞기) · `825cb0c`(**세 발 쪼개기 · 피해 1/2**) ·
> `cb39b6f`(**몬스터 HP 상승폭 절반**) · `e830734`(설정 버튼 폐기) · `7ae1851`(**자산 드리프트 닫음**) ·
> `b0bf9db`(**설정 버튼 되살림**) · `87581ed`·`85d3b6d`·`3ba5b13`(**밸런스 표 셋 · CSV 원천**) ·
> `267e3ab`(**두 줄로 갈리던 글자 여섯**) · `8fd8d42`(태그·Auto 넷) ·
> `5c66915`(**조립 인셋 카메라**) · `105fad7`(**안 오는 몬스터 실측**) ·
> `7724be1`(곁눈질 예산 — 끈 채로) · `be3ce15`(**우회 3칸 켬**) ·
> `ffc7fb4`(**밸런스 표 아홉 장**) · `64f21bc`(**TEXT_DATA**) ·
> `1b55d4e`(**광역 반경 3칸 · dBAoe 50 이관**)
>
> EditMode **1263 중 1257 통과 · 0 실패 · 6 건너뜀**
> 하네스 스위프(문 지난 판 · 출하 값) — S1 ✅ Win 79.8초 · S2 ❌ LoseDead 103.7초 ·
> S3 ❌ LoseDead 83.0초 · S4 ❌ LoseDead 79.2초 · S5 ❌ LoseDead 76.7초
> 빌드 **`?v=1b55d4e`** (`http://localhost:8123/index.html?v=1b55d4e` · index 200)
>
> ⚠️ **「`260918_V04`」는 없다** — 구현이 낸 것은 V01·V02·V03 셋이다(W02 9-3 1번 정정).
> ⚠️ 증빙 로그·xml 은 **이 세션의 스크래치패드**에 있다 — 리포 밖이라 다른 창에서 안 보인다.

---

# 1. 반드시 읽을 것 셋

**① ⚠️⚠️ 「설정 버튼을 되살렸다」가 반만 사실이었다**(3-1장).
09-18 에 붙인 **「메인 메뉴로」가 09-19 낮까지 화면 밖에 있었다.** 패널 윗변을
`Screen.height - 190` 으로 **박아** 두었는데 판 높이가 318 이라, 아래 세 버튼이
통째로 화면 아래로 흘렀다. **붙여 놓고 한 번도 눌릴 수 없었다.**
W02 6장과 UI 문서 설정 절에 「되살림」으로만 적히면 사실과 다르다 —
**「붙였으나 09-19 낮까지 눌린 적 없음 · `267e3ab` 로 닫힘」**으로 적어 달라.

**② ⚠️⚠️ 검증 도구가 게임과 다른 문을 지나고 있었다**(5-2장).
하네스가 `RespawnUnreachable`(못 닿는 적 되돌리기)을 **안 불렀다.** 러너는 매 틱
`Tick` 다음에 부른다(§71-28 2). 그래서 하네스 판에는 **게임에는 없는 「영영 못 닿는
적」**이 남아 굳음 수가 부풀어 있었다. W02 7장 ①이 올린 것과 **같은 병**이다 —
「검증 도구가 검증 대상과 다른 것을 보면 초록불이 거짓말이 된다」.
⚠️ **오늘 이전의 굳음 수치는 전부 이 문을 안 지난 판의 것이다.**

**③ ⚠️⚠️ 우회를 강하게 하자 밸런스가 움직였다**(6장 · 사용자 확정).
S2·S5 가 **LoseTimeout(살아남음) → LoseDead** 로 바뀌었고 S3·S4 는 20초씩 빨리 죽는다.
사용자가 **어려워짐을 감수하고** 켰다. 회귀가 아니라 **밸런스 이동**이므로
**등가선·요구치 재산출의 입력**으로 받아 달라.

---

# 2. `260918_W02` 이행 — 반경 3칸 · dBAoe 50

## 2-1. 광역 판정 반경 2 → 3칸 (`1b55d4e`)

W02 2-1 그대로 넣었다. `confirmed` 는 **false 그대로** 두었다 — 확정된 것은 **이 수**이고
본전 몇 마리가 나오는지는 아직 안 쟀다(설계가 「고리 같은 값」이라 했다).

🗑️ 구 2칸 폐기. 근거는 W02 가 낸 표다 — S5 에서 3.63마리로 본전선 3.64 와 맞고
S1·S3 은 미달이라, 「광역형은 S1 에서 약해도 된다」가 **값 하나로** 선다.

⚠️ **하네스 스위프 판정은 안 바뀌었다** — 이 판들에서는 광역형 드론이 서지 않기
때문이다. 반경이 듣는 자리는 **두 로봇 판의 광역형 줄**이고, 그 판의 실측은 W02 가
이미 가진 표(S1 1.30 · S3 1.92 · S5 3.63)다.

## 2-2. dBAoe 50 좌표 이관 (`1b55d4e` · W02 5장이 문안)

| | 종전 | 지금 |
|---|---|---|
| 문서 | **50** (무기 스펙트럼 좌표) | 표의 좌표 **50** |
| 코드 | 100 × `droneAoeDamageFactor` 0.5 | 좌표 둘을 **나눠서 파생**(50 ÷ 100) |

· ✅ json `params.dBAoe` **50** 신설(confirmed true · 07-17·07-23 좌표)
· 🗑️ json `params.droneAoeDamageFactor` 0.5 **폐기 — 값은 남기되 읽는 곳이 없다**
· 🗑️ `WEAPON_DATA` 의 `AoeDamageFactor` 열도 **안 읽는다.** 열은 남긴다 — 사람이
  **좌표와 몫을 견줄 수 있어야** 하기 때문이다. ⚠️ 고칠 곳은 `Charge` 다.

**거동 불변 증거** — 밸런스 자산 diff 가 **`droneAoeJudgeRadius: 2 → 3` 한 줄뿐**이다.
`droneAoeDamageFactor` 는 0.5 그대로 나온다.

⚠️ **한 가지 문안이 애매하다.** W02 5장은 「이관 뒤에는 **자산에 50이 한 번만 산다**」고
적었는데, 자산의 칸은 **배수**(`droneAoeDamageFactor`)라 50 을 담을 그릇이 아니다.
그래서 **50 은 표에 살고 자산은 파생된 0.5 를 든다**로 닫았다. 자산에 50 을 **글자
그대로** 두려면 시뮬이 읽는 칸을 새로 내야 한다 — **한 마디면 그렇게 고친다.**

## 2-3. 시작 보드 회전 · 회전 절 (W02 4장 · 6장 5번)

⚠️ **손대지 않았다.** W02 가 「어느 문서에 서는지는 아직 안 정했다 · 그때까지 가정
표기를 유지해 달라」고 했으므로 **가정 표기 그대로** 두었다. 문서가 정해지면 넣는다.

---

# 3. 정정 셋

## 3-1. 「설정 버튼 되살림」 — 붙였으나 눌린 적이 없었다

`b0bf9db`(09-18 16:45)로 「메인 메뉴로」를 붙였고 회신문에 **「되살렸다」**로 적었다.
그것은 **코드가 있다**는 뜻이었고 **화면에 있다**는 뜻이 아니었다 — 그리고 화면에는 없었다.

· 패널 윗변 = `Screen.height - 190` (박힌 수) · 판 높이 = 318
· 아래로 **128 넘게 흘러** 「처음부터」·「보드 좌표 덤프」·「메인 메뉴로」가 화면 밖
· 09-19 `267e3ab` 에서 **높이를 먼저 내고 윗변을 거기서 빼도록** 고쳤다
· 🗑️ 줄 수를 `ShowTutorial` 로 세던 것도 폐기 — 안 그려도 `y` 는 내려가므로 두 빌드의
  높이가 같은데 세는 쪽만 달라 **배포 빌드에서 판만 짧아졌다**

📌 **등재 후보** — 「**붙인 것과 보이는 것은 다르다** — 새 버튼은 육안 전에 완료로 적지 않는다」.

## 3-2. `260918_V03` 6장이 게시 당일에 낡았다 (W02 1장 ③ 접수)

「조립 상단 240 띠 상시」가 같은 날 저녁 육안으로 폐기된 것 — **접수한다.**
역기입 전에 지금 꼴을 먼저 확인하는 것에 동의한다.

## 3-3. 하네스가 게임과 다른 문을 지났다 (위 1장 ②)

문을 지나게 한 뒤 재측정했고 **판정 여섯이 전부 동일**했다(개체 수는 안 변하므로
클리어 여부는 같고, 적이 **서 있던 자리**가 달랐던 것이다). 되돌림 실제 발동은
S3 에서 2회뿐이다.

📌 **등재 후보** — 「재는 것과 보는 것은 다른 일」은 이미 09-15 에 등재됐다.
**등재된 규칙이 또 깨졌다는 사실**을 적어 둔다 — 규칙이 문(코드)이 아니라 글이면
같은 자리에서 다시 넘어진다.

---

# 4. 밸런스 데이터가 표로 옮겨 갔다 — 원천 위계 개정 청구

## 4-1. 지금까지 만든 것 (`87581ed`·`85d3b6d`·`3ba5b13`·`ffc7fb4`·`64f21bc`)

```
Tables/<NAME>.xlsx  ──(xlsx_to_csv.py)──▶  Assets/_Project/GameData/<NAME>.csv
                                                   │
                                      CsvTable → GameDataTables → 생성기 → SO
```

| 묶음 | 표 | 배선 |
|---|---|---|
| 09-18 밤 | STAGE_COMP · ENEMY · WEAPON | ✅ **게임이 읽는다** |
| 09-19 | NODE · RECIPE · MODULE · ROBOT · STAGE · COMBAT_RULE · ECONOMY · LOGISTICS · BALANCE_PARAM | ⚠️ **아직 아무도 안 읽는다** |
| 09-19 | TEXT_DATA | ⚠️ 안 읽는다 |

⚠️ 둘째 묶음의 범위는 **사용자가 좁혔다**(09-19) — 지금은 **xlsx 생성 + CSV 변환까지**이고
로더 배선 · 리터럴 치환 · json 폐기 표기는 **영상 이후**다.

## 4-2. 청구 — 원천 위계를 고쳐 달라

지침과 `CLAUDE.md` 가 **「Notion 문서 → json → SO」**로 적고 있는데 지금은 **json 이 아니다.**

> **Notion 문서 → 표(CSV) → SO**

📌 이 줄이 안 고쳐지면 **다음에 값을 고치는 사람이 json 을 고치고** 아무 일도 안 일어난다.

## 4-3. 값이 안 흔들렸다는 증거

· 첫 묶음 — `BalanceConfig` 자산 diff **0** · CSV diff **0** · 하네스 S0~S5 **같은 수**
· 둘째 묶음 — 대조 시험 **아홉**이 자산·json 을 직접 읽어 견준다
  (⚠️ **생성기를 다시 안 돌린다** — 구운 것끼리 견주면 아무것도 안 지킨다)
· 기존 셋 CSV **무변경**

## 4-4. TEXT_DATA — 243 줄 · **1차 수집이다**

Dev_Index(기호 키) · ID(정수) · Text_KR — 사용자가 정한 세 칸. 문안은 **지금 코드 그대로**이고
**개정하지 않았다.**

⚠️⚠️ **사람이 한 번 훑어야 한다.** 화면 글자와 개발자 글자(로그·예외·인스펙터 툴팁)는
**같은 문자열 리터럴**이라 완전한 잣대가 없다. 주석을 걷고 개발자용 호출을 뺀 뒤
남은 것을 **전부** 실었다 — **더 싣는 쪽으로** 틀린다. 빠뜨린 글자는 화면에서 한글이
사라져야 드러나지만, 더 실린 글자는 지우면 그만이다.
(첫 판이 604 줄이었는데 앞머리가 통째로 `[Tooltip]` 이었다 — **유니티 인스펙터**에
뜨는 글자이지 게임 화면이 아니다. 빼니 243 줄이 됐다.)

**설계가 볼 자리 둘** — 표가 Dev_ 칸으로 표시해 둔다:

| 갈래 | 수 | 왜 그대로 못 옮기나 |
|---|---|---|
| **구멍** | 50 줄 | 보간 문자열이라 안에 `{…}` 가 있다. **구멍을 어떻게 적을 것인가** 규약이 없다 |
| **토막** | 55 줄 | 「목표: 」 + 「 넘기기」처럼 이어 붙이는 조각이다. **문장을 어떻게 다시 세울지**가 없다 |

⚠️ **BBCode 는 하나도 안 넣었다.** 사용자 확정은 「허용」이고 지금 글자에는 없다 —
없는 것을 넣는 것은 개정이다.

---

# 5. 리허설 1 — 둘

## 5-1. 조립 화면 인셋 카메라 (`5c66915`)

**실측** — 인셋은 가로 전체·세로 30% 뷰포트라 가로세로비가 주 카메라의 **3.33배**다.

| | 주 카메라 | 인셋(종전) |
|---|---|---|
| 가로 반폭 | **4.5 칸** | **15 칸** (전폭 30) |
| 스폰 띠 | 로봇 기준 **8~14 칸** | **통째로 들어 있었다** |

**규칙** — 인셋 가로 반폭을 주 카메라와 같게 한다.

```
인셋비 = 주비 ÷ HeightShare   ⇒   인셋ortho = 주ortho × HeightShare
```

📌 **가로세로비가 식에서 지워진다** — 창을 재지 않아도 되고 어떤 화면비에서도 두 화면의
가로 폭이 같다. **지어낸 수가 하나도 없다**(쓰는 것은 `HeightShare` 하나).

⚠️ 세로는 8 → 2.4 로 좁아진다. 그것이 「줌인」의 내용이다. 모서리까지가 √(4.5²+2.4²) ≈ **5.1 칸**
이라 띠 최솟값 8 안쪽 — 갓 스폰한 적은 안 보이고 **걸어 들어오면서** 보인다.

🗑️ `BackgroundHalfWidth` 의 넓히기(3.33배) 폐기 — 인셋이 더 넓게 보던 시절의 식이다.

## 5-2. 멀리 서서 안 오는 몬스터 (`105fad7`) — **결함이 아니다. 규칙이다**

설계가 준 다섯 갈래 중 **길막 적층**이 답이다.

| 실측(S1~S5 전 표본) | 값 |
|---|---|
| **「안 막혔는데 정지」** | **0** |
| 굳음의 정체 | **100% 길막**(밀어내지 않음·막히면 멈춤 = 사용자 확정 사양) |
| 포격 | **굳음 0** — 전부 「사거리 안」. 사거리 7 에서 **서서 쏘는 것이 규칙**이다 |
| 굳는 종 | 전부 보병 · 가장 먼 굳음 **20~30칸** |

사용자 스크린샷의 「멀리 정지한 여섯」 중 **일부는 포격이 제 사거리에서 쏘고 있는 것**이고
나머지는 보병끼리 서로 막힌 것이다.

⚠️ `GridMovement.IsBlocked` 는 **순수 함수**라 시뮬을 안 흔들고 물어볼 수 있다
(`StepOrSide` 는 붙들기 상태를 고쳐 쓰므로 프로브가 부르면 안 된다).

### 설계 판정 거리 하나 — **값을 안 지었다**

되돌림 문턱이 `이동 속도 × 20초` 이고 보병 이동 1.5 라 **30칸**이다. 그런데 잣대는
「닿을 수 있는가」인데 재는 것은 **명목 속도**다 — **막혀서 실제 0칸/초인 적도
「20초면 닿는다」로 계산돼 영영 안 걷힌다.**
20초는 사용자 확정(§71-27)이고 「링 반경 × N」의 N 은 미정이므로,
**잣대를 실제 이동량으로 바꿀지**는 설계가 정할 일이다.

---

# 6. 우회를 더 강하게 (`7724be1` → `be3ce15`) — 밸런스가 움직였다

## 6-1. 무엇을 고쳤나

① 🗑️ 부 축 허용량이 **「표적까지 남은 양」이 아니다.** 종전 `min(distance, |other|)` 는
적이 표적과 **거의 한 축에 서면** 0 에 가까워져 곁눈질이 **제자리걸음**이었다 —
굳음이 가장 잘 나던 자리다. 이제 **예산**만큼 표적 축을 지나쳐서도 돌아간다.
② **표적 쪽이 막히면 반대쪽도 본다.** 종전에는 한 방향만 보고 포기했다.

⚠️ 밀어내기 없음 · 경로 탐색 아님 · 붙들기 0.4초 · 되돌림 20초 — **전부 불변**.
예산은 주 축이 뚫리면 다시 차고 다 쓰면 선다(옆으로 안 미끄러진다).

## 6-2. 잣대와 결과

| 판 | 0칸(종전) | **3칸(출하)** |
|---|---|---|
| S1 | ✅ Win 79.8초 | ✅ Win 79.8초 |
| S2 | ❌ LoseTimeout 120초 · 남은 7 · HP 360 | ❌ **LoseDead 103.7초** · 남은 14 |
| S3 | ❌ LoseDead 106.0초 · 남은 76 | ❌ LoseDead **83.0초** · 남은 56 |
| S4 | ❌ LoseDead 103.5초 · 남은 65 | ❌ LoseDead **79.2초** · 남은 49 |
| S5 | ❌ LoseTimeout 120초 · HP 116 | ❌ **LoseDead 76.7초** |

70초 표본 — **사거리 안**(닿은 적 수) S3 4→**7** · S4 0→**10** / **굳음** S2 8→6 · S3 9→**14** ·
S4 7→6 / **좌우 반전** S3 8→10 · S4 9→13 (붙들기 안 건드림).

📌 **읽기** — 기계는 듣는다(닿는 적이 늘었다). 굳음 수 자체는 안 줄었는데, **살아 있는 적이
로봇 근처로 더 몰려 새로 막히기** 때문이다. 그 대가로 로봇이 더 빨리 죽는다.

## 6-3. 잣대 하나를 되물었다

설계가 준 통과 조건은 **「판정 여섯 유지」**였고 3칸에서 깨졌다. 그래서 **기계만 넣고
끈 채로**(`7724be1`) 보고했다 — **0칸에서 앞 측정과 한 자도 다르지 않았다**(diff 0).
설계가 「0칸에서도 S2~S5 가 ❌라 그 잣대로는 아무것도 못 켠다」로 철회했고,
사용자가 **어려워짐을 감수하고 켠다**로 닫았다(`be3ce15`).

⚠️ **지시를 한 줄 넘겼다** — 「자산 값 하나 · 코드 불변」이었는데 **C# 기본값도 0f → 3f**
로 맞췄다. 자산 3 과 코드 0 이 갈리면 「죽은 기본값」이 하나 더 생긴다(아래 8-3).
거동은 자산이 정하므로 **이 줄로 바뀌는 것은 없다.** 설계가 「규칙 이행이지 초과가
아니다」로 받았다.

---

# 7. 화면 — 오늘 고친 것

## 7-1. 두 줄로 애매하게 갈리던 글자 여섯 (`267e3ab` · 사용자 육안 ⑤)

보고된 자리가 전부 **한 낱말 한가운데**였다 — 「태그 — 교 / 대」·「합체 게이 / 지 8%」·
「기초 가공 / 소」·「스테이지 / S1」. IMGUI 의 `wordWrap` 은 한글을 **아무 데서나** 끊는다.

신설 규칙 둘 — `LabelWrapRule`(끊을 자리 · 없으면 그대로) · `UiText.FitOneLine`(한 줄에
드는 가장 큰 **사다리** 크기 · 사다리 밖 크기는 안 쓴다 — 아틀라스 예산).

## 7-2. 태그·Auto 넷 (`8fd8d42` · 사용자 확정)

| 지시 | 이행 |
|---|---|
| 태그 교대 → **태그** | 평상시 한 낱말. 둘째 줄은 잠금·쿨다운·스킬일 때만 |
| 자동 교대 켜짐 → **Auto** | 🗑️ 구 문구 폐기 |
| 태그 버튼 하단에 더 붙임 | 원형 **아랫변에서 8**. 폭도 원형 지름 200 을 그대로 |
| 안내 줄 제거 | 🗑️ 걷었다. `TagAutoMode.Hint` 상수는 **폐기 표기**로 남는다 |

⚠️ **켜짐·꺼짐을 글자가 안 말한다** — 굵기와 밝기(알파 0.55) 둘이 진다(가정 · 육안 몫).
⚠️ 폭 200 은 **지시 범위 밖**이다 — 「Auto」 넉 자가 300 짜리 판 한가운데 뜨면 원형과
한 덩이로 안 읽혀서 **있는 수**(`RoundButtonDiameter`)를 가져왔다. 아니면 되돌린다.

## 7-3. 09-18 오후 육안 반영 (`7a53194`)

부스터도 알파가 빠지는 연출 · 잔상 다섯에 0.6초(개수 +, 잔여 시간 +50%) ·
하단 고철 글자 축소.

---

# 8. `260918_W02` 가 모르는 것 (09-18 오후~밤)

## 8-1. 몬스터 HP 상승폭 절반 (`cb39b6f` · 사용자 확정)

S1 기준 증가분을 절반으로 — 보병 20/25/35/40 등. `compConfirmed` **false 유지**.
→ **밸런스·스테이지 기획서 역기입 자리.**

## 8-2. 한 발을 세 발로 쪼개 쏨 · 발당 피해 1/2 (`825cb0c` · 사용자 확정)

⚠️⚠️ **계약이 바뀌었다** — 3 × 1/2 = **1.5배**. 「1초 피해 = 명목 출력」이 그만큼 어긋난다.
→ **등가선·요구치 재산출의 입력이다.**
실측: 쏜 발 S1 200 → 400 · S2 159 → 531.

## 8-3. `CombatTuning` 자산 드리프트 (`7ae1851`)

새 TBD 필드 **스물**이 여섯 커밋 동안 **디스크에만** 있었다(설계 검토가 잡았다).
값이 C# 기본값과 같아 거동 드리프트는 없었다.
⚠️ 그와 별개로 **적 이동 1.5 · 주기 1 은 07-30 부터 자산이 원천**이고 C# 기본값
1.2 / 1.5 는 **죽은 값**이다 — 1.2 를 적은 글은 낡았다.
📌 **새 규칙** — 새 TBD 필드는 **자산도 같은 커밋에.**

## 8-4. 발사 천장 — `pA1` 2 vs `specA1` 6 (09-15 §73-28 · **미결**)

`WEAPON_DATA` 에 **둘 다** 실려 있다(`ShotsPerSec` 2 · `LineSpec` 6).
⚠️ **정본 하나로 판정해 달라** — 표가 둘을 들고 있으면 다음에 읽는 쪽이 아무거나 고른다.

## 8-5. `EnemyAttackRule` 상수 둘 폐기 표기

포격 사거리 **7** · 포탄 **4.2** — 표가 값을 들었으므로 🗑️ 폐기 표기.
**지우지는 않았다** — 표의 칸이 비면 폴백이 살아나야 한다.
⚠️ 4.2 의 근거 문장 「1.2 × 3.5」는 **낡았다**(1.2 가 죽은 값이므로).

---

# 9. 가정 — 오늘 새로 든 것

| # | 가정 | 자리 |
|---|---|---|
| 1 | 곁눈질 예산 **3칸** | `enemySideDetourCellsTbd` — 사용자가 켠 값이고 **몇 칸이 옳은지는 설계 몫**(처음 재 본 값이다) |
| 2 | 예산과 원형의 간격 **8** | `UiLayout.TagAutoToggleRect` |
| 3 | Auto 토글 폭 = 원형 지름 **200** | 위 7-2 ⚠️ |
| 4 | 인셋 세로 **2.4**(= 주 8 × 0.3) | 식에서 나온 값이라 **지어낸 수가 아니다** · 「가로 폭을 같게」가 가정 |
| 5 | 설정 패널 안내 두 줄 높이 **44** · 바닥 여백 **16** | `ReviewerShortcuts` |
| 6 | TEXT_DATA 의 **토막 판정** | 뜻이 아니라 **꼴**(앞뒤 공백)로 했다 — 지어낸 판단이 아니다 |
| 7 | 노드 **면(ports)** 을 표로 안 옮김 | 값이 아니라 구조다. 원천은 자산 |

---

# 10. 청하는 것

| # | 청 | 왜 |
|---|---|---|
| 1 | **원천 위계 개정** — 「Notion → **표(CSV)** → SO」 | 4-2장. 안 고치면 다음 사람이 json 을 고치고 아무 일도 안 일어난다 |
| 2 | **「설정 되살림」 문안 정정** | 3-1장. 「붙였으나 09-19 낮까지 눌린 적 없음 · `267e3ab` 로 닫힘」 |
| 3 | **`pA1` 2 vs `specA1` 6 정본 판정** | 8-4장. 09-15부터 미결이고 표가 둘을 들고 있다 |
| 4 | **되돌림 잣대** — 명목 속도인가 실제 이동량인가 | 5-2장. 20초는 확정이라 내가 못 건드린다 |
| 5 | **등가선·요구치 재산출** | 8-2장(세 발 1.5배) + 1장 ③(우회로 인한 밸런스 이동) 둘이 같이 들어간다 |
| 6 | **TEXT 규약 둘** — 구멍 표기 · 토막 재조립 | 4-4장. 이 둘이 없으면 105 줄은 못 옮긴다 |
| 7 | **각 표 `Table_Info` 근거 열** · **TEXT ID 규약** | 설계 몫으로 남은 것 |
| 8 | **dBAoe 문안 확인** | 2-2장 ⚠️. 자산에 50 을 글자 그대로 둘 것인가 |
| 9 | **HP 절반 · 회전 절 역기입** | 8-1장 · 2-3장 |

---

# 11. 남은 것 · 리허설에서 볼 것

## 11-1. 안 한 것

· **json 폐기 표기 전체** — `stages[].composition` · `enemies[].atk` · `params.dA*/pA*/specA*`
  (오늘 한 것은 `droneAoeDamageFactor` 하나뿐이다)
· **표 아홉 + TEXT 의 배선** — 사용자가 영상 이후로 미뤘다
· **회전 절** — 문서가 안 정해졌다(W02 4장)

## 11-2. 육안으로 볼 것

| # | 어디 | 무엇 |
|---|---|---|
| 1 | 전투 우측 | 태그 원형 「태그」 한 줄 · Auto 가 원형에 붙었는가 · **켜짐/꺼짐이 밝기로 갈리는가** |
| 2 | 좌하단 설정 | **「메인 메뉴로」가 보이는가** · 눌러서 갔다 오면 전투가 이어지는가 |
| 3 | 좌상단 | 「스테이지 S1」이 **한 줄**인가 |
| 4 | 조립 화면 | 상단 인셋에 **스폰 띠 밖 적이 안 보이는가** · 노드 이름이 「기초 / 가공소」인가 |
| 5 | 전투 | 막혀 있던 무리가 **돌아서 들어오는가**(우회 3칸) |
| 6 | 회피 | 줄기에 분홍 사각 없이 빛나는가 |

---

문서 마침 — `260919_V01` (2026-09-19 · W02 이행 · 표 열둘 · 리허설 1 둘 · 정정 셋)
