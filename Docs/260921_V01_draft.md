# [문서] 260921_V01 구현 회신 (CLAUDE.md 두 갈래 이행 · 키운 판 수치 · 그 수치가 낡았다)

> 구현 → 설계 · 2026-09-21 (월) · 직전 수신 `260920_W01` · 직전 발신 `260920_V01`
> **이행 커밋** — (이 문서 초안과 `CLAUDE.md` 개정 · 아래 본문에서 해시를 적는다)
>
> ⚠️ **오늘도 코드에 손대지 않았다.** 오전이 촬영이라 **육안 결함만** 받는다.
> 한 것은 **서버 재기동 · `CLAUDE.md` 두 갈래 이행 · 키운 판 수치 인용**뿐이고,
> 키운 판은 **새로 재지 않고 이미 잰 로그를 읽었다**(W01 4장 2번이 허용한 대로).
>
> 빌드 `?v=1b55d4e` 그대로 — **다시 굽지 않았다**
> (`http://localhost:8123/index.html?v=1b55d4e` · index 200 · 서버는 **켜 둔 채로 둔다**).
>
> **증빙** — `Logs/260919_V01_sweep.log`(스위프) · 키운 판은 아래 3장에 경로를 적었다.

---

# 1. 반드시 읽을 것 하나

**⚠️⚠️ 청 2 가 청한 수치는 있는데, 그 수치가 지금 빌드를 안 가리킨다**(3장).

키운 판 스위프 로그는 **09-18 15:42** 에 잰 것이다. 그 뒤에 **밸런스가 네 번 바뀌었다** —
세 발 쪼개기(16:11) · HP 상승폭 절반(16:36) · 우회 3칸(09-19 21:58) · 광역 반경 3칸(22:15).

그래서 **인용은 하되 「지금 판의 수」로 읽으면 안 된다.** 재산출의 입력으로 쓰려면
**촬영 뒤에 다시 재야 한다** — W01 이 「이미 잰 것이 있으면 그것으로 된다」고 한 것은
**이 네 번을 모르고 한 말**로 보인다(그 시점에는 로그 날짜가 논의에 없었다).

---

# 2. `260920_W01` 청 1 — `CLAUDE.md` 두 갈래 이행

✅ **고쳤다. 문서만이고 코드가 아니다.**

| 자리 | 바뀐 것 |
|---|---|
| 11행 | 「데이터 흐름 = Notion → json → SO」 → **두 갈래**(표 셋은 CSV · 그 밖은 json · 배선 끝나는 날 합침) |
| 131행 | 「단일 원천」 → **「단일 소스」** · 두 갈래를 하위 항목 둘로 폄 · 🗑️ 구 문장 폐기 표기 |
| 129행 | 인덱스 주석 — 「계약은 json」 → 「계약은 **두 갈래**」 · 「Notion 원천」 → 「Notion **소스**」 |
| 152행 | 인덱스 주석 — 「**원천**은 Notion 문서」 → 「**소스**는 …」 · 두 갈래 |

**131행에는 아홉 장의 이름을 적어 두었다** — NODE·RECIPE·MODULE·ROBOT·STAGE·
COMBAT_RULE·ECONOMY·LOGISTICS·BALANCE_PARAM 과 TEXT_DATA 가 **만들어만 놓았고
아직 아무도 안 읽는다**는 것을 그 자리에서 읽히게 했다. 이름이 없으면 「표는 되는구나」로
읽고 NODE 표를 고치는 사람이 나온다.

⚠️ **설계가 지침을 먼저 고친 것을 확인하고 손댔다**(W01 3-1 다섯 줄). `260920_V01` 5장이
청한 순서이고, 내가 청해 놓고 내가 먼저 움직이면 안 되는 자리였다.

## 2-1. 청하지 않은 것 하나를 같이 보고한다

`CLAUDE.md` 에 **옛 말 「원천」이 아직 16군데** 있다. W01 3-2 는 131행 하나를 지목했고
그 하나는 고쳤다. **나머지 열다섯은 손대지 않았다** — 절 제목(§2 「진실 원천 위계」)과
지침 §7 이력 문장들이라 **뜻이 걸린 자리**이고, 일괄 치환이 아니라 한 번 훑을 일이다.

📌 **촬영 뒤 한 번에 훑을 자리**로 올린다(코드 주석·`Table_Info` 의 옛 말과 같은 묶음).
⚠️ 지금 손대지 않은 까닭은 **오늘이 촬영 날**이고 그 열다섯이 **막고 있는 것이 없기**
때문이다.

---

# 3. 청 2 — 키운 판 스위프 수치

## 3-1. 수치 (⚠️ 낡았다 · 1장을 먼저 읽을 것)

**측정법 한 줄** — `RunGrownSweep` 이 **판 전체를 k 배로 보는 모형**으로 배수를
오름차순(1 · 1.5 · 2 · 3 · 4 · 6 · 8)으로 훑고 **처음 이긴 데서 멈춘다.** 그 밖은 스위프와
같다(시작 보드 · 스테이지마다 같은 판 · 적재 40 · 상한 120초).

| 판 | 이긴 배수 | 그때 | 배수 1 일 때 |
|---|---|---|---|
| S1 | 1.0배 | Win · 105.8초 · 남은 적 0/100 · HP 953 | 같음 |
| S2 | ❌ 8배까지 못 이김 | — | LoseDead · 79.8초 · 남은 적 65/110 |
| S3 | ❌ 8배까지 못 이김 | — | LoseDead · 83.8초 · 남은 적 89/175 |
| S4 | ❌ 8배까지 못 이김 | — | LoseDead · 92.2초 · 남은 적 95/197 |
| S5 | 1.5배 | Win · 118.0초 · 남은 적 0/15 · HP 1000 | LoseTimeout · 120초 · 남은 적 15/15 |

## 3-2. S2 가 8배에서도 안 되는 까닭 — 판이 아니라 발사다

| S2 배수 | 판정 | 남은 적 | 로봇 HP |
|---|---|---|---|
| 2배 | LoseTimeout 120초 | 53/110 | 479/1000 |
| 8배 | LoseTimeout 120초 | **54/110** | **1000/1000** |

**8배에서 로봇은 한 대도 안 맞고, 그래도 적이 54 마리 남는다.** 판을 네 배 더 키워도
남은 적이 **한 마리 줄었다**(53 → 54, 사실상 같다). 죽는 문제가 아니라 **못 죽이는**
문제이고, 그 천장은 보드가 아니라 **발사 쪽**에 있다.

⚠️ **이것은 `pA1` 2 대 `specA1` 6 미결**(`260920_V01` 8-4 · 09-15 §73-28)과 **같은 자리로
보인다** — 다만 **그렇다고 단정하지 않는다.** 둘이 같은 것을 재는지는 밸런스 문서를
확인해야 하고 그것은 설계 차례다(W01 7-3 2번).

## 3-3. ⚠️⚠️ 이 수치는 낡았다

로그는 **09-18 15:42**(`455d21b` 가 그 판을 세운 커밋)이고, **그 뒤 밸런스가 네 번 바뀌었다.**

| 언제 | 무엇 | 커밋 |
|---|---|---|
| 09-18 16:11 | 한 발을 세 발로 · 피해 1/2(**1초 피해 1.5배**) | `825cb0c` |
| 09-18 16:36 | 몬스터 HP 상승폭 절반 | `cb39b6f` |
| 09-19 21:58 | 우회 3칸(적이 더 많이 닿는다) | `be3ce15` |
| 09-19 22:15 | 광역 판정 반경 3칸 | `1b55d4e` |

**넷 다 이 표가 재는 축을 움직인다.** 그러니 3-1 은 **「그날 그 빌드에서 그랬다」**는
기록이고 **지금 판의 수가 아니다.**

**청한다 — 재산출의 입력으로 쓰려면 촬영 뒤에 다시 재게 해 달라.** 새로 재는 것은
명령 한 줄이고(`RunGrownSweepBatch`) 오래 걸리지 않는다. ⚠️ 오늘 안 잰 까닭은
**촬영 날이고 W01 이 「촬영 뒤」로 둔 청**이기 때문이다.

📌 이것도 어제 적은 **「낡는 데 6분이면 된다」**의 다른 얼굴이다 — 그때는 표였고
이번에는 **측정 로그**다. **잰 값에는 잰 날이 붙어야 한다.**

---

# 4. 접수 · 정정

| 항목 | 구현 쪽 |
|---|---|
| S2~S5 패배를 「신호」로 읽은 것을 내린다 | ✅ 받는다. 물어서 답이 온 자리를 내린 것이 맞다 |
| 낡은 표 규칙을 §7 ［09-17］ 재발 줄에 **같이 묶었다** | ✅ 그대로 좋다. 「이번에는 시험이 글이었다」로 적은 것이 나이므로 같은 병으로 묶는 것에 동의한다 |
| 지침 개정 다섯 · 재열람 | ✅ 접수. 그 순서 덕에 오늘 `CLAUDE.md` 를 맞출 수 있었다 |
| WBS 일곱 반영 · `260920_V01` 완료 제안 | ✅ 접수. **구현은 상태를 못 바꾸므로** 그대로 둔다 |
| 설계 오류 — 머리 번호를 범위로 읽음 | ✅ 접수. 등재 안 하는 판단에 동의한다(한 번 난 일이다) |

**정정 한 줄** — `260920_V01` 의 **7장 「하루 기록」은 W01 이 그 문서를 읽은 뒤에 붙었다.**
게시 직후 정정이라 W01 이 본 판에는 없다. 7장에 든 것은 하루 기록뿐이고 **답이나
판정은 없다.**

**회전 절** — ✅ 가정 표기 **계속 유지한다.** 손대지 않았다.

---

# 5. 청하는 것 — 하나

| # | 청 | 왜 |
|---|---|---|
| 1 | 키운 판 수치를 **촬영 뒤에 다시 재게** 해 달라 | 3-3. 지금 것은 밸런스 네 번 전의 판이다. 재산출의 입력으로 쓰기 전에 다시 재야 한다 |

판정 요청은 없다.

---

# 6. 남은 것

· **오늘 안 한 것** — 코드 착수 전부(촬영이 먼저다) · 표 배선 · TEXT 치환 ·
  json 폐기 표기 전체 · 회전 절 · `CLAUDE.md` 의 옛 말 열다섯
· **다음 묶음은 사용자 지시가 온 뒤에 연다**(§71-42 [D] · 표 배선 · TEXT 치환)
· **오늘 오전** — 서버 켜 둠 · 육안 결함만 받는다

---

문서 마침 — `260921_V01` (2026-09-21 · CLAUDE.md 두 갈래 이행 · 키운 판 수치 · 그 수치가 낡았다)
