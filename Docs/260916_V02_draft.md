# [문서] 260916_V02 구현 마무리 회신 (보드 로봇별 분리 · 드론 2종 · 하네스 답 · 하루 기록)

> 구현 → 설계 · 2026-09-16 (수) 마무리 · 직전 발신 `260916_V01`
> 입력: `260916_W03` · 사용자 육안 10차~14차 · 플랜 §74-15 ~ §74-22
> **장 번호는 `260916_V01` 에서 이어 센다** — 이 문서는 9장부터다.
> ⚠️ **이해도 점검(퀴즈)은 건너뛰었다** — 사용자 지시. 흉내 내지 않는다.

---

# 9. 반드시 읽을 것 넷

**① S1 의 손잡이가 바뀌었다 — 묶는 것은 공급이 아니라 발사율이다.**
`260916_W03` 이 물은 「쏜 발이 공급의 절반」의 답이다. **조건마다 답이 갈린다.**
여태 잰 것은 **빈손 하나**뿐이었는데, 플레이어가 S1 에 들어서는 정상 경로는
**튜토리얼 종료 상태**(마운트 40 가득)다. 그 판에서는 **재고 0 인 틱이 한 번도 없고**
마운트 평균이 **39.9/40** 이다 — **공급이 남는다.** 못 쏜 틱의 **88.9%** 가
「표적도 있고 재고도 있는데 안 쏨」이다. 자세한 수는 11장.

**② 보드가 로봇별 둘로 갈렸고, B 판이 실제로 돈다.**
판이 자기 주인을 들고(`BoardGrid.Owner`) 둘 다 매 틱 돈다 — 비용은 쟀다
(두 판 한 틱 **80 마이크로초 · 프레임 예산의 0.48%**). B 시작 보드는 완성본이며
**120초에 드론 116 기**가 마운트에 닿는다. 저장도 둘이고 왕복이 선다.

**③ 오늘 사용자 확정을 전부 이행했다 — 열둘.** 목록은 10장.
그중 **내가 세운 가정 셋이 사용자 육안에 뒤집혔다**(길게 누르기 · 편집 탭 축 ·
정박). 셋 다 「규칙으로는 깨끗했는데 써 보니 틀린」 것이다 — 14장에 따로 적는다.

**④ 판정 요청은 여섯이다**(12장). 그중 **면 화살표 규칙 ②③** 은 내가 고른 것이라
설계가 봐 줘야 한다 — 안 그러면 코어와 에너지 노드에 화살표가 **영영** 남는다.

---

# 10. 오늘 한 일 — 커밋 스물

`260916_V01` 게시(`336bc09`) 뒤부터다.

| 커밋 | 한 일 |
|---|---|
| `bf6bacd` | 방향 양자화 120° · 상단 배경 결손 · 스폰 띠 안쪽 8 |
| `1f6b6ff` | 보드 로봇별 둘 — 판·저장·틱 분리 · B 시작 보드 |
| `d90535f` | 로봇 탭 A/B · `SupplySignals` 로봇별 |
| `8c6ec4c` | S1 하네스 — 시작 조건 둘 · 못 쏜 까닭 가름 |
| `d714d02` | 태그 조작 — 자동 교대 스위치 · 길게 누르기 · 대기 적재 품목 |
| `2b029d5` | 결함 둘 — 교대 자리 · B 마운트에 탄약 |
| `0fff804` | 튜토리얼 — 벨트 드래그 설치·제거 안내 |
| `70d2494` | B 판 반쪽(`munix` 못 찾음) · 안내 시점 고침 |
| `8047dc3` | 탭 임시 진단 걷음 |
| `477d513` | 길게 = 자동 켬/끔 · 탭이 활성 로봇을 따라간다 |
| `6057fb7` | 만재는 경고가 아니다 · 태그 스킬 강조 |
| `9a92a44` | 적 포탄 배선 자리 · 태그 강조 테두리 · 폐기 모듈 F 표기 |
| `a98b93b` | 드론 2종 · 사출 뒤 이동 |
| `cd736da` | 드론 기당 피해 1/10 · 복합 군수를 팔레트에 |
| `092fce5` | 드론 타격 간격 0.5초 |
| `6d2957a` | 아트 — 포격 투사체 `vfx_enemy_shell` 설치 |
| `c97f1c0` | 태그 스킬 회전 폐기 · 드론 조합표 결함 · 적 포탄 자산 |
| `54df5f5` | 도착한 드론이 곧 적재 · 거대 주황 사각 · 모드 판 겹침 |
| `77ca0b6` | 면 화살표 — 아직 다 안 이어진 노드에만 |
| `165b45b` | 출력 아이콘을 칸 가운데로 · 노드 이름판 오토 스케일 |

**마지막 빌드 `?v=165b45b`** · **EditMode 1107 중 1101 통과 · 0 실패 · 6 건너뜀**
(아침 1023 → **시험 84 개 늘었다**).

## 10-1. 사용자 확정 이행 — 열둘

| # | 확정 | 이행 |
|---|---|---|
| 1 | 방향 양자화 120°(±60° 유지) | `DirectionHysteresis.RetainDegrees` · 배수는 `tan 60°` 로 **계산해서** 낸다 |
| 2 | 스폰 띠 안쪽 4 → 8 | 코드 기본값과 SO **둘 다** · 「둘이 같은가」를 시험이 박는다 |
| 3 | 보드 로봇별 분리 | 판·저장·틱·탭·`SupplySignals` |
| 4 | B 시작 보드 = 완성본 | `StartingBoardB` · 120초 도착 116 |
| 5 | 태그 오토/수동 | `TagAutoMode`(`PlayerPrefs` · 기본 꺼짐) |
| 6 | 길게 = 자동 **켬/끔** | 토글 · 뒤집는 자리 한 곳 |
| 7 | 편집 탭이 활성 로봇을 따라간다 | 교대·진입 둘만 · 손을 안 이긴다 |
| 8 | 만재는 경고가 아니다 | 손에 든 것이 있으면 띠를 안 띄운다 |
| 9 | 태그 스킬 가능 시 강조 | 밝기 + 테두리 · 판정은 실제 발동 조건 그대로 |
| 10 | 드론 2종 · 사출 뒤 이동 | 광역 = 궤도 추종 · 누적 = 붙음 |
| 11 | 기당 피해 1/10 · 간격 0.5초 | 총 피해·수명 불변 |
| 12 | 복합 군수를 팔레트에 · 면 화살표 | 팔레트 일곱 · 화살표는 덜 이어진 노드만 |

---

# 11. `260916_W03` 이 물은 둘 — 하네스 답

## 11-1. 시작 조건을 둘로 갈랐다

| | **튜토리얼 종료**(정상 경로) | **심사자 S1 점프**(빈손) |
|---|---|---|
| 승패 | LoseDead | LoseDead |
| 걸린 초 | **81.3** | 54.8 |
| 남은 적 | **21 / 100** | 45 / 100 |
| 쏜 발 | **160** | 49 |
| 첫 발사 | 0.5초 | 6.1초 |
| 출력 축(굴린 60초) | **20.2** ✅ | 18.1 ✅ |

⚠️ **창고는 안 채웠다** — 튜토리얼은 마운트까지 나르고 끝난다. 지어 넣지 않았다.

## 11-2. 못 쏜 까닭 — 틱마다 하나씩 · 차례가 인과다

표적 없음 → 재고 0 → 게이트 차례로 센다. 겹쳐 세면 합이 뜻을 잃는다.

| | 튜토리얼 종료 | 심사자 빈손 |
|---|---|---|
| 쏜 틱 | 9.8% | 4.5% |
| 표적 없음(사거리 밖 대기) | 1.2% | 0.0% |
| 재고 0(창고→마운트 이송) | **0.0%** | **54.8%** |
| 둘 다 있는데 안 쏨(게이트·간격) | **88.9%** | 40.7% |
| 마운트 평균 | **39.9 / 40** | 0.2 |
| 창고 최고 | 3.0 | 0.0 |

**가름 한 줄** — 정상 경로에서는 **공급이 남고 발사율이 묶는다.**
설계가 본 「48초에 43발」은 **빈손 판의 수**다.

⚠️ **곁다리 하나** — 마운트가 늘 가득이라 도착이 막히는데 **창고 최고가 3.0** 뿐이다.
갈 곳이 없으면 상류가 멈추므로 **출력 20.2 는 막힌 상태에서 나온 수**다.
지금 판에서 물류를 더 늘려도 전투가 못 먹는다(「두 축」 논의와 같은 자리).

---

# 12. 판정·역기입 요청 — 여섯 (#29 ~ #34 뒤)

| # | 요청 | 왜 |
|---|---|---|
| A | **S1 손잡이 = 발사율 쪽을 먼저** | 11장 실측. 스폰 간격(가)으로 가기 전에 발사율을 보는 편이 싸 보인다 — 값은 설계 몫 |
| B | **면 화살표 규칙 ②③** | ② 출력은 **하나만** 이어지면 됨 · ③ **전력 포트는 이어진 것으로 셈** — **둘 다 내 판단**이다. 안 그러면 코어 넷·에너지 셋에 화살표가 **영영** 남는다(13-2) |
| C | **광역 타격 연출 자산** | 광역형의 광역 타격에 **그림이 없다.** 폭발탄 스플래시 칸을 빌렸다가 화면 절반이 주황 사각이 됐다(13-1) — 지금은 **안 그린다** |
| D | **만재 정체 규칙** | 「손에 든 것이 있으면 안 띄운다」는 **가정**이다(문서에 「만재 정체」 절이 없다) |
| E | **대기 보드 잉여 산출** | 마운트가 차면 상류가 멈춘다. **남는 것을 어디에 쌓을 것인가**가 안 정해져 있다 |
| F | **역기입 #25 ~ #34** | 아래 가정 목록이 전부 그 대상이다 |

## 12-1. 가정 목록 — 전부 설계 역기입 자리

| 자리 | 값 | 비고 |
|---|---|---|
| 자동 교대 기본값 | **꺼짐** | 「길게 = 켬/끔」이면 처음은 꺼짐이어야 말이 된다 |
| 길게 누르기 | **0.6초** | |
| 안내 문구 | 「탭 = 교대 · 길게 = 자동 켬/끔」 | 앞말은 사용자 확정 · 뒷말 가정 |
| 로봇 탭 자리·크기 | 부유 띠 왼쪽 세로 · **200×150** | 가로로 나누면 97 이라 최소 버튼 150 미달 |
| `StartingBoardB` 배치 | 전체 | 줄의 모양만 확정 · 쓰는 포트 **(9,10)** 하나 |
| 대기 마운트 품목 표시 | 글자 · 관통·표준·폭발·드론 차례 | 칸 그림 아님 |
| 벨트 손짓 안내 | 문안 · **6초** | |
| 배경 여유 장수 | **한쪽마다 두 장** | 뒤처짐 0.75 유닛을 흡수 |
| 드론 궤도 반경 · 각속도 | **1.6** · **1.6 rad/s** | ≈ 4초 한 바퀴 |
| 드론 비행 속도 · 붙음 거리 | **6.0** · **0.35** | 로봇 4.5 보다 빨라야 따라잡는다 |
| 드론 기당 피해 | 충전량 × **0.1** | 총 피해·수명 불변 |
| 드론 타격 간격 | **0.5초** | 한 기가 약 5초 산다 |
| 붙은 적이 죽으면 | **다음 적으로** | 남은 충전량을 버리는 편이 더 나쁘다고 봤다 |
| 드론 사출 이펙트 배율 | **0.5** | |
| 적 포탄 색 | 주황 계열 | 자산은 무채색 · 코드가 틴트 |
| 적 포탄 병종 구분 | **없음** | 시뮬의 포탄이 누가 쐈는지를 안 든다 |
| 태그 강조 | 밝기 **1.7** · 테두리 노랑 **10px** · 「(스킬)」 | 사각 테두리 — 원 자산이 없다 |
| 면 화살표 | 칸의 **1/4** · 미색 · 안쪽 **0.33칸** | 자산은 기존 `arrow.png` |
| 이름판 줄 수 | **최대 두 줄** | 사용자 확정 |

---

# 13. 구현 오류 기록 — 오늘 아홉

**13-1. 내가 만든 결함 셋.**
① **거대 주황 사각** — 광역형 사격의 `aoeRadius` 에 **사거리 9.2** 를 넣었다. 그 칸은
폭발탄 스플래시 그림용이고 화면이 **지름 = 반경 × 2** 로 그린다 → 한 변 **18.4 유닛**.
📌 **없는 자산을 있는 칸에 밀어 넣지 않는다.**
② **열 번을 한 틱에 쏟았다** — 피해를 1/10 로 나누고 **간격을 안 뒀다.** 0.17초에
다 끝나 한 방과 같았다. **쪼개는 것과 띄우는 것은 다른 일**이다.
③ **탭 임시 진단을 넣고 걷었다** — 이것은 오류가 아니라 절차지만, 진단 줄이
자리를 닫은 뒤 **그대로 남는** 일이 이 리포에 반복돼 기록해 둔다.

**13-2. 규칙이 한 번에 안 선 것 — 면 화살표.**
「모든 면이 이어져야 사라진다」로 짰더니 **다 이은 시작 보드에 일곱 개가 남았다**
(코어 넷 · 에너지 셋). 코어의 안 쓴 출력면은 「**플레이어가 줄을 더 놓을 자리**」이고
에너지는 **벨트를 안 문다** — 둘 다 「할 일」이 아니다. 시험이 세 번 고쳐 줬다.

**13-3. 내 예상이 틀린 것.** 「튜토리얼 빈 칸이면 그 줄에 화살표가 돈다」로 시험을
적었는데 **0 이 나왔다.** 틀린 것은 코드가 아니라 나였다 — 비워 둔 칸은 **벨트 줄
한가운데**이고 양옆 노드의 면은 여전히 물려 있다.

**13-4. 어제 적은 것을 정정했다.** 「드론 조합표 둘이 `default` 로 떨어져 탄약에
들어갔다」는 **반만 맞았다** — 복합 군수는 그 `switch` 에 **닿지도 않았다**
(`type != MunitionsBasic` 조건이 위에서 걸렀다). 갈래만 보고 그 위의 `if` 를 안 봤다.

**13-5. 폐기를 한쪽만 한 자리 셋.**
· **모듈 F** — 09-02 에 코드는 지웠는데 `balance_v4.json` 에 남아 **화면이 없는 것을
말했다**(S2 목표 「모듈 배치(M/R/F)」).
· **부유 띠 왼쪽** — 「튜토리얼 두 줄이 쓴다」는 주석이 남아 있었다(그 두 줄은
09-15 에 보드 띠로 나갔다).
· **`DrawMiniMap`** — 부르는 곳 0 건인데 자리는 남아 있었다(이번에 그 자리를 썼다).

**13-6. 같은 병이 다시 났다 — 자리를 비율로 잡았다.**
09-15 에 튜토리얼 두 줄을 부유 띠에서 내보내며 「**띠 안에서 자리를 나누는 것으로는
못 푼다**」고 적어 두고도, 폭을 **62% 비율**로 잡아 좁은 창에서 모드 판과 다시 만났다.
지금은 **이웃의 자리에서 자른다.**

**13-7. 크기를 한 변에서만 잡았다 — 오늘 네 번.**
탭 줄 · 팔레트 · 원형 버튼 · **노드 이름판**. 마지막 것은 높이만 보고 글자 크기를
정해 **긴 이름이 칸 밖으로 넘쳤다.**

**13-8. 시험이 폐기를 잡았다.** 도착 적재로 바꾸자 **구 시험 열둘이 한꺼번에** 빨갛게
떴다 — 전부 폐기된 입력(`DroneInflowRate`)으로 마운트를 채우고 있었다.

**13-9. 시험이 팔레트 구멍을 잡았다.** 복합 군수가 팔레트에 없어 **플레이어가 못
놓았다.** 관통탄·폭발탄·드론 둘이 전부 그 노드를 거친다 — **로그도 예외도 안 냈다**
(화면에 **없는 버튼**이라 아무 신호가 없다).

---

# 14. 내가 세운 가정이 육안에 뒤집힌 셋

**규칙으로는 깨끗했는데 써 보니 틀렸다.** 셋 다 시험으로 내려 두었다.

| 내가 세운 것 | 왜 그렇게 봤나 | 왜 틀렸나 |
|---|---|---|
| 길게 누르기는 **켜기만** | 「길게 켜고 길게 끄면 지금 어느 쪽인지 모른 채 누른다」 | **반대였다** — 켠 뒤 안 꺼지니 **끄는 길이 없는 것처럼** 느껴진다. 지금 상태는 토글 버튼이 글자로 말한다 |
| 편집 축과 전투 축은 **따로** | 탭 = 무엇을 편집하는가 · 태그 = 누가 싸우는가 | B 로 교대해도 조립이 A 판을 보여 **싸우는 로봇의 줄을 못 봤다** |
| 드론은 사출 자리에 **정박** | 문서에 이동 규칙이 없어 구현이 골랐다 | 로봇이 걸어가면 **드론만 뒤에 남는다** |

📌 **공통점** — 셋 다 **문서에 절이 없어 내가 고른** 것이고, 셋 다 **화면에서만**
드러났다. 시험은 「규칙대로 도는가」를 지킬 뿐 **규칙이 맞는가**는 못 본다.

---

# 15. §7 등재 후보 — 셋

**① 자리는 비율이 아니라 이웃에서 잰다.** 화면 요소의 폭·높이를 창 크기의 비율로
잡으면, 창이 좁아질 때 **옆엣것과 만난다.** 이웃의 모서리에서 자르면 창과 무관해진다.
(13-6 · 오늘 두 번째 발생)

**② 크기는 두 변에서 잡는다.** 높이만 보고 글자 크기를 정하면 **폭에서 잘린다.**
오늘만 네 곳에서 같은 모양으로 났다. (13-7)

**③ 갈래를 볼 때 그 위의 조건을 함께 본다.** `switch` 안을 고치면서 바깥 `if` 가
그 값을 **애초에 거르고 있던 것**을 놓쳤다. 「갈래에 없다」와 「갈래에 닿지도
않는다」는 다른 일이고, 고치는 자리가 다르다. (13-4)

---

# 16. 하루 기록

## 16-1. 완료 처리 후보 (⚠️ WBS 상태 변경은 설계 권한 · 여기서는 후보만)

| 후보 | 근거 |
|---|---|
| 보드 로봇별 분리 | `1f6b6ff` · `d90535f` · 시험 · 사용자 육안 통과 |
| B 시작 보드 | `1f6b6ff` · 도착 116 · 육안 통과 |
| 태그 조작(오토/수동) | `d714d02` · `477d513` · 육안 통과 |
| 드론 2종·이동·적재 | `a98b93b` ~ `54df5f5` · 육안 통과 |
| S1 하네스 시작 조건 둘 | `8c6ec4c` · `260916_W03` 요청분 |
| 면 화살표 | `77ca0b6` · `165b45b` · 육안 통과(규칙 ②③ 판정 대기) |

## 16-2. 이해도 점검 — **건너뛰었다**(사용자 지시). 흉내 내지 않는다.

## 16-3. 내일 할 일 다섯

| # | 할 일 | 출처 |
|---|---|---|
| 1 | **설계 `260917_W01` 을 받아 S1 값 반영** | 설계 지정 · 내일 첫 일 |
| 2 | 면 화살표 규칙 ②③ 판정 반영 | 12-B |
| 3 | 길게 누르기 **터치** 확인 결과 반영 | 아무도 못 봤다 |
| 4 | 대기 보드 잉여 산출 규칙이 오면 반영 | 12-E |
| 5 | 광역 타격 연출 자산이 오면 배선 | 12-C |

## 16-4. 오늘의 주의 다섯

① **문서에 절이 없는 자리는 내가 고른 것이고, 고른 것은 화면에서만 검증된다**
(14장). 「시험이 통과했다」는 **규칙이 맞다**는 뜻이 아니다.
② **폐기는 양쪽을 봐야 한다** — 코드·데이터·주석·자리 넷 중 하나만 지우면 남은 쪽이
거짓말을 한다(13-5).
③ **갈래를 고칠 때 그 위의 조건을 먼저 읽는다**(13-4 · §7 후보 ③).
④ **크기와 자리는 두 변·이웃에서 잡는다**(13-6 · 13-7).
⑤ **시험이 한꺼번에 빨개지면 폐기를 잡은 것일 수 있다** — 되돌리기 전에 무엇이
바뀌었는지부터 본다(13-8).

---

# 17. 아직 못 본 것 · 사용자 몫

- **길게 누르기가 터치에서 되는가** — 자리는 전투 화면 태그 원형(캔버스 가로 89% ·
  세로 81%). `MouseDown`/`MouseUp` 으로 짰다. **마우스로 먼저, 손가락으로 그다음**
  해 보면 원인이 갈린다.
- **B 로 실제 전투를 끝까지** — 드론 사출·소진·교대 한 바퀴.
- **광역형 드론 궤도** — 복합 군수 조합표를 광역형으로 바꾼 판에서.
- **좁은 창**(615×1920 근처)에서 모드 판·튜토리얼 두 줄·탭 줄이 안 겹치는가.
- **면 화살표가 실제로 벨트를 물리는 순간 사라지는가** — 시험은 값 층만 본다.
