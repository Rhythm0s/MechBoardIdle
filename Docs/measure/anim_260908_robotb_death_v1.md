# 애니메이션 측정 보고서

- 생성 시각: 2026-09-08 10:41
- 도구 커밋: `442311d`
- **잰 자리: `C:/Users/Kang/AppData/Local/Temp/claude/C--Users-Kang-OneDrive------/4b27ebae-8565-4ef9-951d-a870936a2e68/scratchpad/b1root`** (`-animRoot`로 바꾼다)
- **진폭 측정법: 프레임마다 알파 bbox의 윗변을 잡아, 그 윗변의 최댓값과 최솟값의 차를 프레임 평균 실루엣 높이로 나눈다.** 어깨가 오르내리는 폭이라 윗변으로 잰다
- **최대·최소 방식이다 — 프레임 사이 이동량을 더하는 방식이 아니다**(`260907_W01` 확인 1). 같은 그림을 여러 칸이 가리켜도 최대와 최소가 안 움직이므로 **칸 복제는 이 값을 바꾸지 않는다**
- ✅ **규격 분모는 첫 칸 실루엣 높이다**(`260908_W01` 2-1 확정). 평균 열은 참고로 남긴다. `평균`은 벌의 프레임 평균 실루엣 높이이고, `첫 칸`은 **첫 프레임의 실루엣 높이**다. 무너지는 동작(사망)에서는 둘이 크게 갈린다 — 몸이 낮아지면 평균이 함께 낮아져 같은 픽셀이 더 큰 비율로 나온다
- **가로 열 셋** — 첫 칸 폭 · 벌 안 최소 폭 · 최대 폭(알파 bbox). 주저앉음의 잣대 둘째가 「세로가 줄 때 가로가 따라 줄지 않을 것」이라 **가로가 어느 쪽으로 갔는지**를 봐야 한다(`260908_W01` 2-3 · 3-2). **마지막 칸 열을 함께 낸다** — 주저앉음은 끝난 자세가 결과이고, 중간 칸의 최소는 지나가는 값일 수 있다
- 알파 문턱: 16 초과를 「있다」로 본다 · 캔버스 그대로만 잰다(자르거나 늘이지 않는다)
- 계산: `MBI.Core.SilhouetteOverlap.TryBounds` 재사용
- 도구 커밋은 **실행 시점의 HEAD**다. **잰 파일이 무엇인지는 아래 표의 md5가 말한다**

- 칸 열 셋은 `MBI.Core.Anim.AnimSchedule`이 낸다 — 한 칸 1/16초 · 목표 초는 `CombatTuning`(`260907_W01` 4-5)

| 벌 | 그림 | 캔버스 | **실루엣 높이(첫 칸)** | 실루엣 높이(평균) | 여백 T/B(최소) | 세로 변화 px | **세로 %(첫 칸)** | 세로 %(평균) | **가로(첫 칸)** | **가로(마지막 칸)** | 가로 최소 | 가로 최대 | 잴 수 있나 | 기본 칸 | 필요 칸 | 실제 초 | 첫 프레임 md5 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `robot_b_Death/south` | 9 | 256×256 | **235** | 218.6 | 9 / 12 | 35 | **14.9%** | 16.0% | 218 | **211** | 203 | 218 | 예 | 9 | 32 | 2.00 | `b31d732d` |

**1벌.** 대기 진폭 규격은 256 이상에서 실루엣 높이의 **4~6%**다 (캐릭터 아트 요청 문서(15)「동작의 크기」).

> 이동·사망·태그 행의 진폭은 규격이 없다 — 대기만 4~6%로 판정한다. 나머지는 참고로 둔다.

⚠️ **「잴 수 있나」가 「아니다 — 잘림」이면 그 행의 진폭은 판정 근거가 아니다.** 실루엣이 캔버스 가장자리에 닿아 있으면 bbox의 윗변이 더 올라갈 자리가 없어, 몸이 실제로 오르내려도 숫자가 안 움직인다. 그런 행의 진폭은 **하한값**이다 — 「이만큼은 움직였다」이지 「이만큼만 움직였다」가 아니다.

> 여백이 0인 자산에서는 4~6% 진폭 자체가 캔버스 안에 들어가지 않는다. 진폭을 규격대로 얻으려면 스틸에 여백이 먼저 있어야 한다.

## 칸 규칙 경고

**없다.** 삭제된 칸이 없고 규칙을 지킬 수 없던 벌도 없다 — 모든 그림이 화면에 적어도 한 번 나온다.
