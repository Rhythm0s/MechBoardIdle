# [문서] 260918_V02 구현 회신 (둘째 드론 줄 · 광역형 절반 · 두 로봇 하네스 · 결함 둘)

> 구현 → 설계 · 2026-09-18 (금) · 직전 수신 `260918_W01` · 직전 발신 `260918_V01`
> **이행 커밋 넷** — `e42ab32`(광역 반경 2칸 · 스택 비를 자산으로 · 두 로봇 하네스) ·
> `2c4320c`(둘째 드론 줄 첫 판 · 태그 피해 누락 수정) ·
> `197efd3`(광역형 표적당 절반 · 광역 중심 정정 · 적 간격과 반경 스위프) ·
> `196328d`(**둘째 드론 줄을 포트 (2,10) 로 다시 짠다 — 노드를 180도 돌려서**)
> EditMode **1185 중 1179 통과 · 0 실패 · 6 건너뜀** (결과 xml — `scratchpad/t14.xml`)
> 빌드 **`?v=196328d`** (`http://localhost:8123/index.html?v=196328d` · index 200) — **촬영 빌드**다

---

# 1. 반드시 읽을 것 셋

**① ⚠️⚠️ 결함 하나를 하네스가 잡았다 — 태그 스킬이 광역형 드론을 세지 않고 있었다.**
평균 발당피해를 내는 자리가 `MountItem.Drone`(누적형)만 세고 있었다. 09-16 에 드론이
2종이 되면서 생긴 누락이고, **소진은 하면서 피해에는 안 넣었다.** 둘째 줄을 깔자
총 피해가 **4,000 → 2,500** 으로 떨어져 드러났다(5-2). 고쳤다.

**② ⚠️⚠️ 결함이 둘이었다 — 광역 판정을 재는 자리도 틀렸다.**
곁에 닿는 적을 **드론 자리**에서 재고 있었다. 드론은 로봇 둘레를 돌고 사거리가 9.2 라
**표적에서 멀리 떨어진 채 쏜다** — 반경이 사거리와 같던 동안에는 그래도 곁의 적이
들어왔지만, 반경이 2칸이 되자 **곁에 닿는 적이 0** 이 됐다. 지금은 **표적 둘레**에서 잰다(5-4).

**③ ✅ 「광역형은 표적 하나당 절반」을 코드에 넣었다**(사용자 확정 · 6-3 답).
`params.droneAoeDamageFactor = 0.5` · 가정 표기. ⚠️ **줄인 것은 타격당 피해뿐**이고
충전량·수명은 그대로다 — 문서가 둘 중 어느 쪽인지 안 적어 구현이 닫은 자리다(5-5).

---

# 2. `260918_W01` 접수 — 답 넷

| # | 설계 답 | 코드에 한 일 |
|---|---|---|
| 1 | 광역 판정 반경 **2칸**(잠정 점값 · 확정은 본전 4마리) | **이행** — `params.droneAoeJudgeRadius = 2` · `confirmed:false` · 판정과 고리가 **한 함수**를 본다 · 고리 **켰다** |
| 2 | 둘째 드론 줄을 깐다(팔R·어깨R · 분류기 없이) | **이행 — 길이 두 번 바뀌었다.** 분류기 없이는 자리가 없어 분류기 둘 → 다시 **노드를 180도 돌려 분류기 하나**로 섰다. 도착지도 병합기 합류 → **포트 (2,10)** 로 닫혔다(사용자 확정 · 4장) |
| 3 | 두 로봇 하네스 판을 만든다 | **이행** — 재는 목적 넷이 판에 붙어 있다(5장) |
| 4 | 마운트가 두 드론을 무엇으로 채우는가(사실 확인) | **답했다** — 호출 순서다(3장) |

---

# 3. 5장 답 — 마운트는 **호출 순서**로 채운다

주석이 아니라 **호출자를 돌려서** 쟀다(`CombatSimulation.LoadDronesInto`).

| 물음 | 사실 |
|---|---|
| 기준이 무엇인가 | **호출 순서**다 — 누적형을 먼저 싣고 광역형을 뒤에 싣는다. **탄종 열거 순서가 아니다** |
| 유입 비율대로 차는가 | **찬다** — 3:1 로 넣으면 3:1 로 앉는다(시험 넷) |
| 슬롯이 모자랄 때는 | **먼저 부르는 쪽(누적형)이 마지막 빈 칸을 가져간다** |
| 만충 뒤 넘친 몫은 | ⚠️ **조용히 사라진다** — `Load` 가 안 받은 몫을 호출자가 안 본다. 드론에는 창고가 없다 |

📌 지침 §7 ［09-03］의 병(주석과 호출자가 갈린 자리)은 **여기서는 안 났다.**

⚠️ **다만 실측에서는 0.50 이 아니라 0.37 이 나왔다**(5-3). 유입은 반반인데 적재가
누적형 25 · 광역형 15 로 앉는다 — **만충 근처에서 먼저 부르는 쪽이 이기기 때문**이고,
넘친 몫이 버려지므로 그 쏠림이 고정된다. 시험이 지키는 것은 「슬롯이 남는 동안의 비율」이다.

---

# 4. ③ 이행 — 둘째 드론 줄 (노드를 180도 돌려서 · 포트 (2,10) 로)

## 4-1. 두 번 깔았다 — 왜 회전이 답이었나

| 판 | 어떻게 | 왜 접었나 |
|---|---|---|
| ㉠ 분류기 없이 | — | **자리가 없었다.** 새 줄은 코어 에너지를 먹는 입이 **둘**(부품 줄 · 발전재료 줄)인데, 갈래가 한 자리에서만 갈리면 두 통로가 좁은 골목에서 겹친다 |
| ㉡ 분류기 둘 + 병합기 | 몸통 아래·다리에 깔고 팔L 에서 기존 줄에 **합류** | 🗑️ 폐기 — 사용자가 **비워 둔 포트 (2,10) 을 쓰는 쪽**으로 닫았다 |
| ✅ 지금 | **노드 넷을 180도 돌려** 코어(동) → 포트(서) **한 방향**으로 흘린다 | — |

⚠️⚠️ **막히던 까닭은 하나였다 — 노드는 서면으로 먹고 동면으로 낸다.** 그래서 산물이 늘
동쪽으로 흐르는데, 둘째 줄의 도착지(**B 포트 (2,10)**)는 판 **서쪽 끝**이다. 안 돌리면
줄이 서쪽으로 나갔다가 **동쪽으로 되돌아와야** 하고, 그 되돌아오는 통로가 공급 통로와
겹쳐 자리가 안 났다.

✅ **돌리면 동으로 먹고 서로 낸다**(09-15 회전 코드 · `NodeInstance.Rotation`).
줄이 한 방향으로 흐르자 **분류기도 하나면 됐다** — 코어 서면 한 대가 셋으로 갈린다
(기존 추진제 줄 · 부품 줄 · 발전재료 줄). 🗑️ 구 「분류기 둘 + 병합기 하나」 폐기.

📌 **시작 보드에서 회전을 쓰는 첫 자리다.** A 쪽 `StartingBoard.Slot` 에는 09-15 부터
회전 칸이 있었는데 **B 쪽에는 없어서** 이번에 넣었다.

## 4-2. 깐 모양

| 자리 | 무엇 | 회전 |
|---|---|---|
| **(4,8)** | **분류기** — 남 = 기존 추진제 줄 **그대로** · 서 = 부품 줄 · 북 = 발전재료 줄 | — |
| (3,8) · (2,8) | 변환기(부품) → 기초변환기(**드론 몸체**) | **180°** |
| (4,9) 벨트 · (3,9) · (2,9) | 변환기(발전재료) → 변환기(**배터리**) | **180°** |
| (1,8) · (0,8) · (0,9) · (0,10) | 벨트 — 몸체가 어깨R 로 올라가 복합 **서면**으로 | — |
| (1,9) | 벨트 — 배터리가 한 칸 올라가 복합 **남면**으로 | — |
| **(1,10)** | **복합 변환기 — 광역형 드론** | **안 돌린다** |
| (2,10) | 벨트 → **B 포트 (2,10) 동면** | — |

📌 **복합만 안 돌린다** — 서면(몸체)·남면(배터리)으로 받아 **동면**으로 내야 포트가 있는
동쪽으로 나간다. 돌리면 입력면이 동·북이 되어 포트 자리와 부딪힌다.

⚠️ **회전은 가정 표기다.** 「시작 보드가 회전을 써도 되는가」는 문서에 절이 없다 —
09-15 에 회전 코드가 선 까닭(「산물이 늘 동쪽으로 흘러 S3 프리셋이 못 섰다」)과 같은
자리라 그대로 썼고, 설계가 아니라고 하면 되돌린다.

## 4-3. 세대 표식과 옛 저장

시작 보드를 고쳤으므로 **세대 표식이 새로 났다 — `3fc396fc`**(노드 자리 다섯이 새로 섰다).
표식이 다르면 **옛 B 저장은 안 올라오고 버려진다**(그 까닭이 「세대가 다르다」로 찍힌다).
⚠️ **이것은 손실이다** — 09-17 에 B 판을 만져 둔 저장이 있으면 사라진다. 판이 바뀐 날의
정상 동작이고, 시험이 그 자리를 지킨다.

## 4-4. 기존 줄은 안 흔들렸다 — 재실측

| 잰 것 | 값 |
|---|---|
| A 보드 S1 세 판 | **09-17 과 같다**(Win 100.2초 · HP 582 · 기준선 142 · 자동 조종 끈 판 LoseTimeout 120.0) |
| B 가 나가 있는 동안 회피 스택 최고 | **8**(상한) — 추진제 줄이 돈다 |
| B 가 나가 있는 동안 보호막 최고 | **200**(최대치) — 보호막 줄이 돈다 |
| B 보드 전력 | 공급 30 · 수요 **27**(구 19) — 넘지 않는다 |
| B 포트 둘 도착 | 누적형 **96 기**(포트 (9,10)) · 광역형 **80 기**(포트 (2,10)) |

📌 분류기가 갈래를 셋으로 나눠도 **코어 산출이 10/초**라 어느 줄도 굶지 않는다.

## 4-5. ⚠️ 되돌리다 기존 줄을 한 번 끊었다

병합기로 바꿨던 **(10,8) 벨트를 복구하지 않아** 기존 드론 줄이 통째로 끊겼다.
하네스가 그 자리에서 잡았다 — **「광역 몫 1.00 · 누적형 0 · 이어진 노드 18 에서 17」**.

📌 **수가 예쁘게 나와서 더 위험했다** — 광역 몫 1.00 은 「새 줄이 잘 돈다」로도 읽힌다.
그것을 결함으로 읽게 한 것은 **이어진 노드 수가 하나 줄었다**는 딴 줄이었다.
「한 줄만 보면 좋은 소식과 나쁜 소식이 같은 모양이다.」

---

# 5. 두 로봇 하네스 — 첫 실측

S1 · 튜토리얼 종료 · 자동 조종 켬 · **태그 오토 켬**(⚠️ 가정 — 게임 기본값은 꺼짐이다).
**Win 100.4초 · HP 1000/1000 · 교대 7 회 · 태그 스킬 4 회**
(포트 (2,10) 재배치 + 광역형 절반까지 다 들어간 판 · `196328d`).

## 5-1. 목적 1 — 피해 나눔의 대가

| 때 | 표적 수 | 총 피해 | 한 체 몫 |
|---|---|---|---|
| 28.4초 | 36 | **3,000** | 83 |
| 53.6초 | 26 | **3,000** | 115 |
| 76.5초 | 27 | **3,000** | 111 |
| 100.4초 | **1** | **3,000** | **3,000** |

**총은 고정이고 한 체 몫이 표적 수로 갈린다** — 09-18 오전에 넣은 나눔이 실제 판에서
이렇게 걸린다. S1 하네스로는 한 번도 못 보던 수다.

📌 **총 3,000 은 설계가 2-1 에서 내다본 수 그대로다** — 적재가 **누적형 20 · 광역형 20**
으로 앉아 (20 × 100 + 20 × 50) ÷ 40 × 40 = 3,000 이다.
🗑️ 재배치 전 판의 3,250 폐기 — 그때는 적재가 25 · 15 로 기울어 있었다. 포트를 따로 쓰자
**두 줄이 제 포트로 들어와 반반이 됐다.**

📌 **마지막 줄(표적 1 · 한 체 3,000)이 나눔의 뜻을 보여 준다** — 표적이 하나면 나눌 것이
없으므로 **전량이 한 체에** 들어간다. 「광역이라 약하다」가 아니라 **몫을 나눈다**는 규칙이다.

## 5-2. ⚠️⚠️ 그 수를 재다가 결함이 나왔다

둘째 줄을 깐 첫 판에서 총 피해가 **2,500** 으로 떨어졌다. 까닭은 적재 구성이 아니라
**계산이 광역형을 안 세는 것**이었다 — (누적형 25 × 100) ÷ 40 × 40 = 2,500 으로 딱 맞는다.

🗑️ 구 코드 폐기 — `AverageDamagePerItem` 이 `MountItem.Drone` 만 셌다.
**광역형은 소진되면서 피해에는 안 들어갔다.** 고친 뒤 4,000 으로 돌아왔고,
그 뒤 광역형 절반(6-3 답)을 넣고 포트를 갈라 적재가 반반이 되며 **3,000** 이 됐다.

📌 **드론이 2종이 된 것은 09-16 이다.** 그날부터 이 자리가 비어 있었고, **재는 판이
없어서** 안 보였다. 두 로봇 판을 만든 첫날에 잡혔다.

## 5-3. 목적 2·3 — 스택 40 · 혼재

| 잰 것 | 값 |
|---|---|
| B 마운트 첫 만충 | **28.4초**(구 44.0초 → 31.6초 → 28.4초 · 둘째 줄이 들어오며 빨라졌다) |
| 평균 적재 | 누적형 10.5 · 광역형 9.3 — **광역 비율 0.47** |
| 보드 산출 몫 | 광역 **0.50** |

⚠️ 만충 시각은 **태그 스킬이 나간 자리**로 잰다 — 만충은 틱 안에서 서고 그 자리에서
전량 소진되므로 **틱 경계에서 보면 만충이던 순간이 통째로 안 보인다**(첫 판이
「만충이 안 섰다」로 잘못 적혔다가 잡은 자리).

📌 **0.50 과 0.47 의 차이가 3장의 쏠림**이다 — 만충 근처에서 먼저 부르는 쪽이 이긴다.
🗑️ 재배치 전의 0.37 폐기 — 그때는 두 줄이 **한 포트로 합류**해 한 줄이 앞을 막았다.
포트를 갈라 주자 쏠림이 0.03 으로 줄었다.

## 5-4. 목적 4 — 광역형 한 기의 평균 표적 수 · ⚠️ 재는 자리가 틀렸다

첫 판은 **평균 1.00 마리**였다. 그 수를 의심해 반경 스위프를 붙였더니
**r = 5 칸에서도 0.00 마리**가 나왔다 — 반경을 키워도 안 늘어나는 것은 **자리가 틀렸다는 뜻**이다.

🗑️ **구 코드 폐기** — 곁에 닿는 적을 **드론 자리**에서 재고 있었다. 드론은 로봇 둘레를
돌고 사거리가 9.2 라 **표적에서 멀리 떨어진 채 쏜다.** 반경이 사거리와 같던 동안에는
그래도 곁의 적이 들어왔지만, 반경이 2칸이 되자 **하나도 안 들어왔다.**
📌 「광역」은 **떨어진 자리**에서 퍼지는 것이다 — **쏜 자리**가 아니다. 지금은 표적 둘레에서 잰다.

고친 뒤 실측 — **타격 20 회 · 닿은 표적 합 26 · 평균 1.30 마리.**

## 5-5. 반경 스위프와 적 간격 — 설계가 청한 측정

| 잰 것 | 값 |
|---|---|
| 붙은 적끼리의 간격 | 표본 252 · **평균 0.80 유닛** · 최소 0.67 유닛 |
| r = 2 칸이면 | 평균 **1.20 마리** |
| r = 3 칸이면 | 평균 **1.30 마리** |
| r = 4 칸이면 | 평균 **1.30 마리** |
| r = 5 칸이면 | 평균 **1.45 마리** |

**측정법** — 간격은 「제 공격 사거리 안에 붙은 적」마다 **가장 가까운 이웃까지의 거리**를
0.5초마다 잰 것이다. 스위프는 광역 타격 순간 **표적 둘레 r 안의 살아 있는 적 수**이고,
**값은 안 옮겼다** — 판정은 자산의 반경 하나를 그대로 쓴다.

⚠️⚠️ **본전 4마리는 반경으로 못 만든다 — 적어도 S1 에서는.** 적끼리는 0.80 유닛으로
붙어 서는데도 r 을 2 → 5 로 키워 봐야 1.20 → 1.45 이다. 까닭은 간격이 아니라 **수**다:
S1 은 적이 띄엄띄엄 들어와 **표적 둘레에 애초에 여럿이 없다.**
📌 그러니 반경을 키우는 것으로 4마리를 맞추면 **S1 에서는 여전히 1~2 마리**이고
뒤 스테이지에서만 갑자기 커진다. **본전 수를 스테이지마다 다르게 볼 것인지**가
설계 판단 자리다(6-1).

## 5-6. 6-3 답 이행 — 광역형은 표적 하나당 절반

`params.droneAoeDamageFactor = 0.5` · **가정 표기**(`confirmed:false`).

⚠️ **갈리는 자리를 구현이 닫았다** — 「표적당 절반」이 ① **타격당 피해만 절반**인지
② **충전량 자체가 절반**인지가 문서에 없다. ①로 뒀다: W01 3-1 의 본전 계산이
**한 대 때릴 때의 수**(99 대 49)를 맞대고 있어서다. ②로 두면 수명이 반이 되어
그 표가 다른 것을 재게 된다. 자산 `basis` 에 그대로 적었다.

시험 셋 — 첫 한 대의 피해가 **절반인가** · 비가 0 이면 **구 거동**인가 · 값이 **자산에서 오는가**.
⚠️ 첫 시험은 판 전체의 합으로 재면 안 된다(두 종은 모는 법이 달라 타격 횟수가 섞인다) —
**첫 한 대**만 잰다.

---

# 6. 설계에 청하는 것 — 둘은 답이 와서 이행했고, 청하는 것은 셋이다

**6-1. 광역 판정 반경을 다시 준다 — 실측을 붙여 보냈다** (5-5)
적 간격 **평균 0.80 유닛**, 반경 스위프 **1.20 → 1.45 마리**(r 2 → 5).
**반경만으로는 본전 4마리가 안 된다** — S1 은 표적 둘레에 애초에 여럿이 없다.
그래서 청하는 것이 둘이다: ① 반경을 얼마로 둘 것인가 ② **본전 수를 스테이지마다
다르게 볼 것인가**(뒤 스테이지에서 적이 촘촘해지면 같은 반경이 다른 수가 된다).

**6-2. 접수 — 포트 (2,10) 로 다시 깔았다** (사용자 확정 · 4장)
병합기 합류는 **폐기**했다. 노드를 180도 돌려 줄을 한 방향으로 흘렸고, 그 덕에
분류기도 하나로 줄었다. ⚠️ **회전을 시작 보드에서 쓰는 것 자체가 가정 표기**다 —
문서에 절이 없어 09-15 회전 코드의 까닭을 그대로 따랐다(4-2).

**6-3. 「표적당 절반」의 뜻을 확인해 달라** (5-6 · 답은 받아 이행했다)
타격당 피해만 절반으로 뒀고 **충전량·수명은 그대로**다. 문서에 그 갈림이 없어 구현이
가정으로 닫은 자리이므로, 뜻이 다르면 자산 값 하나만 고치면 된다.

---

# 7. WBS

| 문서 | 처리 |
|---|---|
| `260918_W01` | **완료** — 이 장이 답한다 |
| `260918_V02` (이 장) | 진행중 |

⚠️ WBS 상태 변경은 **설계 권한**이다 — 여기 적은 것은 요청이 아니라 **대조용 기록**이다.

---

# 8. 게시 뒤 재열람

⚠️ **글자만 보면 안 된다** — 09-18 오전 `260918_V01` 에서 **깨진 글자는 0 이었는데
괄호 조각 넷이 통째로 빠졌다**(커밋 id · 시험 이름 · 문장 하나 · 줄 하나).
전부 `**굵은 글씨**(괄호)` 나 백틱 바로 뒤에 괄호가 붙은 자리였고, **서식 해석에서 나는 것**
이라 통째로 다시 보내도 같은 자리가 또 빠진다.

📌 그래서 재열람에서 보는 것은 셋이다 — **글자 · 서식 · 빠진 조각**.
빠진 자리는 앵커를 바꿔(괄호를 없애) 채우고, 두 번째 재열람으로 확인한다.

---

문서 마침 — `260918_V02` (2026-09-18 · 둘째 드론 줄 · 광역형 절반 · 두 로봇 하네스 · 결함 둘)
