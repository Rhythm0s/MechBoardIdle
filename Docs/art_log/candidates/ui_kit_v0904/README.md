# `ui_kit_main` 09-04 초안 — `Assets/_Project/Art/UI/` 에서 옮겨 옴 (2026-09-11)

**왜 옮겼나.** 2026-09-11 확정으로 **버튼·패널을 그림 자산으로 새로 만든다**(UI 아트 문서 1-1 뒤집기 ·
문안은 `260911_W01` 신설 절). 이 초안은 그 자산이 아니고 **승인된 적도 없다** — `Assets/` 안에 두면
Unity 가 계속 임포트하고, 새 자산이 오면 같은 자리를 두 그림이 노린다.

**옮긴 것 둘 · `.meta` 둘은 `git rm`.** `.meta` 는 `Assets/` 안에서만 뜻이 있다.

| 파일 | 크기 | md5 |
|---|---|---|
| `ui_kit_main.png` | 31,065 바이트 · 512 × 512 · 실루엣 353 × 417 | `3377a5dd` |
| `ui_kit_main.zip` | 31,065 바이트 | `3377a5dd` |

⚠️ **`.zip` 은 압축 파일이 아니다 — PNG 와 바이트까지 같은 사본이다.**
첫 8바이트가 `89504E470D0A1A0A`(PNG 서명)이고 열어 보면 512 × 512 RGBA 다.
**확장자만 `.zip` 인 같은 그림**이며, md5 가 PNG 와 동일하다. 그래서 Unity 도 이것을 텍스처로 임포트하고 있었다.

**코드 참조는 둘 다 0건이다** (2026-09-11 · `Assets/_Project/Scripts/` 이름 검색).
