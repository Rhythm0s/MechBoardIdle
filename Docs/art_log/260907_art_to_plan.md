# 구현-아트 → 플랜 세션 보고 (2026-09-07)

> 플랜 §20-4 가 「플랜 세션의 검토 대상은 V01 초안 + `Docs/art_log/`」로 정했다. 이 문서는 그중
> **플랜이 판단해야 하는 것만** 추린 것이다. 작업 기록 전문은 `260907_anim.md` 에 있다.
>
> 이 세션은 Notion 과 `Docs/HANDOFF.md` 를 쓰지 않는다 (플랜 §20-1 · HANDOFF §1-1).

---

## 0. 먼저 알아야 할 것 — 플랜 §20 의 전제 하나가 어긋나 있었다

**§20-4 는 「아트 세션을 새로 열어 18-2 ① 부터 시작한다」를 전제한다.** 그런데 이 세션은
§20 이 정해지기 전에 이미 §18 을 통째로 돌린 세션이고, 착수 시점에 **합체 256 스틸(사용자 승인)과
애니메이션 27벌이 이미 리포에 커밋돼 있었다.**

그래서 **다시 뽑지 않았다.** 다시 뽑으면 약 214 생성을 쓰고, 도구가 매번 다르게 내므로(15 규칙 6)
**사용자가 승인한 스틸과 커밋된 27벌이 다른 그림으로 바뀐다.** 승인을 무효로 만드는 쪽이라 멈추고
빠진 것만 만들었다 — `Docs/art_log/` 와 `_anim_sheet.png`.

⚠️ **이 세션은 §20-1 의 소유 경로 밖을 이미 커밋한 이력이 있다** (§20 이전).
`Scripts/**` · `HANDOFF.md` · `art_manifest.md` · `measure/**` · `art_prompts_260907.md` · `Units/boss.png`.
커밋 `df255b8` ~ `a612ca5` 에 들어가 있고, §20-2 3번대로 이후로는 손대지 않았다.

---

## 1. 사용자가 확정한 것 넷

GIF 로 움직여 본 뒤 나왔다. **정지 시트로는 넷 중 셋이 안 잡혔다** (5장 참조).

| # | 확정 | 근거 |
|---|---|---|
| 1 | **합체 256 전투 스틸 2차 승인** — job `47cdb935` · md5 `b3ef28a8` | 그전까지 이 자리는 512 승인본의 NEAREST 축소본이었다. 256 전투 스틸은 생성된 적이 없었다 |
| 2 | **`robot_b` 대기는 동면 하나만 생성하고 좌우 대칭으로 쓴다** — 서면 폴더 삭제 | 동면·서면 둘 다 반짝임·어깨가 어색했다 |
| 3 | **대기는 되감기(핑퐁) 재생** — 0·1·2·3·4·3·2·1 | 마지막에서 첫 프레임으로 튀는 자리가 어색했다 |
| 4 | **`robot_a_Idle` 통과** | — |

---

## 2. 문서와 어긋난 자리 — **설계 판정이 필요하다**

구현이 문서를 고치지 않았다 (지침 §10 — 값·규칙의 신설·폐기는 판정 요청).

### 2-1. 15-3 5-2 의 「김」 — 사용자가 빼라고 했다

**연기는 도구가 지어낸 것이 아니다.** 1차 동작 묘사에 `steam venting from the exhaust ports` 가
들어 있었고, 그것은 **15-3 5-2 가 「배기구에서 김이 샌다」로 규정한 것을 그대로 옮긴 결과**다.
도구는 시킨 대로 그렸다. 2차에서 그 절을 빼고 「도장은 무광이고 기체 주변 공기는 완전히 비어 있다」로 바꿨다.

**판정할 것: 15-3 5-2 에서 김을 뺄 것인가.** 지금은 문서와 자산이 어긋난 채다.

### 2-2. 로봇 B 대기가 3방향 + 미러 — 4방향 규격을 건드린다

15 §4 와 15-2 는 대기·이동을 **4방향**으로 정해 두었다. 사용자 확정으로 로봇 B 대기가
**3방향 + 미러 하나**가 됐다.

**미러는 로봇 B 에만 쓸 수 있다.** 15-2 가 로봇 B 를 「어깨는 평평하고 비어 있으며 무기는 등의
수직 발사관」으로 좌우 대칭으로 규정한다. **로봇 A 는 뒤집으면 안 된다** — 15-1 3-2 가 「마운트가
붙은 쪽 팔이 명확히 두껍다」로 비대칭을 규정해서, 뒤집으면 무기가 반대팔로 간다.
합체는 15-3 3-3 이 이미 대칭·3방향으로 정해 두었다.

**판정할 것: 15 §4 · 15-2 의 방향 수 규격을 「대칭인 기체는 3방향 + 미러」로 고칠 것인가.**

### 2-3. 대기 진폭 — 규격 4~6% 에 못 미친다 (`260907_V01` ❓2-1 · 미결)

도구(`MBI.Editor.AnimReport`)가 낸 값이다. **잴 수 있는 다섯 벌이 1.3~3.5%.**
나머지는 실루엣이 캔버스 가장자리에 닿아 bbox 윗변이 못 움직여 **잴 수가 없다.**

뿌리는 승인본이다 — 로봇 A 승인본의 여백이 **위 7 · 아래 0**이라 4~6%(10~15픽셀)가 캔버스에
들어갈 자리가 없다. 세 갈래를 V01 로 올려 두었고 **(가) 코드가 위치를 흔든다**를 권했다
(15-1 9장 A-2 가 발사 반동에 이미 같은 처리를 확정해 두었다).

⚠️ **이 판정은 아직 열려 있고, 2-2 의 되감기 확정이 여기에 영향을 줄 수 있다** — 되감기로
왕복하면 같은 진폭이라도 화면에서 다르게 읽힌다. **되감기가 들어간 뒤에 다시 보는 편이 낫다.**

### 2-4. 프레임 재생 속도 — 어느 문서도 정한 적이 없다 (`260907_V01` ❓2-2 · 미결)

지금 값은 **구현 가정**이다 — 대기 6fps · 이동·사망·태그 8fps. GIF 도 이 속도로 만들었으므로
**사용자가 본 속도가 확정 속도가 아니다.** 값이 정해지면 GIF 를 다시 만든다.

---

## 3. 구현-문서·코드 세션에 넘긴 것 둘

둘 다 `Assets/_Project/Scripts/**` 라 이 세션 소유가 아니다. 손대지 않았고 로그 3-2 에 적었다.

| # | 넘긴 것 |
|---|---|
| 1 | **`SpriteFrameAnimator` 에 되감기 재생을 넣어야 한다.** 지금은 마지막 다음에 0 으로 돌아간다. 대기는 0·1·2·3·4·3·2·1 로 돌아야 한다. **프레임 파일은 그대로 두고 순서만 바꾼다** |
| 2 | **`UnitAnimWiringTests` 가 로봇 B 대기를 4방향으로 기대한다**(`fourWay`). 서면이 없어져 `AllClipFolders_Exist` 가 건너뛴다. **26벌 + 미러 하나**로 고쳐야 한다 |

**되감기를 프레임으로 구우면 안 된다.** 구우면 대기가 5장에서 8장이 되어 15「동작의 크기」의
「대기 5」 규격과 배선 테스트가 깨진다. 리포의 프레임은 5장 그대로다.

---

## 4. 지금 상태

| | |
|---|---|
| 벌 | **26벌 · 프레임 161장** · 전부 256×256 · 규격 위반 0 |
| 구성 | 로봇 A 10 (대기 4 · 이동 4 · 사망 1 · 태그 1) · 로봇 B 9 (대기 3 · 이동 4 · 사망 1 · 태그 1) · 합체 7 |
| 커밋 | `8f3a977` · `ec9c03f` · `8eb2461` · `24c1061` (+ `a1b51ab`) · **`main == origin/main`** |
| 비용 | 스틸 40 + 회전 24 + 애니 1차 150 + 2차 40 = **254 생성** |
| 보는 도구 | `_anim_sheet.png`(정지) · `Docs/art_log/preview/*.gif`(움직임) · 생성기 넷을 리포에 뒀다 |

### 4-1. 진행 중 하나

**`robot_a_Move` 3차.** 다리가 좌우 교차하지 않고 같이 움직인다는 판정을 두 번 받았다.
글로 시켜서 두 번 실패했으므로 **`template_animation_id="walking-6-frames"`** 로 바꿨다 —
도구가 가진 걷기 골격이라 다리 교차가 묘사가 아니라 구조로 강제되고, 프레임 수도 6 으로 규격과
맞으며 비용은 방향당 1 생성이다. **덮지 않고 임시로 꺼내 2차와 나란히 놓고 사용자가 고른다** —
골격이 탑뷰 기체에 안 맞을 수 있어 미리 단정하지 않는다.

---

## 5. 플랜이 고칠 자리 — 이번에 실측으로 드러난 것 다섯

### 5-1. §20-2 1번(「경로 명시 `git add`」)만으로는 부족하다

**실제로 부딪혔다.** 두 세션이 체크아웃 하나를 쓰는데 **git 인덱스도 하나**라, 이 세션이
`git add` 해 둔 GIF 28개가 문서·코드 세션의 커밋 `0be3755` 에 딸려 들어갔다.
잃은 것은 없었다(내용 온전 · 서로의 파일을 고치지 않았다).

**실제 처방은 `git commit -- <경로>` 다** — 인덱스를 우회해 그 경로의 워킹트리 내용만 담는다.
§20-2 1번을 이렇게 고치는 것이 맞다.

⚠️ **다만 그 우회에도 대가가 있고, 그것에도 한 번 걸렸다.** `git commit -- <경로>` 는
**미추적 파일을 담지 않는다.** 로봇 B 서면이 빠지며 GIF 번호가 밀려 11번부터 이름이 전부 바뀌었는데,
옛 이름은 삭제로 들어가고 **새 이름 16개가 커밋 밖에 남았다**(`8eb2461` 로 수습).
**새 파일은 `git add` 를 따로 해야 한다** — 이 한 줄이 규칙에 함께 들어가야 한다.

### 5-2. §20-1 의 「사용자 판정: 진폭 육안(시트)」 — 시트로는 판정이 안 된다

**정지 시트로는 256 캔버스 안에서 10픽셀쯤 오르내리는 것을 눈이 못 따라간다.**
사용자가 「gif로 움직여봐야 확실하게 알 수 있겠다」고 했고 그것이 맞았다.
**GIF 를 보고 나서야 판정 넷이 나왔고, 그중 셋은 시트에서 아무도 못 봤던 것이다** —
이동의 다리, 합체의 연기, 로봇 B 의 어깨.

**§20-1 의 판정 수단을 「시트」가 아니라 「GIF」로 고치는 것이 맞다.** 시트는 한눈에 훑는 용도로 남긴다.

### 5-3. §20-3 의 「재생성 상한 2회」가 어느 루프에 걸리는지 불분명하다

원문은 **진폭이 규격 밖일 때의 루프**에 상한 2회를 두었다. 그런데 이번에 상한을 쓴 것은
**사용자 판정 루프**였고 `robot_a_Move` 가 **3차**로 갔다. 두 루프가 상한을 공유하는지,
사용자 판정에는 상한이 없는지가 안 적혀 있다.

### 5-4. §20-4 의 출발선이 지켜지지 않았다

「문서·코드 세션이 HANDOFF §1 에 소유 표를 적는 것이 아트 세션의 출발선」인데, 아트 세션이
열릴 때 **§1 에 표가 없었다.** 플랜 §20-1 에서 직접 읽어 진행했고, 나중에 들어온 §1-1
(커밋 `036318b`)과 대조하니 아트 쪽 경로 셋이 같았다 — 결과는 맞았으나 출발선은 안 지켜졌다.

**출발선이 없을 때 무엇을 소스로 삼는지**를 §20-4 에 한 줄로 두는 편이 낫다.

### 5-5. 구현의 실수 하나 — 「완료」가 거짓이었다

생성이 끝나기를 기다리는 폴링 루프에 **실제 지연을 넣지 않았다.** 80회를 2분 만에 태우고
루프가 정상 종료해 **「완료」 알림이 왔으나 실제로는 아직 생성 중이었다.**
배치모드 `exit 0` 이 통과가 아니라는 지침 §7 조항과 같은 종류다 —
**루프의 정상 종료는 조건 충족이 아니다.** 20초 간격으로 다시 걸었다.

---

## 6. 이 세션이 쓴 경로 (플랜 §20-1 소유 표 안)

```
Assets/_Project/Art/Anim/**
Assets/_Project/Art/Units/robot_fusion_256.png   (§20 이전에 이미 승인·커밋)
Docs/art_log/**
```

`Docs/art_log/` 에 둔 것 — 기록 둘과 생성기 넷.

| 파일 | 무엇 |
|---|---|
| `260907_anim.md` | 작업 기록 전문 — 벌별 표 · 프롬프트 전문 · 물러난 묘사 |
| `260907_art_to_plan.md` | 이 문서 |
| `make_art_log.py` | 위 기록을 만든다. md5·프레임 수·수신 시각을 실물에서 찍는다 |
| `make_anim_sheet.py` | 정지 시트 |
| `make_anim_gif.py` | GIF. 대기는 되감기로 낸다 |
| `fetch_anim_frames.py` | 재생성분을 받아 제자리에 넣는다. **넣기 전에 대상 폴더를 비운다** |

**생성기를 리포에 둔 이유는 하나다** — 임시 스크립트는 세션이 끝나면 사라지고, 그러면 다음 사람이
같은 것을 다시 만들 수 없다. 지침 §7 「승인은 파일이 놓인 자리를 바꾸지 않는다」와 같은 뿌리다.
