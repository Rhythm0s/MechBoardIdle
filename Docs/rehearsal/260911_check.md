# 260911 점심 전 확인 시트 (사용자용)

- 만든 것: 문서·코드 세션 · 2026-09-11 · 플랜 §71-14
- **결과 칸만 채우면 된다.** 채운 시트를 그대로 `260911_V02` 「화면 확인」 절로 옮긴다.
- 개발 빌드: `Build/WebGL` · 로컬 `http://127.0.0.1:8000/`
- 담긴 커밋: **`e086c2a`** 까지 — ⚠️ **육안 여덟 수정이 전부 들어간 판**이다(아래 9번).

> ⚠️ **스크린샷은 못 찍었다.** 개발 빌드에서 **자동화 클릭이 Unity 에 안 닿아**
> 「게임 시작」을 못 눌렀다(네이티브 클릭 · hover 뒤 클릭 · JS 포인터 셋 다 실패 ·
> `260911_V01` 8장 5). 그래서 **기대값을 좌표·크기 숫자로** 적어 둔다 — 아래 표의
> 숫자와 화면을 대조해 주면 된다.

> ⚠️ **숫자는 기준 캔버스 1440×2560 값이다.** 창이 그보다 작으면 **세로 비율로 줄어든다**
> (배율 = 창 세로 ÷ 2560). 예컨대 창 세로가 1280 이면 모든 숫자가 **절반**이다.
> **자리와 비율**이 맞는지를 보면 되고, 픽셀 자로 재실 필요는 없다.

---

## 1. 육안 순서 — 전투·조립 화면

### 1-0. 메뉴 (게임 시작 전)

| 볼 것 | 기대 | 보이는가 | 눌리는가 | 겹치는가 |
|---|---|---|---|---|
| 껍데기 톤 | 반투명 검정 판 · **미색 4px 각진 테두리** · 둥근 모서리 **없음** | | | |
| 「게임 시작」 | 눌린다 | | | |
| 「볼륨」 | 눌린다 · 슬라이더가 열린다 | | | |
| 「포트폴리오 문서 보기 **(준비 중)**」 | **안 눌린다**(어둡게) — URL 이 비어 있다 | | | |
| 「관련 노션 보기 **(준비 중)**」 | **안 눌린다**(어둡게) | | | |

### 1-1. 전투 화면 — **레이어 1 · 절대 좌표**

⚠️ **x·y 는 가운데 좌표다**(왼윗모서리가 아니다).

| # | 요소 | 문서 좌표·크기 (1440×2560) | 차지하는 사각 | 보이는가 | 눌리는가 | 겹치는가 |
|---|---|---|---|---|---|---|
| a | 상단 정보줄 | 높이 **120** · 폭 전체 | y **0 ~ 120** | | — | |
| b | 상태창 (HP · 회피 눈금) | 왼윗모서리 **(40, 160)** ⚠️ 크기는 문서에 없다 | x 40~ · y 160~ | | — | |
| c | 방치 보상 | **우상단** ⚠️ 좌표가 문서에 없다 — 자리만 본다 | 오른쪽 위 | | | |
| d | **태그 원형** | 가운데 **(1280, 2080)** · 지름 **200** | x 1180~1380 · y **1980~2180** | | | |
| e | **합체 원형** | 가운데 **(1280, 2300)** · 지름 **200** | x 1180~1380 · y **2200~2400** | | | |
| f | **조립 진입 막대** | 가운데 **(720, 2460)** · **600×160** | x 420~1020 · y **2380~2540** | | | |

**d·e 는 같은 x1280 에 세로로 쌓인다** — 가운데 사이가 220 이고 지름이 200 이라 **20 이 뜬다.**
붙어 보이거나 겹쳐 보이면 반려다.

**f 는 화면 한가운데(x720)** 이고 **d·e 와 x 가 다르다** — 막대가 원형 아래 정중앙에 오면 맞다.

| 볼 것 | 기대 | 결과 |
|---|---|---|
| ✅ **하단 베이지 띠가 없는가** | 레이어 2 바탕(변수 패널)이 **안 그려져야** 한다 — 전투에는 위 여섯만 | |
| ✅ **HP 바가 오른쪽에서 왼쪽으로** 준다 | 가운데로 모이면 반려 | |
| ✅ **대각으로 걸을 때 그림이 안 깜빡인다** | 동면·북면이 번갈아 뜨면 반려 | |
| 심사자용 바로가기 패널 | **오른쪽 아래** · 접힘/펼침 · 스테이지 줄 · 「현재 적 전멸」 · 「처음부터 (저장 초기화)」 · **「보드 좌표 덤프」** | |

### 1-2. 조립 화면 — **레이어 2 · 띠 다섯**

「▼ 조립 (물류 보드)」를 누른 뒤. **띠 다섯을 더하면 정확히 2560 이다.**

✅ **액션바 128 → 150 · 보드 1352 → 1330**(설계 확정 (가)). 「적용 128 < 버튼 최소 150」
판정 요청이 이것으로 닫혔다 — 띠를 버튼 최소에 맞추고 보드에서 22 를 덜었다.

| # | 띠 | 높이 | y 범위 | 든 것 | 보이는가 | 겹치는가 |
|---|---|---|---|---|---|---|
| g | **인셋 전투** | 768 | **0 ~ 768** | 전투가 **계속 돌고 있다**(멈추면 반려) | | |
| — | (경고 띠) | 96 | 768 ~ 864 | 「생산이 멈췄습니다」 — **경고가 있을 때만** 뜬다 | | |
| h | **보드** | **1330** | **768 ~ 2098** | 격자 · 노드 · 벨트 · **파츠 색 바닥** | | |
| i | **부유 띠** | 200 | **2098 ~ 2298** | 미니맵 좌 · 팔레트 가운데 · 모드 우 | | |
| j | **변수 패널** | 112 | **2298 ~ 2410** | 세 칸(막힌 것 / 전력 / 벨트·배율·일감률) ⚠️ **조립 화면에만** | | |
| k | **액션바** | **150** | **2410 ~ 2560** | 「▲ 전투로」 좌 · 「적용」 우 | | |

**부유 띠(i) 안쪽 자리**

| 요소 | x 범위 | y 범위 | 크기 | 결과 |
|---|---|---|---|---|
| **미니맵** (왼쪽) | **24 ~ 200** | 2110 ~ 2286 | 176 정사각 안에 보드 가로세로비로 | |
| **노드 팔레트** (가운데) | **216 ~ 1224** | 2098 ~ 2298 | 버튼 **150 정사각** · 가로 줄 · **넘치면 좌우 스크롤** | |
| **모드 버튼** (오른쪽) | **1240 ~ 1416** | 2110 ~ 2286 | 176 정사각 · 「이동 모드」/「조립 모드」 | |

**액션바(k) 안쪽 자리**

| 요소 | x 범위 | y 범위 | 크기 | 결과 |
|---|---|---|---|---|
| 「▲ 전투로」 (왼쪽) | **24 ~ 324** | 2410 ~ 2560 | 300×150 ⚠️ **가정**(문서에 없다) | |
| 「적용」 (오른쪽) | **1096 ~ 1416** | 2410 ~ 2560 | **320×150**(문서) ⚠️ 아직 **안 그린다** — 자리만 잡혀 있다 | |

| 볼 것 | 기대 | 결과 |
|---|---|---|
| **팔레트가 보드 위에 안 떠 있는가** | 보드(h) 안에 팔레트가 겹치면 **반려** — 오늘 보드 밖으로 냈다 | |
| 배율 **+ / −** | 누르면 보드가 커지고 작아진다 · 「보드 배율 ×1.00」 글자가 **안 잘린다** | |
| 조작 안내 한 줄 | 부유 띠 **바로 위** · 「탭 = 노드 배치 · 드래그 = 벨트」 | |
| 구역 이름표 여덟 | 구역 왼윗모서리 · **글자가 안 잘린다** | |
| ✅ **「마운트」 이름표** | 마운트 포트 칸 **가운데 위** · 구역 이름표와 같은 크기·색 | |
| ✅ **파츠 색 다섯** | 바닥이 파츠마다 옅게 다른 색 · **좌우는 같은 색** · 점선도 같은 색(맞닿은 변만 미색) | |

---

## 2. 리허설 결함 ① 재현 — 물류가 마운트에 닿는가

> **결함 ①** = 「물류가 마운트에 안 닿는다」. 하네스에서는 **안 난다** — 사용자 화면에서만 났다.
> 그래서 **놓인 배치 그대로**가 필요하다.

### 2-1. 절차

| # | 할 것 | 어디 |
|---|---|---|
| 1 | 「▲ 전투로」로 전투 화면 → 심사자용 바로가기 펼치기 | 오른쪽 아래 |
| 2 | **「처음부터 (저장 초기화)」** 누르기 | 패널 아래쪽 |
| 3 | 튜토리얼이 처음부터 시작되는지 확인 | |
| 4 | 「▼ 조립」 → **고스트 칸 밖이 어두운가**(DIM) | 보드 |
| 5 | **팔레트에 「기초 군수」만 켜져 있는가**(나머지는 어둡게) | 부유 띠 |
| 5-1 | ✅ **그 앞 국면도 하나만 켜지는가** — 조립 진입 국면에서는 **「▼ 조립」 하나**, 모드 국면에서는 **모드 버튼 하나**. 나머지는 어둡고 **안 눌려야** 한다 | 전 화면 |
| 6 | 빈 칸(고스트가 가리키는 자리)에 기초 군수 놓기 | |
| 7 | ✅ **막이 걷히고 팔레트가 전부 켜지는가** — ⚠️ 종전에 **안 걷히던 결함**이다(고쳤다) | |
| 8 | 「▲ 전투로」 → **마운트 재고가 오르는가** 관찰 | 상태창 |

### 2-2. 기대값 (하네스 실측 · `260911_V01` 1-3)

| 것 | 기대 | 관찰값 |
|---|---|---|
| 첫 도착까지 | **≈ 5.5초** | |
| 마운트 **0 → 40** 채움 | **≈ 16초** (5.5 + 40÷4) | |
| 정상상태 산출 | **≈ 4.0개/초** | |

⚠️ **빈 칸을 놓기 전에도 세 줄이 흐른다**(현행 배치) — 「놓기 전 0」이 **안 선다.**
이것은 **알고 있는 것**이고 `260911_V01` 2-1 판정 요청에 올라가 있다. 여기서 볼 것은
**놓은 뒤 마운트가 40까지 차는가**다.

### 2-3. 안 오르면 — 둘 중 하나를 남겨 주면 된다

**(가) 보드 좌표 덤프 (권장 · 자동)**

1. 「▲ 전투로」로 돌아가 심사자용 바로가기 → **「보드 좌표 덤프 (클립보드로)」**
2. 누르면 **클립보드에 들어간다** — 아무 데나 붙여 넣어 주면 된다
3. 버튼 글자가 「복사됨 1」로 바뀌면 떠진 것이다

> 이 글은 **하네스가 그대로 읽는 꼴**이다 — 노드/벨트의 좌표와 **면 전부**가 들어 있어
> 사람이 옮겨 적을 필요가 없다. (옮겨 적다 한 칸 틀려 **게임과 다른 보드를 재는** 사고가
> 오늘 실제로 있었다.)

**(나) 스크린샷** — 저장 자리 `Docs/rehearsal/img/`

- **격자 좌표가 보이게** · **보드 배율 ×1.00** 으로 맞춘 뒤 찍어 주면 된다

| 남긴 것 | 결과 |
|---|---|
| (가) 덤프 붙여 넣기 / (나) 스크린샷 파일명 | |

### 2-4. ⚠️ 직전 판에서는 **재현되지 않았다**

앞선 빌드에서 사용자가 돌았을 때 **마운트 재고 40/40** 이었다 — 결함 ① 이 안 났다.
**병합기 수정(`f061d01` 코드 결함 ①②)으로 닫힌 것으로 보인다** — 종전에는 합류가
엉뚱한 쪽으로 흘러 마운트에 안 닿았다.

⚠️ **「보인다」로 적는다.** 안 나는 것은 고쳐진 증거가 아니라 **이번에 안 났다는 것**뿐이다.
월요일 2차에서 한 번 더 본다.

---

## 3. `boss_Move` 동면 GIF

| 것 | 값 |
|---|---|
| **동면 GIF** | `Docs/art_log/preview/_boss_Move_v4_east.gif` |
| **동↔서 미러 GIF** | `Docs/art_log/preview/_boss_Move_v4_east_west.gif` — 서면이 동면을 뒤집어 쓰는 모습 |
| 설치 | **설치됨** — `529082c` · `451464e`(`east.meta` 포함) · 6칸 · 첫 프레임 `94dc959c` |
| 벌 수 | **보스 5벌** (대기 남 · 사망 남 · 이동 **남·북·동**) — 생성기 재실행 완료 `73f2069` |
| 값 | wrap 화소 차 **0.7%** · wrap 겹침 **0.993** · **대칭도 0.9965**(아트 실측) |

⚠️ **서면이 바뀌었다.** 지금까지 보스 서면은 동면이 없어 **남면으로 떨어지고** 있었다.
이제부터 **동면을 좌우로 뒤집어** 쓴다 — 화면에서 **왼쪽으로 걸을 때**를 꼭 봐 주면 된다.
(서면 폴더는 만들지 않는다. 만들면 같은 그림이 두 벌이 되어 미러 규정이 죽는다.)

**볼 것 셋**

| 잣대 | 결과 |
|---|---|
| 궤도가 **구르는가**(미끄러지지 않는가) | |
| **이음새 튐**이 없는가(한 바퀴 돌 때) | |
| 남·북과 **같은 기체**인가 | |

| 이음새 실측 | wrap 화소 차 | wrap 겹침 | 이웃 평균(참고) |
|---|---|---|---|
| 남 | 0.4% | 0.996 | 2.2% |
| 북 | 2.5% | 0.975 | 5.7% |
| **동** | **0.7%** | **0.993** | **2.5%** |

세 면 다 **닫혔다**(잣대 0.78 을 넉넉히 넘는다 · `Docs/measure/anim_260911.md`).

**대칭도 0.9965 는 아트가 잰 값이다** — 동면을 좌우로 뒤집었을 때 얼마나 제 모습으로
돌아오는가이며, **서면을 `flipX` 로 대신해도 되는 근거**다.
⚠️ **문서·코드는 이 값을 다시 재지 않았다** — `AnimReport` 에는 대칭도 열이 없다.
(넣을지는 필요해지면 정한다. 지금은 미러를 쓰는 벌이 보스 서면 하나뿐이라 열을
늘릴 이유가 없다.)
⚠️ **이웃 평균이 낮은 것은 하한 미달이 아니라 참고값**이다 — 남(2.2)과 동(2.5)이 낮은데,
이는 「가운데 칸이 덜 움직인다」는 신호이고 **판정은 육안**이다(아트 로그 3-27 과 같은 자리).

---

## 4. 효과음 여덟 — 게임 안에서 청취

⚠️ **소리가 나려면 먼저 아무 곳이나 한 번 클릭해야 한다** — 브라우저가 첫 제스처 전까지
오디오를 잠근다. 「게임 시작」을 누른 것이 그 제스처가 된다.

| # | 사건 (게임에서 언제 나는가) | 파일 | 갈래 | 길이 | 뭉개짐 / 길다 / 괜찮다 |
|---|---|---|---|---|---|
| 1 | **노드를 격자에 놓는 순간** | `sfx_nodesnap` | 조작음 | ⚠️ 1.06초 (상한 0.5) | |
| 2 | **벨트가 올바른 면에 닿아 연결이 성립할 때** | `sfx_beltconnect` | 조작음 | ⚠️ 1.01초 (상한 0.5) | |
| 3 | **병목이 생긴 순간** (상태가 바뀔 때 **한 번만**) | `sfx_bottleneck` | **경고** | ⚠️ 1.03초 (상한 0.5) | |
| 4 | **로봇 A 발사** (초당 여러 번) | `sfx_fire_a` | 효과음 | ✅ **0.480초** — 09-10 에 잘랐다 | |
| 5 | **플레이어 로봇 피격** | `sfx_hit` | 효과음 | ⚠️ 1.72초 (상한 0.5) | |
| 6 | **드론 사출** | `sfx_dronelaunch` | 효과음 | ⚠️ 3.28초 (상한 0.5) | |
| 7 | **합체 전환 연출** (합체 원형을 눌렀을 때) | `sfx_fusion` | 효과음 | ⚠️ 3.97초 (상한 **2.2**) | |
| 8 | **버스트 타격** (합체와 **같은 순간**에 나는 둘째 소리) | `sfx_burst` | 효과음 | ⚠️ 2.99초 (상한 0.5) | |

- 볼륨 갈래는 **경고 > 효과음 > 조작음** 이다(사운드 문서 2장).
- **7·8 은 한 순간에 나는 두 사건**이고 **음색으로 갈려야** 한다 — 그것도 함께 봐 주면 된다.
- 피크는 여덟 다 통과했다(가장 큰 것이 `sfx_fire_a` −3.6 dBFS).
- ⚠️ **일곱이 길이 상한을 넘는다.** 자를지 말지는 **청취 뒤 결정**이다(안 잘라도 촬영은 된다).

| 총평 | 결과 |
|---|---|
| 자를 것이 있는가 (있으면 번호) | |

### 4-1. ✅ 소리 옵션이 **채널 셋**이 됐다 (사운드 문서 7장)

「소리 ▼」를 열면 슬라이더가 셋이다.

| 채널 | 기본값 | 움직이는가 | 그 채널만 바뀌는가 |
|---|---|---|---|
| **배경 음악** | 시험판 **0%** · 배포판 30%(확정값) | | |
| **효과음** | **100%** ⚠️ 가정 | | |
| **조작음** | **100%** ⚠️ 가정 | | |

⚠️ **경고음(병목)은 효과음 채널을 탄다** — 채널이 셋뿐이라 넷째를 못 만든다.
효과음을 줄이면 경고도 같은 비율로 줄어 「경고가 가장 크다」는 관계는 남는다.
⚠️ 값은 **채널마다 따로 기기에 남는다** — 껐다 켜도 그대로여야 한다.

---

## 5. `PortfolioLinks` 값 넷

`Assets/_Project/ScriptableObjects/PortfolioLinks.asset` — 채워 주시면 SO 에 넣고 배포 빌드 전에 확인한다.

| 칸 | 필드 | 지금 값 | 채울 값 |
|---|---|---|---|
| 1 | `documentUrl` — 포트폴리오 문서 URL | *(비어 있음)* | |
| 2 | `notionUrl` — 노션 공개 URL | *(비어 있음)* | |
| 3 | `notice` — 자리표시 문구 | `포트폴리오용 데모` | |
| 4 | `version` — 판 표기 | *(비어 있음)* | |

⚠️ **URL 이 비면 버튼이 안 눌린다**(지금 「(준비 중)」으로 어둡게 뜨는 이유). 채우면 켜진다.

---

## 6. 버튼·패널 자산 후보

⏳ **오후 갱신** — 아트가 후보를 올리면 경로를 여기 채운다.

**볼 것 셋**

| 잣대 | 결과 |
|---|---|
| **톤** — 아이언사가 · 무채색 금속 + 미색 테두리 | |
| **각짐** — 모서리가 둥글지 않은가 | |
| **9-슬라이스 모서리** — 늘렸을 때 테두리 두께가 그대로인가 | |

> 참고 — 지금 화면은 **코드로 그린 껍데기**(`UiSkin`)다. 자산이 오면 **텍스처 한 자리만**
> 갈아끼우면 되고 자리·크기는 안 바뀐다.

---

## 7. 완료 후보 체크

⚠️ **WBS 상태 변경은 설계 몫이다** — 여기서는 **후보만** 낸다. 체크 칸만 봐 주면 된다.

### 7-1. `260910_V03` 17-2 표 그대로

| 후보 | 근거 | 남은 것 | 체크 |
|---|---|---|---|
| `260910_V01` · `V02` · `W01` · `W02` · `W03` 회신문 다섯 | 다음 장이 답했다 | — | |
| `[개발] 레시피 개정 (NodeType 7→8 …)` 대기→**진행중** | `3ae96d1` `68b06f3` 정본 반영 · 앵커 시험 | 코어 산출·`perOutput`·분기 판정 뒤 시작 보드 | |
| `[개발] 아이템 모델 여섯 덩어리` | 배선 + 리허설 육안(보드 아트 · 팔레트 그림 ✓) | **완료 후보** | |
| 적 아트 배선 · 메인 메뉴 · 인셋 전투 · 보스 셋 | 커밋 다수 · 육안 통과 | WBS 행 없음(통보) | |
| `[문서] 포트폴리오 조립 시스템 기획서` | 캡처 열 장 | 초안 v0.2(사용자) | |
| **진행 중** — 리허설 결함 · 시작 보드 · 효과음 길이 · `PortfolioLinks` | — | — | |

### 7-2. 어제 답이 온 셋 — **오늘 이행이 끝났다**

| 후보 | 답 | 이행 | 체크 |
|---|---|---|---|
| **코어 대당 산출** | `260910_W04` → **10** | `f061d01` | |
| **`perOutput` 센티넬** | 1 확정 · 「미정」 표시만 걷기 | `PerOutputTbd` → `PerOutput` · `f061d01` | |
| **코어 출구 분기** | **(D) 출력면 넷** | `f061d01` · 입력면 0 | |

> ⚠️ **「분류기 미도입」은 완료 후보가 아니다** — **안 하기로 한 것**이라 끝낼 일이 없다(S3 수업).
> 남은 답 하나(`boss_Idle` 0.0%)는 `260911_W01` 5장에서 **사후 승인**으로 닫혔다.

### 7-3. 오늘 새로 생긴 후보

| 후보 | 근거 | 남은 것 | 체크 |
|---|---|---|---|
| **리허설 결함 ②**(태그 유령) | `072ba05` · 원인 둘 다 제거 | 육안 재확인 | |
| **리허설 결함 ③**(BGM) | `072ba05` · 「게임 시작」 뒤 재생 + 시험 둘 | 육안 재확인 | |
| **전력 벨트 배선 불일치** | `f061d01` · HANDOFF 「해소」 | — | |
| **UI 스킨·좌표 개편** | `d05a302` · `24950de` | 이 시트 1번 결과 | |
| **리허설 결함 ①** | — | **이 시트 2번 결과** | |

---

## 9. ✅ 육안 여덟 — 이번 빌드에 들어간 수정

| # | 무엇 | 커밋 | 통과 / 반려 |
|---|---|---|---|
| ① | 전투 화면 하단 베이지 띠 제거(변수 패널은 조립 화면 것) | `a19670b` | |
| ② | 「마운트」 이름표 + 12-4 점멸 한자리 | `e086c2a` | |
| ③ | 소리 옵션 채널 셋(배경·효과음·조작음) | `837145d` | |
| ④ | 튜토리얼 국면별 허용 버튼(나머지 어둡게 + 입력 막음) | `dee6fc1` | |
| ⑤ | 채운 뒤 DIM 이 안 걷히던 결함 | `dee6fc1` | |
| ⑥ | HP 바 우 → 좌 감소 | `d2b45d4` | |
| ⑦ | 방향 경계 떨림(히스테리시스 1.5×) | `d2b45d4` | |
| ⑧ | 구역 점선·바닥을 파츠 색 다섯으로 | `e086c2a` | |

**가정 넷** — 설계 역기입 자리다.

| 가정 | 값 | 어디 |
|---|---|---|
| 방향 히스테리시스 | **1.5×** | `DirectionHysteresis.Hysteresis` |
| 효과음·조작음 기본 | **100%** | `AudioChannels` |
| 파츠 색 다섯 | 청·모래·보라·주황·초록(저채도) | `PartPalette.Of` |
| 바닥 틴트 진하기 | **0.22** | `PartPalette.FloorAlpha` |

---

## 남길 말 (자유 칸)

```
(여기에 적어 주시면 V02 에 그대로 옮긴다)
```
