# [문서] 260920_V01 구현 회신 (W01 청구 다섯 답 · 낡은 표 하나 · 코드 착수 없음)

> 구현 → 설계 · 2026-09-20 (일) · 직전 수신 `260919_W01` · 직전 발신 `260919_V01`
> **이행 커밋** — `d41fe7e`(읽지 않는 값 둘에 표기 · 낡은 표 하나 되구움) ·
> `fa91d52`(이 문서 초안)
>
> ⚠️ **게시 커밋은 목록에 없다** — `260919_V01` 12장을 붙인 `81b6993` 도, 이 문서의
> 게시본도 마찬가지다. 목록이 저를 담으려 들면 끝이 없고, 그 자리는 `HANDOFF` 가 든다.
>
> **아트 커밋은 따로** — `ecf4d51`(Move 3차 · 09-21 00:01). 아트 창 소관이라
> 이 문서가 답하지 않는다.
>
> ⚠️ **오늘 구현은 코드에 손대지 않았다.** 내일 오전이 촬영이라 새 착수를 안 열었다.
> 한 것은 **서버 재기동 · W01 청구 다섯 답 · 그 답을 내다 발견한 낡은 표 하나**뿐이다.
>
> EditMode **1263 중 1257 통과 · 0 실패 · 6 건너뜀**
> 빌드 **`?v=1b55d4e`** 그대로 — **재굽기 없음**(`http://localhost:8123/index.html?v=1b55d4e` ·
> index·loader·wasm·data·framework **다섯 다 200**). 서버는 **켜 둔 채로 둔다**.
>
> **증빙** — `Logs/260920_V01_tests.xml`(시험 1263/1257).
> ⚠️ 초안 1차는 「오늘 시험 로그는 스크래치패드」로 적었다 — `260919_V01` 이 세운
> 관례(리포 안 `Logs/`)로 **하루 만에 되돌아간 자리**다. 고쳤다.
> ⚠️ `Logs/` 는 `.gitignore` 대상이라 커밋에는 안 실린다(같은 기계에서만 열린다).

---

# 1. 반드시 읽을 것 하나

**⚠️⚠️ 표 하나가 낡아 있었고, 시험이 그것을 덮고 있었다**(4장).

09-19 에 json `params` 에 `dBAoe` 를 넣고 **`BALANCE_PARAM_DATA` 를 다시 안 구웠다.**
표는 47 · json 은 48. 그런데 시험이 `Assert.AreEqual(47, ...)` 로 **박힌 수**를 지키고
있어서 **초록불이 거짓말을 했다** — 어제 1263 전부 통과한 그 판이다.

`260919_W01` 9장이 「규칙이 글이면 같은 자리에서 다시 넘어진다」를 등재 후보로 올렸는데,
**이번에는 시험이 글이었다.** 수를 박아 둔 시험은 규칙을 지키는 것이 아니라
**지키는 시늉**을 한다.

---

# 2. `260919_W01` 7장 청구 다섯 — 답

## 2-1. 청 1 — 하네스 스위프는 어떤 보드로 도는가

**시작 보드다. 스테이지마다 같은 보드로 돈다.** 코드에서 확인했다(`StageClearHarness`).

| 무엇 | 실측 |
|---|---|
| 판을 세우는 문 | `StartingBoard.Apply` + `StartingBoard.Place(FillsEmptySlot)` — **게임이 세우는 것과 같은 문**이다 |
| 스테이지별 차이 | **없다.** `Run(stageId)` 이 스테이지마다 같은 `Board()` 를 새로 세운다 |
| 시작 적재 | **튜토리얼 종료 판** — 마운트 가득(A 4칸 × 스택 10 = **40**). ⚠️ 창고는 **안 채운다** |
| S5 만 다름 | 두 로봇이라 `StartingBoardB.Apply` 로 B 보드를 같이 세운다 |
| 고정 표본인가 | 아니다 — **매번 같은 문으로 다시 세운다**(스냅샷이 아니다) |

**그래서 5-2 의 읽기가 바뀐다.** S2~S5 가 ❌인 것은 **뒤 스테이지를 시작 보드로 도는
판**이기 때문이다. 게임에서는 플레이어가 그 사이에 보드를 키운다.
**하네스는 「지금 보드로 어디까지 가나」를 재는 장치이지 「스테이지가 깨지나」를 재는
장치가 아니다.**

⚠️ **「그러니 괜찮다」는 아니다.** 지침 §4 「S5·S6 는 물류 + 시스템 이해만으로 통과
가능해야 함」이 걸리는지는 **키운 판**으로 재야 하고, 그 판은 이미 있다
(`RunGrownSweep` — 판 배수 k 로 훑는다). ⚠️ 다만 그것은 **「몇 배가 필요한가」**를
답하지 **「어떤 배치인가」**는 답하지 않는다. 배치는 문서에 없다.

## 2-2. 청 2 — 읽지 않는 값 둘에 표기 (`d41fe7e`)

✅ 붙였다.

· json `params.droneAoeDamageFactor` 의 **이름**에 「🗑️ 파생 · 읽지 않음」
· `WEAPON_DATA` 의 `AoeDamageFactor` **열 머리**에 「(파생·안 읽음)」

⚠️ **값은 안 건드렸다** — 무기 CSV diff **0**이다(열 머리는 한글 줄이라 CSV 에 안 실린다).

## 2-3. 청 3 — 「Notion 문서 → json → SO」를 본 자리

**`CLAUDE.md` 다. 지침이 아니다** — 리포에서 직접 세었다.

| 자리 | 문장 |
|---|---|
| `CLAUDE.md` 11행 | 「데이터 흐름 = Notion 기획 → `balance_v4.json` → `BalanceAssetGenerator` 가 SO 생성 → 게임/시뮬」 |
| `CLAUDE.md` 131행 | 「**데이터 흐름(단방향)** — **Notion 문서 = 단일 원천** → `balance_v4.json` → `BalanceAssetGenerator` → Unity SO」 |
| `CLAUDE.md` 129행 · 152행 | 같은 뜻의 인덱스 주석 둘 |

⚠️⚠️ **`260919_V01` 4-2 의 「지침과 `CLAUDE.md` 가」는 우리 오기다.** 지침에는 그 문장이
없고, 설계가 전문을 확인해 못 찾은 것이 맞다. **정정한다.**

📌 이것도 09-18 에 정정한 자리와 **같은 병**이다 — 자리를 안 열어 보고 「지침과」를 붙였다.
어제 하루 기록의 여섯째(「세기 전에 열어 본다」)가 **바로 다음 날 두 번 걸렸다**(여기와 4장).

## 2-4. 청 4 — 회전 절 가정 표기 유지

✅ **유지한다.** 손대지 않았다. 문서가 정해지면 넣는다.

## 2-5. 청 5 — 새 글에는 「소스」

✅ **접수.** 오늘 새로 쓴 글부터 「소스」를 쓴다. 이 문서도 그렇게 썼다.
⚠️ **회신문은 기록물이라 고치지 않는다** — `260919_V01` 의 「원천」은 그대로 둔다.
⚠️ 코드 주석·`Table_Info` 에 남은 옛 말은 **한 번에 훑을 자리**로 남긴다(촬영 뒤).

---

# 3. 사용자 확정 넷 — 접수

| # | 확정 | 구현 쪽 |
|---|---|---|
| 1 | 등가선·요구치 재산출 = **촬영 뒤** | ✅ 접수. 설계의 가정(「뒤」)이 확정으로 섰다 |
| 2 | 소스 위계는 **갈라 적는다** | ✅ 접수. ⚠️ **순서가 있다** — 아래 |
| 3 | WBS 다섯 · 등재 셋 **승인** | ✅ 접수. **쓰는 것은 설계**다(구현은 WBS 상태를 못 바꾼다) |
| 4 | 촬영 = **내일(월) 오전** · 오늘 낮 안 함 | ✅ 접수. 그래서 오늘 코드 착수를 안 열었다 |

**2번의 순서** — 지침 §10 「값」 행을 **설계가 먼저** 두 줄로 고치고, 그 뒤에 구현이
`CLAUDE.md` 11·131행을 같은 꼴로 맞춘다. **지금은 손대지 않았다.**
📌 순서를 지키는 까닭은 `CLAUDE.md` 가 지침의 사본이 아니라 **리포 쪽 갈래**이기
때문이다 — 먼저 고치면 둘이 다른 말을 하는 날이 하루 생긴다.

---

# 4. 청 2 를 내다 낡은 표를 하나 찾았다 (`d41fe7e`)

## 4-1. 무엇이 낡았나

09-19 에 json `params` 에 **`dBAoe` 를 넣었는데** `BALANCE_PARAM_DATA` 를 다시 안 구웠다.

| | 09-19 밤 | 지금 |
|---|---|---|
| json `params` | 48 | 48 |
| `BALANCE_PARAM_DATA` | **47** | 48 |
| 시험 | `AreEqual(47, ...)` — **통과** | json 을 세어 견준다 |

⚠️ **「09-19 밤」이 두 시각이다.** 표는 `ffc7fb4`(22:09)에 **47 로 맞게** 구워졌고,
`dBAoe` 는 **6분 뒤** `1b55d4e`(22:15)에 들어갔다. 표가 처음부터 틀린 것이 아니라
**그 6분 사이에 낡았다** — 그리고 그날 밤 시험도 빌드도 그 뒤에 돌았는데 아무것도
안 걸렸다. 📌 **낡는 데 6분이면 된다**는 것이 이 자리의 내용이다.

## 4-2. 시험이 왜 못 잡았나

**수를 박아 두었기 때문이다.** 표가 47 이고 시험도 47 이면 둘이 같이 낡아도 초록불이다.
`CombatTuning` 자산 드리프트(09-18)와 **같은 모양**이고, 그때 세운 규칙
「새 TBD 필드는 자산도 같은 커밋에」가 **json 쪽에는 안 걸려 있었다.**

## 4-3. 고친 법

시험이 **json 을 세어** 표와 견준다. 칸이 늘면 표도 같이 늘어야 하고, 안 늘면 죽는다.

⚠️ **세는 열쇠를 열어 보고 골랐다** — `"key"` 로 세면 **52** 다(적 목록에도 `key` 가 있다).
`params` 에만 있는 **`"group"`** 으로 센다. 안 열어 봤으면 이 시험이 **또** 거짓말을 했다.

📌 **새 규칙** — json `params` 에 칸을 더하면 **`BALANCE_PARAM_DATA` 도 같은 커밋에.**

---

# 5. 청하는 것 — 하나

| # | 청 | 왜 |
|---|---|---|
| 1 | 지침 §10 「값」 행 두 줄 개정을 **설계가 먼저** 해 달라 | 3장 2번. 그 뒤에 구현이 `CLAUDE.md` 11·131행을 같은 꼴로 맞춘다. 순서가 뒤집히면 둘이 다른 말을 하는 날이 하루 생긴다 |

판정 요청은 없다. 나머지는 전부 접수·이행이다.

---

# 6. 남은 것

· **오늘 안 한 것** — 코드 착수 전부(촬영이 먼저다) · 표 배선 · TEXT 치환 ·
  json 폐기 표기 전체 · 회전 절
· **다음 묶음은 사용자 지시가 온 뒤에 연다**(§71-42 [D] · 표 배선 · TEXT 치환) —
  둘 다 사용자가 **영상 이후로** 미룬 것이다
· **내일 오전** — 서버 확인 · 육안 결함만 받는다

---

---

# 7. 하루 기록 (2026-09-20 일)

📌 **이 장은 게시 뒤 정정으로 붙였다** — `260920_V01` 이 오늘의 마지막 구현 장이라
하루 기록이 여기 들어간다.

## 7-1. 완료 후보 — 체크는 사용자가 한다

⚠️ **내가 완료로 적지 않는다.** 아래는 **후보**이고, 체크는 사용자가 · **WBS 쓰기는
설계**가 한다(구현은 WBS 상태를 못 바꾼다).

| 후보 | 근거 | 남은 것 |
|---|---|---|
| 회신문 둘 — `260919_W01` 수신 · `260920_V01` 발신 | 게시 · 설계가 WBS 에 W01 완료 · V01 진행중으로 썼다 | V01 은 설계의 다음 장이 답하면 완료 |
| `260919_V01` 「12. 하루 기록」 게시 직후 정정 | `81b6993` · 재열람 통과 | — |
| W01 청구 다섯 답 · 낡은 표 하나 | `d41fe7e` · 이 문서 2장·4장 | — |
| 사용자 확정 넷 접수 | 이 문서 3장 | 지침 개정은 **설계 차례** · `CLAUDE.md` 는 그 뒤 |
| 아트 — mob_cannon 이동 북면 설치 | `046ff19`(아트 창 소관) | 남면 재생성 · judge png 셋 미추적 |
| **진행 중** — 촬영(월 오전) · 편집·이력서 · 육안 여섯 · TEXT 243줄 훑기 | — | — |

## 7-2. 이해도 점검

**건너뛰었다** (사용자 요청).

## 7-3. 내일 할 일 — 2026-09-21 (월)

| # | 항목 | 누가 |
|---|---|---|
| 1 | 아침 체크 → 육안 여섯 → 리허설 정식 → **촬영** | 사용자 |
| 2 | 문서·코드는 **대기** — 육안 결함만 · 새 착수 없음 | 구현 |
| 3 | 지침 §10 두 줄 + §7 재발 셋 · 튜토리얼 낡은 값 셋(**여덟 번째 이월**) · 청구 2·3 문서 확인 | 설계 |
| 4 | 편집 · 이력서 · 기술경력서 · 자기소개서(촬영 뒤) | 사용자 |
| 5 | judge png 셋 커밋 · mob_cannon 남면 재생성 | 아트 |

⚠️ **촬영 뒤 첫 묶음은 사용자 지시가 온 뒤에 연다**(§71-42 [D] · 표 배선 · TEXT 치환) —
둘 다 사용자가 **영상 이후로** 미룬 것이라, 지시 없이 먼저 열면 범위를 되돌린 것이 된다.

## 7-4. 오늘의 주의 다섯

**① 세기 전에 열어 본다.** 오늘 **양쪽 창에서 셋** 걸렸다 — 계획 세션이 CSV 를 47 로
(헤더를 두 줄로 **가정**하고 뺀 산수) · 내가 「지침과」를 확인 없이 붙인 것 ·
`"key"` 로 세어 52 가 나온 것. ⚠️ 어제 하루 기록에 적어 둔 여섯째가 **바로 다음 날**
세 번 걸렸으므로, **한쪽 창의 버릇이 아니라 양쪽에 걸리는 것**으로 본다.

**② 낡는 데 6분이면 된다.** 표는 22:09 에 맞게 구워졌고 json 은 22:15 에 늘었다.
그 뒤 시험도 빌드도 돌았는데 아무것도 안 걸렸다 — **박힌 수를 지키는 시험은
규칙을 지키지 않고 지키는 시늉을 한다.** json 칸을 더하면 표도 같은 커밋에.

**③ 변환기는 파일만 막는다.** 어제 세운 노션 규칙 셋(굵게 한 줄 · 굵게 안 백틱 ·
표 셀 굵게)은 **파일**을 고치는 일이라 변환기가 막았는데, 오늘 깨진 자리는
**내가 도구 인자에 본문을 어떻게 넣느냐**였다. 넷째 자리다 — **본문은 리터럴로.**

**④ 하루 만에 관례가 되돌아간 것이 둘.** 게시 커밋을 목록에 넣은 것과 증빙을
스크래치패드로 적은 것. 둘 다 **어제 내가 세운 관례**이고 **검토가 잡았다.**
📌 규칙은 글이 아니라 **문**이어야 한다는 말이 오늘만 세 자리에서 나왔다
(시험 · 관례 · 변환기).

**⑤ 번호로 된 머리말은 범위가 갈린다.** 설계에 답할 때는 **항목 이름**으로 적는다 —
「1. 승인」 같은 줄은 무엇을 승인한 것인지가 읽는 쪽에서 갈린다.

---

문서 마침 — `260920_V01` (2026-09-20 · W01 청구 다섯 답 · 낡은 표 하나 · 코드 착수 없음)
