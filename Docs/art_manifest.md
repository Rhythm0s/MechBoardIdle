# 아트 자산 매니페스트 — 승인본 출처 (2026-09-07)

> `Docs/art_pending/README.md`를 대신한다. **`260906_W05` 2-1이 「`README.md` 내용을 먼저 옮긴 뒤에 폴더를 지운다」고 지시했고 그 옮긴 자리가 여기다.**
> **md5 열은 사람이 적지 않는다** — 이 파일을 만들 때 스크립트가 `Assets/_Project/Art/` 실물에서 찍는다. 자산 이름은 세대가 바뀌어도 같으므로 이름만으로는 세대를 가를 수 없다.

## 이 파일이 왜 있나

**2026-09-06 재생성 승인본 전량이 세션 임시 폴더에만 있었고, 리포 `Assets/` 아래는 전부 09-04판이었다.** 측정 도구(`MBI.Editor.OverlapReport`)를 만들어 처음 돌렸을 때 드러났다 — 도구는 리포를 읽는데 나온 숫자가 회신문에 적은 값과 달랐다.

| | 리포 `Assets/` (09-04판) | 회신문에 적은 값 (임시 폴더 승인본) |
|---|---|---|
| 노드 21쌍 최대 | **0.894** (`node_booster` × `node_storage`) | 0.860 (`node_core` × `node_muni_basic`) |

**같은 자산 이름이 두 그림을 가리키고 있었다.** 회신문은 승인본을 보고 썼고 리포는 구판을 들고 있었다.

`260906_W05` 2-1이 **「옮긴다」**로 판정했다 — 옮기는 것은 임포트 설정을 확정하는 일이 아니고, 그대로 두어 얻는 것이 없기 때문이다. **2026-09-07에 23개 파일을 `Assets/_Project/Art/` 아래로 옮기고 `Docs/art_pending/`을 지웠다.**

## 목록 — 24개 파일

**재생성 승인본 22종 + `node_energy` + 합체 256 스틸(09-07)**(재생성 대상이 아니어서 09-04판이 그대로 승인본이나, 측정 세트를 온전하게 하려고 함께 둔 것). 그래서 파일은 24개고 「승인본 22종」과 어긋나지 않는다.

| 파일 (`Assets/_Project/Art/` 기준) | md5 | 출처 | job | 비고 |
|---|---|---|---|---|
| `Board/node_core.png` | `8513ac4b` | `board_v2/node_core.png` | `419617d5` |  |
| `Board/node_muni_basic.png` | `8a305963` | `board_v2/node_muni_basic_try2.png` | `1efb8e61` |  |
| `Board/node_muni_complex.png` | `52de3a98` | `gen3/node_muni_complex.png` | `4bbbae4a` |  |
| `Board/node_processing.png` | `bbaf730d` | `gen3/node_processing.png` | `a10d8962` |  |
| `Board/node_storage.png` | `e0d71a9f` | `gen3/node_storage.png` | `4837a128` |  |
| `Board/node_booster.png` | `e689eee8` | `gen5/node_booster_try3.png` | `56c541e4` | 3차에 삼각과 여백을 둘 다 얻은 것 |
| `Board/port_input.png` | `988a22bb` | `gen4/` | `97b051e5` | `260906_W04` 2-1이 셋 다 통과로 확정 |
| `Board/port_output.png` | `d1b84504` | `gen4/` | `7bc000d4` | 같은 판정 |
| `Board/port_power.png` | `299b2630` | `gen4/` | `400bb0a0` | 같은 판정 |
| `Board/node_energy.png` | `c84f9828` | — (재생성 대상 아님) | — | **09-04판이 그대로 승인본이다.** 측정 세트를 온전하게 하려고 2026-09-06에 함께 두었다 |
| `Items/ammo_explosive.png` | `a57ccf07` | `items_v3/` | — |  |
| `Items/ammo_pierce.png` | `3c0cb423` | `items_v3/` | — |  |
| `Items/ammo_standard.png` | `e3a47549` | `items_v3/` | — |  |
| `Items/basic_parts.png` | `2ec59118` | `items_v3/` | — |  |
| `Items/battery.png` | `16eb652c` | `gen6/` | `b540b804` | `260906_W04` 6장 지시로 09-06 재생성 · 후보 c1 |
| `Items/core_energy.png` | `b13d155e` | `items_v3/` | — |  |
| `Items/defense_material.png` | `0394fce7` | `items_v3/` | — |  |
| `Items/drone_body_parts.png` | `9911db42` | `items_v3/` | — |  |
| `Items/power_material.png` | `bd55def4` | `items_v3/` | — |  |
| `Items/propellant.png` | `14988acf` | `items_v3/` | — |  |
| `VFX/vfx_specialstrike_a.png` | `a5cbe83a` | `vfx_v2/` | `895e4c86` | ⚠️ **폐기 — 사용자 확정 2026-09-08.** 태그 스킬 연출을 갈아탄다 (아래 절). 🗑️ **파일도 리포에서 지웠다** (2026-09-08 · 아트 `31064c7` · 사용자 지시) — 이 줄은 남긴다 |
| `VFX/vfx_specialstrike_b.png` | `11d74516` | `vfx_v2/` | `9f1ff31d` | ⚠️ **폐기 — 사용자 확정 2026-09-08.** 같은 자리 (아래 절). 🗑️ **파일도 리포에서 지웠다** (2026-09-08 · 아트 `31064c7` · 사용자 지시) — 이 줄은 남긴다 |
| `Units/boss.png` | `0f536866` | `gen3/boss_512.png` | `d136d116` | **`boss_512.png`에서 이름을 바꿔 놓았다** — `CombatAssetGenerator.LoadArt`가 `Art/Units/<이름>.png`로 읽기 때문. 캔버스는 512 그대로. ⚠️ **2026-09-10 부터 이 512 는 「스틸·컷인·앵커」다** — 전투 벌의 앵커는 아래 `boss_256.png` 이다 |
| `Units/robot_fusion_256.png` | `b3ef28a8` | `create_image_pro` 2차 | `47cdb935` | **2026-09-07 생성·승인.** 그전까지 이 자리는 512 승인본의 NEAREST 축소본(`29290d8` 「참조본」)이었다 — **256 전투 스틸은 생성된 적이 없었다.** 앵커 = 로봇 A 승인본 |

**품목 열 종의 job 번호는 `items_v3/` 일괄 생성분이라 자산별로 나뉘어 있지 않다** — `battery`만 09-06에 따로 다시 뽑았다. 없는 번호를 지어 적지 않는다.

## 배경 셋 (2026-09-09 표기)

**위 「목록 — 24개 파일」에 안 들어 있다** — 그 목록은 재생성 승인본만 담는다. 배경은 2026-09-03에 한 번 만들어진 뒤 재생성된 적이 없다.

| 파일 (`Assets/_Project/Art/` 기준) | md5 | 캔버스 | 생성 | 승인 |
|---|---|---|---|---|
| `Backgrounds/bg_combat.png` | **`38ce9d2e`** | 256 × 256 | **2026-09-11 재생성**(job `22c4cd3f` · 후보 `d_grit`) | ✅ **사용자 확정 09-11** — 공장 판 → 흙·모래. 휘도 28.2% · 이음매 좌우 5.12(이웃 5.49) 상하 7.19(이웃 5.56). 구판 `dc4d2238` 은 `Docs/art_log/candidates/bg_v0903/` |
| `Backgrounds/bg_combat_boss.png` | `b9a9da43` | 256 × 256 | 2026-09-03 · 커밋 `51f69c7` | ✅ **사용자 육안 09-11 · 유지 확정** — 재생성하지 않는다. 휘도 20.6% · 이음매 좌우 7.52(이웃 9.83) 상하 6.46(이웃 7.22) 로 **둘 다 잣대 안**이다. 재생성 후보 셋은 상하가 전부 잣대를 넘었으므로 이 값은 구판이 낫다. 후보는 `Docs/art_log/candidates/bg_combat_v1/` |
| `Backgrounds/bg_board.png` | `65396107` | 192 × 192 | 2026-09-03 · 커밋 `51f69c7` | ⚠️ **기록 없음** |


### 두 전투 바닥의 색 축이 다른 것은 **의도다** (2026-09-11 사용자 판정)

일반 전투는 **색상 32°(황토)**, 보스 전투는 **색상 236°(청회색 강철 판)** 으로 색상환에서 정반대다.
아트가 「일반을 흙으로 바꿨는데 보스만 공장 판으로 남는다」를 사실로 올렸고, 사용자가 판정했다.

- **극명한 차이가 좋다** — 보스 전투가 다른 자리라는 것이 바닥으로 바로 읽힌다
- **보스 전투는 몬스터가 소량만 스폰**하므로 바닥이 복잡해도 화면이 붐비지 않는다
- **몬스터와 바닥의 구분에 문제가 없다**

⚠️ **09-11 「배경 둘 교체」 승인 중 보스 몫은 이 판정으로 닫혔다** — `bg_combat` 만 교체됐다.
보드 배경(`bg_board`)은 색상 227° 로 보스 쪽과 같은 축이며, **일반 전투 하나만 축이 다르다.**

⚠️ **승인 열을 「기록 없음」으로 둔 이유.** 그 커밋의 제목은 「아트 승인본: 배경 3종 …」인데, **문서 어디에도 이 셋의 육안 판정 기록이 없었다.** (보스는 2026-09-11 에 기록이 생겼다 — 위 절. `bg_board` 는 그대로 비어 있다.) 자산 레지스트리는 「배경·캐릭터 17장 · 규격이 전부 문서와 맞는다」로 **규격만** 적었고, 연출 열한 종처럼 「승인된 적이 없다」를 붙여 둔 자리도 아니다.

**커밋 제목을 승인으로 세지 않는다** — 지침 §7 ［09-02］가 「했다고 쓰려면 화면에서 본 자리를 적는다」로 정했고, 같은 조항이 **커밋 메시지를 화면 확인과 혼동한 것**을 사례로 들고 있다. **여기서 판정하지 않고 자리만 적는다.**

✅ **2026-09-09에 셋 다 코드에 배선됐다** (`260909_V01`). 전투 배경 둘은 `CombatTuning`, 보드 배경 하나는 `BoardArtSet`이 든다. **임포트 설정은 안 건드렸다** — 한 장을 Repeat으로 늘리는 대신 코드가 격자로 복제해 깐다.

✅ **랩 모드가 닫혔다 — 문서가 코드에 맞춰졌다** (2026-09-09 · `260909_W01` 4장).
배경 아트 요청 문서 3-3의 랩 모드가 **`Repeat` → `Clamp`**로 바뀌었고, 반복은 코드가 격자로 복제해 만든다.
⚠️ **근거는 성능이 아니라 예외다** — 타일 수십 개는 WebGL에서 문제가 안 되고, 구현이 「시야 + 여유 한 장」으로
묶어 타일 수가 늘지도 않는다. **진짜 비용은 임포트 규격에 예외 갈래가 생기는 것**이고,
그러면 다음 자산부터는 매번 어느 쪽인지를 판정하게 된다. 화면 결과는 둘이 같다.
✅ **`SpriteImportRules`는 고칠 것이 없다** — 규격이 하나로 유지된다.

## 모듈 기호 둘 (2026-09-09 설치 · 아트 커밋 `f5c5dad`)

| 파일 | md5 | 실루엣 | job |
|---|---|---|---|
| `Board/mod_m.png` | `d8985ba2` (구 `ebc850c0` · 가운데로 옮기기 전) | 33×59 · 여백 L15 R16 T2 B3 | `c897983b` (2차 c10 · 회전 안 함) |
| `Board/mod_r.png` | `2b1d32f3` | 52×36 | `4b7570b1` (1차 c00) |

✅ **B-7 실측 — 겹침 0.319** (아트 로그 3-22 · `mod_m` 을 가운데로 옮긴 뒤).
판정선 0.90 아래다. ⚠️ **옮기기 전 값은 0.209였다** — 가운데로 모으면 둘이 가까워져 값이 올라간다.
**둘 다 통과이며 유효한 값은 0.319다.** **해소로 바꿀지는 설계가 정한다** — 보드 문서 10-8에 역기입했다.
✅ **구현 배선 완료**(`a57a5f1`) — 글자 자리표시를 그림으로 바꿨다. 밝기 축은 안 걸고 강조색도 안 쓴다.

⚠️ **`mod_m`이 캔버스 가운데에 있지 않다** — 여백이 **L29 R2 T2 B3**이다. 규칙 9 장치가 왼쪽 절반을
비우게 만든 결과이며 그림 자체의 흠은 아니지만, **64 칸에 그대로 놓으면 오른쪽으로 붙어 보인다.**
`mod_r`은 L6 R6으로 가운데에 있어 **둘이 나란히 놓이면 어긋나 보인다.**
⚠️ **구현은 이것을 코드로 보정하지 않았다.** 보정하면 코드가 특정 자산 한 장의 여백을 기억하게 되고,
그 그림이 바뀌는 날 조용히 어긋난다. **자산을 가운데로 옮기는 것이 옳은 자리**이며 화소 이동이라
손실이 0이다(회전본으로 실증됨). **승인본을 건드리는 일이라 사용자 판정이다.**

## `.meta` 추적 — 규칙과 실제가 갈려 있다 (2026-09-09 실측 · 판정 요청)

아트 세션이 「`Board/`는 하나도 추적되지 않는데 `Anim/`은 44개가 추적된다」를 올렸다.
**실제로 세어 보니 수가 다르고, 갈린 곳도 둘이 아니라 넷이다.**

| 경로 | 추적 | 미추적 |
|---|---|---|
| `Art/Anim` | **24** | 353 |
| `Art/Board` | **0** | 18 |
| `Art/Items` | **0** | 11 |
| `Art/VFX` | **0** | 11 |
| `Art/UI` | **0** | 7 |
| `Art/Units` | **15** | 2 |
| `Art/Backgrounds` | **3** | 0 |
| `Audio` | **3** | 0 |

⚠️ **어느 쪽으로도 규칙이 서 있지 않다.** 배경과 소리는 전부 들어가 있고, 보드·품목·연출·UI는
하나도 안 들어가 있으며, 애니메이션과 유닛은 섞여 있다.

⚠️ **두 번 틀렸다. 아래가 세 번째 서술이며 이번에는 잰 것과 안 잰 것을 갈라 적는다.**
① 「참조가 전부 끊겨 보드 그림이 안 나온다」 — **재지 않고 썼다. 폐기.**
② 「Unity가 임포트하면서 SO를 다시 써 버린다 · 알아서 이어 붙여 준 결과다」 — **원인을 잘못 짚었다. 폐기.**

**잰 것 셋 · 안 잰 것 하나** (2026-09-09 · 새 클론 + 배치모드):

| | 무엇 |
|---|---|
| ✅ **잼** | `.meta` 없이 클론해 임포트하면 **GUID가 새로 발급된다** (`mod_m` `6b789cb5…` → `27b95c1e…`) |
| ✅ **잼** | 클론에서 시험 21개가 통과한 것은 **생성기가 시험 안에서 다시 돌았기 때문**이다 — `BoardArtWiringTests.Build()`가 곧 `BoardConfigGenerator.LoadOrCreateArt()`이고, 그 함수가 **32자리를 경로로 다시 읽어** 채운 뒤 `SetDirty`한다 |
| ✅ **잼** | `BoardArtSet.asset`의 **28자리가 새 GUID로 다시 써졌다.** 이것도 **그 함수의 결과**이지 Unity가 한 일이 아니다 |
| ❌ **안 잼** | **생성기를 안 돌리고 웹빌드하면 어떻게 되는지는 재지 않았다.** 그림이 나오는지 안 나오는지 **단정하지 않는다** |

⚠️ **시험이 재려던 조건을 스스로 만들었다.** 「생성기 없이도 참조가 사는가」를 물었는데
**시험의 첫 줄이 생성기를 부른다** — 그 배치로는 그 물음에 답할 수 없다.
답하려면 **생성기를 안 부르고 `BoardArtSet.asset`을 그대로 읽는 검사**가 따로 있어야 한다.

✅ **판정이 났다 — 넣는다** (2026-09-09 사용자 확정 · 아트가 전량 커밋했다).
구 조항 「`Assets/_Project/Art/` 아래의 `.meta`를 커밋하지 않는다」는 **폐기**됐고,
**재생성이 끝난 폴더의 `.meta`는 커밋**한다. **임포트 설정의 소스는 `SpriteImportRules`·`AudioImportRules`**다 —
매 임포트마다 코드가 규격을 먹이므로 `.meta`가 리포에 있어도 설정이 갈리지 않는다.
**금지가 지키려던 것을 이제 코드가 지킨다.** 인계 §7에 새 조항이 있다(커밋 `d0717da`).
⚠️ **지침 문서 쪽 문안은 설계가 `260910_W02` 뒤에 쓴다** — 그때까지 §7이 최신이고 지침 원문은 낡은 채다.

## 연출 자산의 상태 — 폐기 셋 · 반려 넷 (2026-09-08 사용자 육안)

**연출 열한 종은 2026-09-04 초안이며 승인된 적이 없다.** 2026-09-08 육안에서 일곱이 갈렸다. **파일은 지우지 않는다** — 삭제는 사용자 몫이고, 여기는 상태만 적는다.

| 식별자 | 상태 | 사유 · 다음 |
|---|---|---|
| `cutin_fusion` | ✅ **새 컷인 승인·설치 (2026-09-08 · `3ce220c`)** — md5 `29945c27` · 512 × 512 | 총이 있는 로봇 팔(A)과 드론 베이(B)가 합쳐지는 장면 · **참조는 현행 승인본 셋** · 15-3 F-1의 2단계 방식. ⚠️ **구 서술** — 초안의 모델이 폐기된 세대(구 로봇 A 앵커)라 폐기하고 재설정했다 |
| `vfx_specialstrike_a` | **폐기 (사용자 확정) · 🗑️ 파일 삭제됨** (2026-09-08 · `31064c7`) | 새 A 태그 인 = **120도 부채꼴 탄환비가 내리며 360도 회전** |
| `vfx_specialstrike_b` | **폐기 (사용자 확정) · 🗑️ 파일 삭제됨** (2026-09-08 · `31064c7`) | 새 B 태그 인 = **밝은 레이저 한 줄기가 맵을 360도 회전** |
| `vfx_dronelaunch` | ✅ **재생성본 승인·설치 (2026-09-08 · `8b2775b`)** — md5 `9da01d79` | 반려 사유 「미사일로 보인다」 → 문안을 **뒤에 남는 분사**로 고쳐 다시 뽑았다 (틀 A) |
| `vfx_booster` | ✅ **재생성본 승인·설치 (2026-09-08 · `8b2775b`)** — md5 `f30f8e82` | 반려 사유 「폭발로 보인다」 → **길이 축 3배** · 옆 제트 제거 (틀 A) |
| `vfx_droneexpire` | ✅ **재생성본 승인·설치 (2026-09-08 · `8b2775b`)** — md5 `f28114d4` | 반려 사유 「배터리가 펑 하고 작게」 → **한 점에서 짧게 터지는 것**으로 (틀 B) |
| `vfx_ammoout` | ✅ **재생성본 승인·설치 (2026-09-08 · `8b2775b`)** — md5 `c98a03fa` · ⚠️ **우선순위가 선택 → 필수로 올랐다** (2026-09-09 · `260909_W01` 3-2 · 연출 필수가 여섯에서 **일곱**으로) | 반려 사유 「폭발 같고 경고 느낌이 없다 · 사선이 없다」 → **사선을 첫 문장으로 올리고 틀 C(화면에 붙는다)로** 바꿨다 |

✅ **컷인도 새로 뽑아 설치했다** (2026-09-08 · `3ce220c` · md5 `29945c27` · 512). **파일 이름은 같지만 그림이 다르다** — 구 초안은 폐기된 세대의 모델이었다.

✅ **반려 넷은 재생성본이 사용자 통과해 설치됐다** (2026-09-08 · `8b2775b`). 위 표의 넷 행이 그것이다 — 이전 초안은 `candidates/`에 보관돼 있다.

**남은 넷**(`vfx_muzzleflash` · `vfx_shelleject` · `vfx_death` · `vfx_burst`)**은 이번 육안에서 갈리지 않았다** — 승인도 반려도 아니고 초안 그대로다.

## 필수 자산이 일곱에서 여섯이 됐다 (2026-09-08 · `260908_W04` 3-4)

`vfx_specialstrike_a` · `_b` 둘이 빠지고 **`vfx_tagbullet` 하나가 들어왔다.**

| 식별자 | 캔버스 | 상태 | 무엇 |
|---|---|---|---|
| **`vfx_tagbullet`** | **64 × 64** · 실루엣 **26 × 48** | ✅ **승인·설치 (2026-09-08)** — md5 `a55f3366` · 후보 c11 · job `865c756f` · 커밋 `89d7d09` | 내려꽂히는 탄환 한 발. **탄환비는 이 한 발을 코드가 부채꼴로 뿌리고 돌려** 만든다. 여백 L19 R19 T10 B6 · **후처리 없음**(테두리 휘도 64.2 — 검은 테두리가 아니다) |

⚠️ **레이저(B 태그 인)는 자산이 없다** — 전부 코드 드로잉이라 이 목록에 들어오지 않는다.

⚠️ **실루엣 세로 48은 규격 「약 38」보다 크다.** 꼬리를 포함한 값이고 몸통만 재면 30 안팎이다. **줄이지 않았다** — 38이 몸통인지 꼬리까지인지가 문서에 없어 **값의 뜻은 설계가 정한다**(`260908_V05` 판정 요청). 코드도 같은 이유로 **실제 자산에는 배율을 강제하지 않는다**(`c1e000f`).

⚠️ **캔버스를 64로 둔 근거.** 도구 하한은 **16**이라 38도 받는다(아트 로그 3-15 · `2716797`). W04 3-2가 「64가 되면 64」로 정했고, 38은 도구가 캔버스를 채우는 성질 때문에 탄환이 테두리에 닿기 쉽다.

⚠️ **위 일곱은 이 파일의 「목록 — 24개 파일」에 들어 있지 않다** — 그 목록은 재생성 승인본만 담기 때문이다. `vfx_specialstrike_a/b` 둘만 예외로 거기 있고, 두 줄에 폐기 표기를 붙였다.

🗑️ **그 둘은 파일까지 지워졌다** (2026-09-08 · 아트 커밋 `31064c7` · 사용자 지시). **줄은 지우지 않는다** — 폐기는 항목을 지우는 것이 아니라 표기로 남기는 것이고(지침 §10), md5와 job 번호가 없어지면 「그 그림이 무엇이었나」를 되짚을 자리가 사라진다. 앞으로 이 둘을 찾으면 **리포에 없는 것이 맞다.**

## UI 자산 — `Assets/_Project/Art/UI/` (2026-09-11 등재)

⚠️ **여섯이 리포에 있는데 이 대장에 한 번도 오른 적이 없었다.** 월요일 「아이콘 다섯 배선」이
md5 를 찾을 자리가 없어 먼저 적는다. **승인 상태는 아트가 판정하지 않는다** — 육안 기록을 못 찾았다.

| 파일 | md5 | 캔버스 | 실루엣 | 여백 L R T B | 상태 |
|---|---|---|---|---|---|
| `UI/icon_logi_normal.png` | `8289d3c4` | 128 | 94 × 38 | 17 17 47 43 | 09-04 생성 · ⚠️ **육안 기록 없음** |
| `UI/icon_logi_slow.png` | `22af3cc2` | 128 | 78 × 55 | 25 25 36 37 | 〃 |
| `UI/icon_logi_stop.png` | `1d95e7d3` | 128 | 98 × 28 | 15 15 49 51 | 〃 |
| `UI/icon_not_connected.png` | `59d54db7` | 128 | 66 × 74 | 38 24 27 27 | 〃 |
| `UI/icon_power_short.png` | `9ceb4692` | 128 | 72 × 64 | 28 28 30 34 | 〃 |
| ~~`UI/ui_kit_main.png`~~ | `3377a5dd` | **512** | 353 × 417 | 64 95 64 31 | ✅ **이동 · 09-04 초안 · 미승인** — 2026-09-11 에 `Docs/art_log/candidates/ui_kit_v0904/` 로 옮겼다(`.zip` 사본과 `.meta` 둘 함께) |

**아이콘 다섯은 코드가 아직 안 읽는다** — `Assets/_Project/Scripts/` 에 파일 이름으로 참조하는 자리가 **0건**이다(2026-09-11 grep). 월요일 배선이 그 자리를 만든다.

⚠️ **`ui_kit_main.zip` 은 압축 파일이 아니었다 — PNG 와 바이트까지 같은 사본이다** (2026-09-11 실측).
첫 8바이트가 PNG 서명이고 md5 가 `3377a5dd` 로 PNG 와 **완전히 같다.** 확장자만 `.zip` 인 같은 그림이며,
그래서 Unity 가 이것도 텍스처로 임포트하고 있었다. **둘 다 옮겼다.**

⚠️ **`ui_kit_main` 은 2026-09-11 확정으로 쓰지 않는다** — 버튼·패널을 **그림 자산으로 새로 만든다**(UI 아트 문서 1-1 뒤집기 · 문안은 `260911_W01` 신설 절). 이 초안은 그 자산이 아니다.

### 그릇 자산 — 종류 둘 (2026-09-11)

**늘어나는 것 (9-슬라이스)** — 원본 64 · 모서리 여백 16 · 코드 배율 ×2 · 최소 64.
가운데와 변 조각에 무늬가 없어야 하며, **내용물(막대·링·숫자·글자·아이콘)은 코드가 그린다.**

| 파일 | md5 | 캔버스 | **여백 L R T B** | 가운데 sd | 변 sd (T B L R) | 상태 |
|---|---|---|---|---|---|---|
| `UI/ui_plate_button.png` | `170e6f87` | 64 | **16 16 16 16** | **0.000** | **0.00 0.00 0.00 0.00** | ✅ **사용자 승인 09-11** — 후보 `c13`(버튼 1차 16장 중)을 **9-슬라이스가 되도록 수리한 것**. 수리 = 변 조각을 늘림축 최빈 줄로 채움 · 가운데 최빈색 · **모서리 16 무손상** · 새 색 0(팔레트 20색 유지) · 바뀐 화소 78/4096 = 1.9%. 도구 `Docs/art_log/fix_nineslice.py` |

⚠️ **이 대장에서 「여백」은 절마다 뜻이 다르다 — 그릇 자산은 모서리 여백이다.**
아이콘·유닛 절의 「여백 L R T B」는 **실루엣이 캔버스 안에서 비워 둔 자리**(잰 값)다.
그릇 자산의 「여백」은 **9-슬라이스 모서리 조각의 크기**(정한 값 · W02 9장 = 16)이며,
코드의 **Sprite Border 16,16,16,16** 과 같은 수다. 그릇은 캔버스를 가장자리까지 채우므로
실루엣 여백은 언제나 0 0 0 0 이고, 그 값을 적어 봐야 아무것도 알려 주지 않는다.

⚠️ **이 자산의 색 축은 W02 6-1 과 다르다** — 채운 색 `#3B2B1B`(색상 30° · 채도 0.54) · 빗각 선 `#6D381F`(색상 19° · 채도 0.72) 로 **주황 축 안**이다. 6-1 은 「반투명 검정 바탕 · 미색 테두리 · 주황은 코드 틴트」. **판정은 설계 자리**이며 아트는 사실만 적는다(`art_to_plan` M-1-1).

⚠️ **문서·코드 몫 하나** — 9-슬라이스로 쓰려면 임포트에 **Sprite Border 16,16,16,16** 이 필요하다. `SpriteImportRules` 가 소스이므로 아트는 손대지 않았다.

**고정 (9-슬라이스 아님)** — 원형 200(합체·태그 · 링 자리 비움). 아직 없다.

## 보스 전투 승인본 256 (2026-09-10 · 사용자 승인)

**보스에 승인본이 둘이 됐다 — 쓰임이 다르다.** 도구 상한이 256 이라(아트 로그 3-24) 512 로는 회전도 애니도 못 만든다.
**화면에서는 코드가 2배로 키운다**(사용자 확정 2026-09-10).

| 파일 | md5 | 캔버스 | 쓰임 | 출처 · job |
|---|---|---|---|---|
| `Units/boss_256.png` | **`a909ae6a`** | 256 × 256 · 실루엣 **142 × 132** | ✅ **전투 승인본** — 회전·사망 벌의 **앵커** | `candidates/boss_256_v1/c00.png` · job `a73a0def` |
| `Units/boss.png` | `0f536866` | 512 × 512 · 실루엣 414 × 384 | **스틸 · 컷인 · 앵커** | `gen3/boss_512.png` · job `d136d116` |

**256 은 512 의 축소본이 아니다** — 512 를 **참조로 걸어 새로 그린 것**이다(`create_image_pro`).
512 와의 **모양 겹침 0.977** · 여백 **L57 R57 T59 B65**(좌우가 같다 = 정면이다).
**폐기된 앵커 후보** — 09-04 벌의 첫 칸 `a36883f7` 은 3/4 로 기운 그림이라 **승인 아님**(512 와 겹침 0.744).

✅ **`boss_Death` 남면 아홉 칸을 이 승인본 위에서 다시 만들어 설치했다** (2026-09-10 · 사용자 육안 통과).
세로 변화가 **1px → 37px(첫 칸 높이의 28.0%)** 이 됐고 가로는 142 → 199 로 퍼진다.
**구 09-04 벌은 `Docs/art_log/candidates/boss_Death_v1/` 에 보관**한다. 대기·이동 09-04판은 손대지 않았다.

⚠️ **2배로 키워도 구 512 스틸보다 작다** — 256 의 실루엣 142 × 132 를 2배 하면 284 × 264 이고
512 승인본은 414 × 384 다. **배율은 코드가 정하는 자리**라 아트는 값만 적어 둔다.

## 모듈 기호 둘 (2026-09-09 · 보드 아트 요청 문서 10장)

**노드 위에 얹히는 64 기호다.** 구현이 D구간 촬영용으로 글자 두 자를 자리표시로 쓰고 있던 자리를 그림으로 바꾼다.
**위 「목록 — 24개 파일」에 안 들어 있다** — 그 목록은 재생성 승인본만 담는다.

| 파일 (`Assets/_Project/Art/` 기준) | md5 | 캔버스 | 출처 | job | 상태 |
|---|---|---|---|---|---|
| `Board/mod_r.png` | `2b1d32f3` | 64 × 64 · 실루엣 **52 × 36** | `candidates/mod_v1/mod_r/c00.png` (16후보 중) | `4b7570b1` | ✅ **1차 c00 사용자 승인·설치 (2026-09-09)** |
| `Board/mod_m.png` | `d8985ba2` | 64 × 64 · 실루엣 **33 × 59** | `candidates/mod_v2/mod_m/c10.png` (16후보 중) | `c897983b` (2차) · 구 `0c48a155` (1차 · 전량 X) | ✅ **2차 c10 사용자 승인·설치 (2026-09-09)** — 회전하지 않은 원본. **승인 뒤 가운데로 옮겼다** — 구 md5 `ebc850c0` |

**`mod_r` 승인본의 실측** — 여백 L6 R6 T14 B14 · 채움 56.4% · 평균 휘도 121.2(최고 218.7).
⚠️ **어두운 테두리가 붙어 있다**(테두리 평균 휘도 **24.6** · 몸통 121.2) — 10-5 는 「clean unbordered edge」이고
명도도 「TWO value steps」인데 실제는 여섯 단이다. **사용자가 이 장을 보고 골랐으므로 반려 사유가 아니라 실측으로 남긴다.**

⚠️ **`mod_m` 1차가 전량 실패한 이유는 문안이다.** 10-5 의 `TWO nested chevrons opening OUTWARD` 를 도구가
**등을 맞댄 꺾쇠 둘 = X** 로 읽었다. 좌우 접기 일치도가 16장 중 11장에서 **100%**(세로축에 대고 접으면 완전히 포개진다)라
X 임이 숫자로도 확인된다. `vfx_ammoout` 이 세 번 X 를 그린 것과 같은 자리이며, 그때 통한 **규칙 9**(반대쪽을
「비어 있는 투명 배경」이라고 문장으로 못 박기)를 2차에 걸었다. **문안이 바뀌었으므로 10-5 를 고칠 자리가 생긴다 — 문서 세션 몫이다.**

✅ **B-7 이 실측으로 통과했다** (보드 문서 10-8). 두 승인본을 각자의 캔버스 그대로 겹쳐 재니 **0.319** 로,
3-1-1 의 판정선 **0.90** 에서 한참 아래다. 하나는 오른쪽으로 벌어지는 꺾쇠고 하나는 누운 빗금이라 실루엣이 갈린다.
**해소로 바꿀지는 설계가 정한다** — 아트는 잰 값만 낸다. (가운데로 옮기기 전 값은 0.209 였다 — 옮기면 둘이 가까워져 값이 올라간다. **둘 다 통과다.**)

✅ **`mod_m` 을 캔버스 가운데로 옮겼다** (2026-09-09 사용자 지시 · **자산만** · 코드 보정 없음).
규칙 9 장치가 왼쪽 절반을 비우게 만들어 여백이 L**29** R2 였고, 64 칸에 그대로 놓으면 오른쪽으로 붙어 보였다.
가로로 **14px** 옮겨 **L15 R16 T2 B3** 이 됐다. **무손실이다** — 보이는 화소 **1070개의 다중집합이 완전히 같고**
투명 화소의 RGB 도 구본과 같게 채웠다. 그래서 md5 만 `ebc850c0` → `d8985ba2` 로 바뀐다(그림이 아니라 자리가 바뀐 것).
`mod_r` 은 L6 R6 으로 원래 가운데에 있어 손대지 않았다.

⚠️ **회전은 손실이 없다는 것을 실측했다** (사용자 물음 · 2026-09-09). 2차 16장을 반시계 90도로 돌려
**화소 다중집합이 완전히 같음**을 확인했다 — 90도는 보간이 없어 「명도 두 단」이 깨지지 않는다.
돌린 판과 가운데로 옮긴 판은 `candidates/mod_v2/mod_m_rot90*/` 에 있다. **채택하지 않았다** — 사용자가 회전 없이 c10 으로 정했다.

⚠️ **위쪽 화살표 16장도 뽑아 두었다** (`candidates/mod_v3/mod_m_arrow/` · job `3746f996`). **설치하지 않았다.**
사용자가 c10 으로 정하면서 쓰이지 않았고, 지우지 않고 남긴다 — 10-3 의 형태를 다시 볼 일이 생기면 재료가 된다.

✅ **`.meta` 규칙이 바뀌었다 — Art/ 아래 전량을 커밋한다** (2026-09-09 사용자 확정 · 커밋 `38bd426`).
아래 「임포트에 대한 금지」 절의 구 규칙은 **폐기됐다.** 이 커밋이 새로 추적한 것이 **444개**이고, 커밋 뒤
**미추적 `.meta` 는 0**, `Art/` 아래 추적 총합은 **488개** 다.

⚠️ **그중 35개는 `Assets/_Project/Art/_candidates/` 의 것이고 그 폴더의 PNG 32장은 여전히 미추적이다** —
자산 없는 `.meta` 라 Unity 가 새로고침할 때 지울 수 있다. **판정이 필요한 자리다.**

## ~~임포트에 대한 금지는 그대로 살아 있다~~ — **폐기 (2026-09-09)**

> ⚠️ **아래 두 줄은 2026-09-09 사용자 확정으로 폐기됐다.** `Art/` 아래 `.meta` 는 **전부 커밋한다**(커밋 `38bd426`).
> 바꾼 이유 셋 — 재생성이 끝나 자산이 고정됐고, 임포트 설정의 진실 원천이 `SpriteImportRules` 이며,
> `BoardArtSet` 이 **guid 32개**를 들고 있어 guid 가 흔들리면 씬 참조가 끊긴다.
> **문단은 지우지 않고 표기로 남긴다**(지침 §10).

`260906_W04` 2-4가 확정한 금지의 실질 둘은 **옮긴 뒤에도 그대로다.**

- **`.meta`는 커밋하지 않는다** — Unity 에디터가 만들지만 리포에 넣지 않는다
- **`.gitignore`에도 넣지 않는다** — 금지를 규칙으로 굳히는 것이 아니라 지금은 넣지 않기로 한 것이다

## 옮긴 것을 무엇으로 확인했나

`MBI.Editor.OverlapReport.Run`을 `-artRoot Assets/_Project/Art`로 다시 돌려 **자산별 md5가 `Docs/measure/overlap_260906_pending.md`(승인본 세트를 잰 것)와 같은지** 본다. 다르면 아직 안 옮겨진 자산이 남은 것이다.

