# [문서] 260918_V03 구현 회신 (S3·S5 실측 · 리허설 열둘 · 전투 HUD 시안 3 · 정정 둘)

> 구현 → 설계 · 2026-09-18 (금) · 직전 수신 `260918_W01` · **사용자 확정 09-18**(HUD 시안 3 · 플랜 창)
> · 직전 발신 `260918_V02`
> **이행 커밋 — `260918_V02` 게시(11:00) 뒤 전부**
> `479597c`(V02 게시본 문서) · `344dfd8`(**하네스에 스테이지 점프 판 S1·S3·S5** · 12:39) ·
> `ba3a648`(**리허설 열둘 반영** · 12:51) · `11f2ef9`(HUD 헤더 분리 · **같은 날 폐기** · 13:17) ·
> `17147b8`(**전투 HUD 시안 3** · 14:31) · `4c6dae5`·`c09d2ca`(이 문서) ·
> `f93315a`(설계 검토 ③ — 전투력 식을 되돌린다 · 14:41)
> ⚠️ **아트 것은 리포에 없다** — `Docs/art_log/` 아래 새 그림들이 **추적 밖**이라 커밋이 안 잡힌다(10장).
> EditMode **1204 중 1198 통과 · 0 실패 · 6 건너뜀**
> — 결과 xml `C:/Users/Kang/AppData/Local/Temp/claude/C--Users-Kang-OneDrive------/4b27ebae-8565-4ef9-951d-a870936a2e68/scratchpad/hud3_tests_260918.xml` (2026-09-18 14:40)
> 하네스 **태그 두 로봇 S1 — Win 77.7초 · 남은 적 0/100 · 로봇 HP 1000/1000** (웨이브 전 81.2초)
> — 로그 `C:/Users/Kang/AppData/Local/Temp/claude/C--Users-Kang-OneDrive------/4b27ebae-8565-4ef9-951d-a870936a2e68/scratchpad/hud3_tag_s1_260918.log` **399~401 줄** (2026-09-18 14:41)
> 빌드 **`?v=f93315a`** (`http://localhost:8123/index.html?v=f93315a` · index 200 · wasm 12,463,623바이트 · 14:46)
>
> ⚠️ **경로는 이 세션의 스크래치패드다** — 리포 밖이라 다른 창에서 안 보인다.
> 증빙을 찾을 때는 위 절대 경로를 그대로 쓸 것(지침 §7 「xml 숫자가 증거다」).

---

# 1. 반드시 읽을 것 셋

📌 이 문서는 `260918_V02` 게시(11:00) **뒤 오후 전부**를 싣는다 — 2장 · 3장 · 4~7장.

**① ⚠️⚠️ 「뒤 스테이지는 촘촘해진다」가 실측과 반대였다**(2장 · `344dfd8`).
간격은 **벌어진다**(S1 0.80 → S5 **3.23**). 그래서 광역 반경 2칸이 S5 에서 **1.00 마리**,
곧 주 표적 하나뿐이 된다. ✅ 사용자가 「**S1 에서 약해도 된다 · 값 불변 · 반경과 본전 수는
설계 W02**」로 닫았고 **값은 한 칸도 안 옮겼다** — 표는 **센 것**이지 고친 것이 아니다.

**② ✅ 리허설 열둘을 넣고**(3장 · `ba3a648`) **그 뒤 HUD 를 시안 3 으로 다시 짰다**
(4~7장 · `17147b8` · 사용자 확정 09-18 · 플랜 §85-8).
좌상단 글자 블록이 **통째로 사라졌고** 수치 아홉 줄은 **개발 빌드 전용 「i」 패널**로 들어갔다.
🗑️ 같은 날 오전의 「헤더 + 아래 블록」(`11f2ef9`)은 **폐기**다 — 줄이 늘 때마다 잘릴 자리를
다시 재야 하는 꼴이었고, 09-14 에 세 줄을 잃은 뒤 **네 번 고친 자리**다.
**네 번 고쳤으면 값이 아니라 꼴이 틀린 것이다.**

**③ ⚠️⚠️ 정정 둘을 먼저 적는다**(아래 9장).
**종전 회신문 하나**가 틀린 말을 실었다 — `260918_V02` 의 「충전량·수명은 그대로」이며
코드에서 사실이 아니었다. 함께 적는 **읽기 오류 하나**는 회신문이 아니라
**세션 사이의 독법**에서 났다(9장 ②).

---

# 2. S3·S5 표적 수 측정 — 전제가 실측과 반대였다

`344dfd8` (하네스에 **스테이지 점프 판** S1·S3·S5 · 12:39).

⚠️⚠️ **왜 쟀나.** 「광역형 한 기가 S1 에서 평균 1.6 마리밖에 못 친다」는 `260918_V02` 5-6 의
실측에 대해, 「**뒤 스테이지에서는 적이 촘촘해지니 괜찮다**」가 기대였다.
그것이 참인지 재려면 **S1 말고 뒤 판을 돌려 봐야** 한다 — 그래서 하네스가 스테이지를 건너뛴다.

| 스테이지 | 적 간격(평균) | r=2 | r=3 | r=4 | r=5 |
|---|---|---|---|---|---|
| S1 | 0.80 | 1.20 | 1.30 | 1.30 | 1.45 |
| S3 | 0.74 | 1.31 | 1.92 | 2.08 | 2.35 |
| S5 | **3.23** | **1.00** | 3.63 | 5.96 | 7.27 |

📌 **전제가 실측과 반대다.** 뒤 스테이지는 **촘촘해지지 않는다** — 개체가 커져
**간격이 벌어진다**(S1 0.80 → S5 3.23). 그래서 반경 2칸은 S5 에서 **1.00 마리**,
곧 **주 표적 하나뿐**이 된다. 본전 4마리는 S5 에서 r ≈ 3.5 가 있어야 선다.

✅ **사용자 결정** — 「**광역형은 S1 에서 약해도 된다 · 값은 안 바꾼다 · 반경과 본전 수는
설계 W02 가 정한다**」. 그대로 따랐다. ⚠️ **값은 한 칸도 안 옮겼다** — 위 표는 **센 것**이지
고친 것이 아니다.

✅ **설계 이의제기 접수** — `droneAoeDamageFactor` 는 **신설 값이 아니라** 무기 스펙트럼의
좌표 **50** 이다. 맞다. ⚠️ **이관은 W02 문안 뒤로 미룬다** — 문안보다 먼저 옮기면
좌표가 어디서 왔는지 모르는 수가 자산에 남는다.

---

# 3. 리허설 열둘 반영

`ba3a648` (**리허설 반영 열하나 — 회전 결함 · 이름 롤백 · 연출 넷 · 진단 줄** · 12:51).
사용자 리허설(`?v=196328d`) 결과를 한 묶음으로 넣었다.

## 3-1. 사용자 값·결정 다섯

| # | 무엇 | 넣은 값 |
|---|---|---|
| ③ | 회피 이동 거리 | **0.5 → 1.25**(`balance_v4.json` · `confirmed: true`). 시간은 그대로라 **2.5배 빠르다** |
| ④ | 무적 깜빡임 | **폐기** — 같은 날 오전에 넣었다가 저녁에 걷었다 |
| ⑦ | 탄환비 | 크기 **×2** · 파도 **셋** · 파도마다 **0.22초**(⚠️ 셋 다 가정) |
| ⑩ | 설치 전 「방향 0/90/180/270」 버튼 | **폐기**(`a27182a` 3/3 되돌림) · `_placeRotation`·`DrawRotationArrow` 도 걷었다 |
| ⑪ | 노드 이름 | **롤백 — 기초 가공소 · 복합 가공소**. `proc` 변환기 · `shield` 보호막은 그대로 |

⚠️ 함께 들어간 사용자 확정 둘 — 적 포탄 **속도 6 → 4.2**(「지금 속도의 70%」)와
**크기 0.25 → 0.5**(⚠️ 크기는 가정 · 「안 보인다」에 대한 답).

## 3-2. 결함 넷

**① ⚠️⚠️ `BoardController` 가 회전을 무시했다** — 제 놓는 손을 따로 갖고 있어 회전 칸을
안 읽었다(**같은 일을 하는 자리 둘**). 하네스는 `StartingBoardB.Apply` 를 지나 면 경고 0 칸인데
화면은 「면이 다름」이 떴다 — **둘이 다른 판을 세우고 있었다.** 문을 하나로 합치고 시험으로 지킨다.

**② ⚠️⚠️ 광역형 절반이 수명을 두 배로 만들고 있었다** — 충전 소비가 타격당 피해와 **같은 수**라
절반으로 줄이니 두 배를 때렸다(한 표적 총 피해가 그대로다). **먹는 양과 주는 양을 갈랐다.**

**③ 드론이 공격할 때 사라졌다** — 적·로봇과 **같은 정렬 층 0** 이라 붙는 순간 뒤로 숨었다.
액터 층 안에서 두 칸 올렸다.

**④ 태그 섬광이 화면을 통째로 덮었다** — 흰 사각에 색만 입혀 **불투명**이었다.
반투명 **0.35** · **0.18초**로 바꿨다(⚠️ 둘 다 가정).

**⑤ 병합기·분류기가 안 보였다** — 보드에서 「합」·「분」 **검은 글자가 그림을 덮고** 있었다
(09-10 품목 글자와 같은 자리). 글자를 걷고 팔레트 버튼에 그림을 얹었다.

## 3-3. 답 둘

- **부스터 효과음은 없다.** 조달하려면 지시가 필요하다(아직 안 했다 · 10장).
- **태그 스킬 진단 줄** — **개발 빌드에서만** 표적 수·총 피해·한 체 몫을 화면에 적는다.
  ⚠️ 그 줄은 지금 **「i」 패널 안**으로 들어갔다(4장).

📌 **9장 정정 둘이 바로 이 커밋의 수정이다** — ①이 9장 ②(회전)이고 ②가 9장 ①(충전량)이다.

---

# 4. 전투 화면 — 무엇이 어디로 갔는가

| 자리 | 지금 든 것 | 종전 |
|---|---|---|
| 상단 칩 줄 | 썸네일 + 「MechPilot01」 + 「전투력 n」 / 골드 칩(수 + `+n/초`) · 소리 · 설정 | 없음(상단 정보줄 120 은 빈 자리였다) |
| 칩 줄 아래 왼쪽 | S1 배지 + 목표 한 줄 + 「남은 몬스터 n · 리젠 mm:ss」 | 글자 블록 열두 줄의 첫 두 줄 |
| 적 | **발밑 얇은 빨간 바** | 머리 위 초록 바 |
| 로봇 | 몸 밑 HP 바(수치) · 보호막 바(수치) · 회피 눈금 | 글자 줄 안의 「적 n/n 로봇 HP …」 |
| 오른쪽 기둥 | 태그 원형(**적재 링**) → 「자동」 토글 → 합체 원형 → 마일스톤 카드 | 태그 · 합체 둘뿐, 토글은 태그 **위** |
| 좌측 「i」 | 개발 빌드 전용 접이식 진단 패널(물류·갭·발사율·저장고·창고·추진제·회피 누적·얼굴·태그 스킬) | 화면 본문이던 글자 블록 |
| 그 밖 | **없다** — 글자 블록·헤더·접기 버튼·튜토리얼 진행 두 줄 전부 폐기 | |

**📌 소리 버튼은 자리가 하나다.** 칩 줄은 **소리 버튼을 안 그린다** — 자리(`UiLayout.ChipSoundRect`)만
내주고 `AudioOptionsPanel` 하나가 거기에 제 버튼을 그린다. 칩 줄이 따로 그리면
**같은 일을 하는 자리가 둘**이 되고, 그것이 이 리포에서 되풀이된 결함 종류다.

**🗑️ 함께 폐기 — 튜토리얼 진행 두 줄**(`Stage0Session.OnGUI`). 같은 두 줄을 우하단 카드가
보이므로 두 벌이 뜬다. **판정은 `Stage0Goal` 에 그대로 남고** 옮긴 것은 그리는 일뿐이다.

---

# 5. 신설 넷

## 5-1. 드롭·마그넷
처치한 자리에 재화가 떨어져 **잠깐 멈췄다가** 로봇에게 빨려 들고 「+n 고철」이 떠오른다.
⚠️ **적립을 여기서 안 한다** — 고철은 `KillRewardRule`, 골드는 `GoldRewardRule` 이
방치 런타임 안에서 이미 했고, 화면은 **그 일이 일어났다는 것을 보이게** 할 뿐이다.
마리당 고철 값도 `IdleSignals` 가 게시한 것을 읽는다(같은 값을 두 자산 경로로 안 읽는다).
⚠️ 코인 스프라이트는 아트 몫이며 **오기 전에는 원형 폴백**이다.

## 5-2. 골드 지갑 — **규칙 신설**
「몬스터 20처치마다 5골드」(사용자 값). `CurrencyWallet.Gold` · 저장 · 활용처 없음.
⚠️⚠️ **남은 처치 수를 저장이 든다**(`SaveDataV1.killsTowardGold`) — 그것이 없으면
19 마리를 잡고 끈 사람이 켤 때마다 0 에서 다시 센다.
⚠️ 값 둘(20 · 5)은 `EconomyConfig` **한 자산에만** 살고 규칙은 수를 안 든다.

## 5-3. 웨이브 스폰 — **규칙 신설 · ⚠️ 가정 · 설계 판정 자리**
⚠️⚠️ **종전 스폰에는 「리젠까지 남은 시간」이 없다.** `index × cadence` 는 늘 0.8초 뒤에
하나가 나오므로 타이머를 걸면 같은 수만 되풀이한다. 묶음과 묶음 **사이의 빈 시간**이
있어야 셀 것이 생긴다. 그래서 **묶음 W, 간격 T** 로 바꿨다.

📌 **평균 속도는 안 바꿨다.** 간격이 0 이면 `W × spawnCadence` 로 끌어오므로
평균 마리/초가 종전과 같다(4 / 3.2 = 1 / 0.8 = 1.25). **값을 지어내지 않고 있는 값에서 끌었다.**
달라지는 것은 **고르게 오던 것이 뭉쳐서 온다**는 것 하나다.

**실측 — S1 Win 유지**: 태그 두 로봇 하네스 `Win 77.7초 · 남은 적 0/100 · 로봇 HP 1000/1000`
(웨이브 전 `81.2초`). 하네스에도 같은 웨이브를 걸었다 — 안 걸면 **하네스는 한 마리씩 오는
판을, 게임은 묶음으로 오는 판을** 돌린다(09-15 에 겪은 「프로브는 초록인데 화면은 낡았다」).

⚠️ **묶음 크기 4 는 가정이다.** 묶음 1 이면 식이 종전과 같아져 **되돌릴 길이 값 하나로 남아 있다.**

## 5-4. 마일스톤 카드
우하단. 튜토리얼 목표 둘 + 「보상 받기 · 골드 20 / 고철 50」.
⚠️ **보상액은 가정**(설계 역기입 자리). ⚠️ **한 번만 받는다.**
⚠️ **지급은 방치 런타임이 한다** — 전투가 지갑을 직접 안 만진다.

---

# 6. 조립 화면 상단

경고 띠(768~864)를 **상단 상태 영역**으로 넓혔다 — 높이 **96 → 240 · 상시**
(✅ 사용자 확정 09-18 · 설계 역기입 대상 · UI 12장 띠).
든 것은 **고철 수치**와 **문제 목록**(미연결 · 생산 정지 · 전력 부족 · 마운트 빔).

🗑️ **「경고가 있을 때만 뜬다」 폐기.** 자리에 수치가 들어왔으니 늘 있어야 한다 —
있다 없다 하는 자리에 수치를 얹으면 「왜 사라졌나」가 생긴다.
⚠️ 문제 판정은 전부 이미 있던 함수가 내고 `Problems` 는 **말로 옮기기만** 한다.
⚠️ 목록이 비면 「문제 없음」 한 줄이다 — **없는 문제를 지어내 채우지 않는다.**
⚠️ 윗변 768 은 그대로라 인셋과 맞닿는 관계(`CombatInsetView.ClearsBand`)는 안 바뀐다.

---

# 7. 가정 목록 — 전부

**V02 게시 뒤 들인 가정 전부다** — 시안 3(1~24)과 **리허설 반영**(25~31 · `ba3a648`)을
한 표에 모은다. 따로 두면 「어느 문서에 적었더라」가 되고, 그 사이에서 값이 샌다.

| # | 가정 | 사는 곳 | 근거 |
|---|---|---|---|
| 1 | 칩 줄 높이 = 상단 정보줄 120 | `UiLayout.ChipBarHeight` | 문서에 칩 줄 치수가 없다 · 이미 있는 수에서 끈다 |
| 2 | 칩 줄 좌우 여백 40 | `UiLayout.ChipBarPad` | 상태창 x40 과 같은 값 |
| 3 | 아이콘 한 변 = 최소 버튼 150 | `UiLayout.ChipIconSide` | UI 6-2 규격 |
| 4 | 배지 줄 760 × 210 | `UiLayout.StageBadgeRect` | 상태창과 같은 폭(화면 절반을 안 넘긴다) |
| 5 | 태그 원형 y **1680**(구 2080 폐기) | `UiLayout.TagButtonCenter` | ✅ **사용자 확정 09-18 · 설계 역기입**(UI 아트 4장 좌표) — 기둥이 넷이 되어 다시 쌓았다 |
| 6 | 합체 원형 y **2000**(구 2300 폐기) | `UiLayout.MergeButtonCenter` | ✅ **사용자 확정 09-18 · 설계 역기입**(UI 아트 4장 좌표) |
| 7 | 자동 토글 300 × 80 · y1840 | `UiLayout.TagAutoToggleRect` | 설계가 준 차례(태그 → 자동 → 합체) |
| 8 | 마일스톤 카드 560 × 240 · y2120 | `UiLayout.MilestoneCardRect` | 합체 아랫변 2100 과 조립 막대 윗변 2380 사이 |
| 9 | 마일스톤 보상 골드 20 · 고철 50 | `MilestoneCard` 상수 | 문서에 마일스톤 보상 절이 없다 |
| 10 | 전투력 = **물류 출력 그대로**(마운트계수 **0 = 안 곱한다**) | `CombatTuning.combatPowerMountFactorTbd` = 0 | 🗑️ 구 「출력 × (1 + 적재율 × 계수) · 계수 1」 폐기 — 설계 검토 ③. **지시에 없던 새 식**이었고 적재율만큼 전투력이 최대 두 배로 부풀어 **같은 화면의 물류 출력과 갈렸다.** 마운트계수는 **값이 없는 칸**이지 1 로 채울 칸이 아니다 — **설계 판정 대기** |
| 11 | 웨이브 묶음 4 | `CombatTuning.waveSizeTbd` | 문서에 웨이브 절이 없다 |
| 12 | 웨이브 간격 = 묶음 × cadence | `CombatTuning.waveIntervalSecondsTbd` = 0 | 평균 속도 불변 |
| 13 | 드롭 멈춤 0.25초 · 빨림 0.45초 · 크기 0.3유닛 | `CombatTuning.drop*Tbd` | 연출값 |
| 14 | 팝 0.9초 · 떠오름 0.8유닛 | `CombatTuning.dropPop*Tbd` | 연출값 |
| 15 | 상단 상태 영역 높이 **240 · 상시** | `SupplyStopRules.DesignBandHeight` | ✅ **사용자 확정 09-18 · 설계 역기입**(UI 12장 띠) — 머리 한 줄 + 목록 석 줄 최소 |
| 16 | 적 체력 바 발밑 −0.62 · 두께 0.10 | `CombatEntityView` | 「발밑 얇은 빨간 바」 |
| 17 | 보호막 바 = HP 바 **아래** 한 칸(구 위 폐기) | `CombatEntityView` | HP 가 발밑으로 내려왔다 |
| 18 | 로봇 수치 시작 = 몸 −(반경 + 0.9) | `StageRunner.Hud` | 월드 막대를 안 가리는 선 |
| 19 | 설정 아이콘 = **메인 메뉴를 다시 연다** | `MainMenuGate.Open` | 새 설정 화면을 지어내지 않는다 |
| 20 | 소리·설정 아이콘은 **글자 두 자** | `StageRunner.Hud` · `AudioOptionsPanel` | 아이콘 자산이 없다 |
| 21 | 썸네일 = 로봇 A 스프라이트 · 없으면 원형 폴백 | `StageRunner.Hud` | 얼굴 크롭은 아트 몫 |
| 22 | 적재 링 = **사각 테두리** 둘레 | `StageRunner.Hud.DrawLoadRing` | 원호 그림 자산이 없다 |
| 23 | 초당 골드 = 지급 누계 ÷ 경과 | `StageRunner.Hud.GoldPerSecond` | 비를 화면이 다시 적지 않기 위해서다 |
| 24 | 진단 패널은 개발 빌드 전용 | `Debug.isDebugBuild` | 심사자 화면에서 소음이다 |
| 25 | 태그 섬광 알파 **0.35** | `CombatTuning.tagFlashAlphaTbd` | 리허설 ⑥ — 불투명이라 화면을 덮었다. 「얼마나 옅어야 하나」는 문서에 없다 |
| 26 | 태그 섬광 지속 **0.18초** | `CombatTuning.tagFlashSecondsTbd` | 리허설 ⑥ — 0.40초를 가득 덮으면 「가림막」으로 읽힌다 |
| 27 | 탄환비 크기 **×2** | `CombatTuning.tagBulletSizeMultTbd` | 리허설 ⑦ — 38 아트 픽셀은 화면에서 점이었다 |
| 28 | 탄환비 파도 **셋** | `CombatTuning.tagBulletWavesTbd` | 리허설 ⑦ — 「파도 여럿이 빠르게」의 수는 안 주어졌다 |
| 29 | 파도 간격 **0.22초** | `CombatTuning.tagBulletWaveSecondsTbd` | 리허설 ⑦ — 위와 같다 |
| 30 | 적 포탄 한 변 **0.5유닛**(구 0.25) | `CombatTuning.enemyProjectileViewUnitsTbd` | 리허설 ⑨ — **속도 70%는 사용자 확정**이고 크기는 「보이게」만 주어졌다. 격자 반 칸으로 잡았다 |
| 31 | 드론 정렬 층 = **액터 + 2** | `SortingLayers.Actor + 2` | 리허설 결함 ③ — 같은 층 0 이라 붙는 순간 숨었다. 몇 칸 올릴지는 문서에 없다 |

---

# 8. 청하는 것

1. **나머지 28 을 역기입할 것인가** — 5·6·15 는 **사용자가 받았다**(09-18 · 설계 역기입 대상).
   남은 것은 자리·크기·연출값·문구이며 10 은 역기입이 아니라 **값 판정**(마운트계수)이다.
   ⚠️ 25~31 은 **육안이 고른 연출값**이라 문서에 절이 없다 — 절을 낼 것인지부터가 판정이다.
2. **웨이브 스폰을 규칙으로 승인할 것인가**(5-3). 승인하면 묶음 크기가 값 칸에 선다.
3. **골드 활용처** — 지금은 쌓이기만 한다.
4. **광역 반경·본전 수는 W02 대기**(2장) — 사용자가 「S1 에서 약해도 된다 · 값 불변」으로
   닫았고, 실측은 **뒤 판일수록 간격이 벌어진다**(S5 에서 r=2 는 1.00 마리)를 말한다.
   `droneAoeDamageFactor` 를 좌표 `dBAoe` 50 으로 **이관하는 것은 W02 문안 뒤**다.
5. **UI 문서 「노드 회전」 절 역기입** — 사전 회전 버튼을 없앴다(09-18 오전).

---

# 9. 정정 둘 — 회신문 하나 · 세션 사이 독법 하나

**① 「⚠️ 줄인 것은 타격당 피해뿐이고 충전량·수명은 그대로다」(`260918_V02` 1장 ③)는 틀렸다.**
그때 코드에서 **충전 소모가 피해와 같은 값**이었다 — 피해를 절반으로 줄이면 충전도 절반만
닳아 **수명이 두 배**가 됐다. 「그대로」가 아니라 **반대로 움직이고 있었다.**
고친 것은 `DroneUnit.Fire` 를 **먹는 양과 주는 양으로 가른 것**이고, 그래야 회신문의
문장이 코드에서 참이 된다.

**② 「면이 다름 = 거짓 경고」 — 출처를 바로잡는다.**
⚠️ **이 말은 회신문에 없다.** 계획 세션의 독법이었고(플랜 §85-7 · 세션 메시지),
**구현이 확인 없이 받았다.** 실제로는 하네스가 회전한 판을 돌리는 동안
`BoardController` 가 **제 배치 루프**를 갖고 있어 회전을 무시했다 —
**게임은 회전 없는 판을 돌리고 있었다.** 경고가 맞았고 받아 적은 쪽이 틀렸다.
지금은 `StartingBoardB.Apply` **한 문**으로 들어간다.

📌 **고칠 것은 코드가 아니라 받는 법이다** — 남의 독법을 확인 없이 제 판정으로 삼으면,
그 판정이 회신문에 실려 **출처 없는 사실**이 된다.

📌 **둘 다 같은 뿌리다** — 같은 일을 하는 자리가 둘이면, 한쪽만 보고 한 말이 문서에 남는다.
①은 코드에서, ②는 세션 사이에서 그 일이 났다.

---

# 10. 남은 것

- **아트** — `Docs/art_log/hud_face_v1_actual.png` 가 나와 있다(얼굴 크롭). 아직 `Assets` 로
  안 들어와 있어 **원형 폴백**이 뜬다. 코인 스프라이트도 아직이다.
- **효과음** — 드롭·마그넷·보상 받기에 소리가 없다. 사용자가 원하면 조달+훅(지시 대기).
- **육안** — 「리허설」 대상은 아래 열셋이다(11장).

---

# 11. 리허설 — 볼 것 열셋

| # | 볼 것 |
|---|---|
| 1 | 상단 칩 줄 — 썸네일·닉네임·전투력 / 골드 칩·소리·설정이 **안 겹치는지** |
| 2 | 골드 칩의 「+n/초」가 처치와 함께 움직이는지 |
| 3 | 배지 줄 — S1 · 목표 · 「남은 몬스터 n · 리젠 mm:ss」 |
| 4 | **리젠 타이머가 실제로 줄었다가 묶음이 오는지**(웨이브) |
| 5 | 적 체력이 **발밑 빨간 바**인지 |
| 6 | 로봇 몸 밑 — HP 수치 · 보호막 수치 · 회피 눈금 |
| 7 | 태그 원형 둘레의 **적재 링**이 차는지 |
| 8 | 태그 → **자동 토글** → 합체 차례가 세로로 서는지 · 셋 다 눌리는지 |
| 9 | 처치할 때 **재화가 떨어져 빨려 들고** 「+n 고철」이 뜨는지 |
| 10 | 스무 마리쯤에서 **골드가 하나 떨어지는지** |
| 11 | 우하단 **마일스톤 카드** — 목표 둘 체크 · 「보상 받기」가 한 번만 눌리는지 |
| 12 | 조립 화면 상단 — **고철 수치 + 문제 목록**(문제가 없으면 「문제 없음」) |
| 13 | 칩 줄의 **전투력이 같은 화면의 물류 출력과 같은 수**인지(「i」 패널 첫 줄과 견준다) |

⚠️ 좌측 「i」는 **개발 빌드에서만** 보인다. 웹빌드에서 안 보이는 것이 정상이다.
